Переведи текстовые поля каждой записи с русского на $lang_name.
Это МАССИВ (не объект) — порядок записей в ответе должен ТОЧНО совпадать с порядком во входе,
ничего не пропускай и не переставляй местами (по позиции сверяются с ФИО учёного — их тут нет,
только текст, порядок — единственная связь).
НЕ переводи и не трогай поле related_tags (список id, оставь как есть).
Поле fields и key_discoveries — списки строк, переведи каждую строку, структуру списка сохрани.

$payload

Ответь JSON-объектом: {"items": [ ...та же длина массива, переведённые записи в том же порядке... ]}
