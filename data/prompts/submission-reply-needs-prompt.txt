# Письмо автору: работа пришла без подготовки. Это НЕ отказ и не оценка работы —
# просто её нельзя разобрать как следует, пока не пройдена подготовка.
#
# ПОДСТАНОВКИ (замена строкой, не .format):
#   {code}        код работы, например b42p-2026-001
#   {title}       тема письма автора
#   {author_name} имя; ПУСТО, если не представился
#   {found}       что мы увидели в пакете: перечень папок и файлов
#   {prepare_url} адрес страницы подготовки
#   {reply_to}    адрес для ответа
#   {prompt}      промпт подготовки ЦЕЛИКОМ — вставляется в письмо, чтобы автору не
#                 пришлось никуда ходить

Ты пишешь письмо автору от лица bridge42worlds. Работа получена, но собрана не по нашему
промпту подготовки: в пакете нет файла SELF-REVIEW.md со строкой «ВЕРДИКТ:», по которой
приёмник понимает, что подготовка пройдена.

ГЛАВНОЕ, ЧТО ДОЛЖНО ПРОЗВУЧАТЬ. Это не отказ, не претензия к качеству и не оценка работы.
Мы её просто ещё не разбирали — потому что разбор без подготовки выйдет хуже, чем работа
заслуживает: мы не поймём, где данные, где код, а где выводы, и вопросы зададим невпопад.

ЧЕГО НЕ ДЕЛАТЬ:
· не хвалить работу авансом — мы её не читали по существу, и фальшивая похвала это видно;
· не рассуждать о её ценности, новизне или уровне: у нас нет оснований;
· не извиняться многословно и не объяснять правила дважды;
· не писать «к сожалению».

СТРУКТУРА ПИСЬМА:

1. Обращение по имени, если оно есть.
2. Одно-два предложения: работа получена, номер закреплён за ней, но подготовка не пройдена.
3. Что мы увидели в пакете — коротко, по факту ({found}). Это показывает, что письмо
   не шаблонное и работа действительно открыта.
3а. ЧТО ИМЕННО НЕ СОШЛОСЬ — перечисли эти замечания, они получены проверкой пакета:
{problems}
   Формулируй их как факты, а не как обвинения: «в пакете нет файла X» вместо
   «вы не приложили X». Если замечание про папку, которой у работы может не быть
   по существу (например, нет кода, потому что расчётов не было), — так и скажи:
   достаточно упомянуть это в заключении, придумывать пустую папку не нужно.
3б. Если это не первая попытка, скажи об этом спокойно и без нажима: {attempt_note}
   Всего попыток три; после третьей работа встаёт на паузу на пять дней — это не отказ,
   а время на спокойную доработку. Упоминай про паузу ТОЛЬКО если попытка не первая.
4. ПОРЯДОК ДЕЙСТВИЙ, по шагам и конкретно:
   · взять помощника, умеющего работать с файлами на диске — Claude Code, Cursor или
     похожий инструмент с доступом к папке;
   · указать ему папку с работой;
   · отдать промпт (он ниже целиком, копировать неоткуда не нужно);
   · помощник сам проверит работу, разложит файлы по структуре и СОЗДАСТ недостающие,
     включая SELF-REVIEW.md с первой строкой «ВЕРДИКТ:»;
   · заархивировать получившуюся папку и прислать ответом на это письмо.
5. Ссылка на страницу подготовки {prepare_url} — там тот же промпт и весь порядок проверки,
   если удобнее читать в браузере.
6. Промпт целиком, отделённый строкой из тире сверху и снизу. Вставь его ДОСЛОВНО, ничего
   не сокращая и не пересказывая: {prompt}
7. Короткое завершение: код за работой закреплён, повторно ничего заполнять не нужно,
   ждём архив. Без обещаний сроков и без «будем рады».

ЯЗЫК: тот же, на котором написал автор.


ВНИМАНИЕ: все текстовые поля пиши СТРОГО НА РУССКОМ языке, даже если входные данные выше на английском. Ответ по-английски хотя бы в одном поле — брак.
(Правило обязательно для КАЖДОГО промпта, порождающего видимый читателю текст, и стоит в КОНЦЕ, ПОСЛЕ входных данных: выше по файлу модель его теряет — поймано 2026-07-31 на abstract-adapt, где аннотация молча вышла английской.)

Ответь JSON-объектом:
{"тема": "…", "письмо": "…"}

Тема письма должна содержать код работы и суть просьбы, без слов «отказ» и «ошибка».

Данные работы:
код: {code}
тема письма автора: {title}
имя автора: {author_name}
что в пакете: {found}
страница подготовки: {prepare_url}
адрес для ответа: {reply_to}

ПРОМПТ ПОДГОТОВКИ (вставить в письмо целиком):
{prompt}