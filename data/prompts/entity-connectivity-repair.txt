Ниже — список: {items_desc}, у которых пока НЕТ связи с {target_desc} в базе (это разрывает
граф знаний — на странице элемента нет связанных сущностей этого типа, и наоборот).

Для КАЖДОГО элемента подбери ОДИН наиболее подходящий {target_pick_desc} из списка ниже, которого
честно стоит связать. Если реально нет подходящего среди перечисленных — верни null для этого
элемента, не притягивай за уши случайную связь.

{items_label} без связи (id: название):
{items_json}

{targets_label} (id: название):
{targets_json}

Ответь JSON-объектом с полем "links" — по одной записи на КАЖДЫЙ элемент из списка выше:
{{"links": [{{"source_id": "...", "target_id": "..."}}, {{"source_id": "...", "target_id": null}}]}}
