# Замечания по интерактивному курсу — рабочий список

Накопитель фидбека по `course.html` / `memo.html` / движку. Отмечать сделанное `[x]`.
Источник: юзер, 2026-07-17 (сессия сборки курса «Термодинамика»).

---

## A. Баги — сломано, чинить первым

- [ ] **A1. В шпаргалке (`memo.html`) не рендерятся формулы.** Видны сырые `$1{,}4$`,
  `$\sqrt{2} \approx 1{,}41$`, `$C_P - C_V = R$`, `$\eta_{\max} = 1 - T_х/T_г$` в блоках
  «Как запомнить» и «Типовой расчёт». Причина: в memo.html нет auto-render KaTeX (есть только
  явный `katex.render` для главной формулы). → подключить `auto-render.min.js` и прогнать по карточке.
- [ ] **A2. Текст наезжает на приборы в первом стенде.** В модели газа подпись/строка значений
  перекрывает манометр и термометр на динамической картинке. → пересчитать раскладку сцены:
  зарезервировать зону приборов, текст не должен в неё заходить.

## B. Физика и поведение моделей

- [ ] **B1. Связанные ползунки (повтор, говорил ранее).** Двигаю один параметр — остальные
  зависимые величины должны меняться на глазах. Сейчас часть индикаторов статична.
- [ ] **B2. Убрать вылет шариков через дырку в модели газа.** Эффузия ломает главную идею
  параграфа: если частицы улетают, давление не может быть постоянным. Для первого параграфа
  сосуд должен быть замкнут. (Эффузию можно оставить как отдельный опциональный режим позже.)

## C. Формулы, константы, масштаб — архитектурное

- [ ] **C1. Везде, где формула — только KaTeX.** Сквозное правило, включая переменные и
  соотношения внутри текста, подписи, шпаргалку, тултипы. Никакого сырого текста формул.
- [ ] **C2. Сразу после формулы — расшифровка ВСЕХ её символов.** Записали `PV = nRT` → тут же
  таблица: каждая переменная, её размерность, что означает. Сейчас константы идут отдельным
  блоком внизу — это поздно. Порядок: формула → что означает каждый символ → потом остальное.
- [ ] **C3. Тултип-карточка на каждую константу.** Наведение/клик на символ в любом месте курса →
  всплывает мини-карточка: значение, единицы, смысл, мнемоника.
- [ ] **C4. Общий словарь констант (переиспользуемый).** Раз список констант всё равно
  образуется — вынести в единый справочник (`data/theory/constants.json`), а уроки ссылаются
  на записи по ключу. Одна константа описана один раз, используется во всех 50 темах.
- [ ] **C5. Нумеровать все формулы сквозно** и сделать формулу тоже переиспользуемой карточкой
  (как константы) — чтобы быстро собирать новые темы из готовых блоков.
- [ ] **C6. Делать масштабно сразу** — решения принимать в расчёте на 50 тем × 4 материала,
  а не под одну термодинамику.

## D. Подача и UX

- [ ] **D1. Шпаргалку показывать мини-превью,** а не просто текстовой ссылкой «🖨 шпаргалка» —
  видно, что это карточка.
- [ ] **D2. Результат параграфа показывать сразу.** Сейчас пишем «Параграф пройден: 1 из 3.
  Результат сохранён — он виден в оглавлении курса», но не показываем сам разбор/итог на месте.
  Не жалко — показать сразу.

## F. Образные картинки (генератор)

- [x] **F1. Одна образная картинка на параграф** — для визуального запоминания. Сделано: FLUX через
  наш `gen_llm.generate_image(prompt, path, preset='image_cheap')` (DeepInfra, FLUX-1-schnell, дёшево).
  Лежат в `data/theory/courses/<topic>/img/<lessonId>.jpg`, показываются героем в начале параграфа
  и миниатюрой в оглавлении (закрывает заодно часть D1 — превью вместо голой ссылки).
  **Промпт-рецепт, который сработал:** editorial science illustration + warm cream paper background +
  muted ink/teal/brass palette + soft grain + vintage textbook aesthetic + **no text, no letters,
  no numbers** (важно: без текста картинка не требует перевода на 4 языка) + generous negative space.
  Сама попадает в палитру наших theory-статей.
- [ ] **F2. Проверять результат глазами.** Первый вариант картинки двигателя вышел нечитаемым
  (петля цикла выглядела как ручка ведра) — перегенерация с конкретной композицией
  («side view cross-section, piston, connecting rod, spoked flywheel») дала нужное. Абстрактные
  просьбы («loop suggesting a cycle») FLUX понимает плохо, конкретные предметы — хорошо.
- [ ] **F3. Распространить на остальные темы** при масштабировании на 50 тем: 1 картинка на параграф
  + возможно 1 обзорная на тему.

## E. Движок

- [ ] **E1. Декомпозировать движок анимации внутри.** Выделить переиспользуемые части
  (панели приборов, раскладка сцены, сетка индикаторов), чтобы новые стенды собирались
  быстро из готовых блоков, а не рисовались с нуля каждый раз.

---

## Порядок реализации (предложение)

1. **A1, A2** — видимые баги, дёшево.
2. **C4 + C2 + C3** — словарь констант, расшифровка под формулой, тултипы. Это одно связанное
   изменение и оно же самое ценное для масштаба.
3. **B2, B1** — физика моделей.
4. **C5, E1** — нумерация формул как карточек + декомпозиция движка (готовит масштаб на 50 тем).
5. **D1, D2** — полировка подачи.

---

## G. Сделано в проходе «методичка + связь с базой» (2026-07-17)

- [x] **G1. Методичка для самообучения** — `courses/<topic>/guide.json`, рендер `course.html?t=&p=guide`,
  ссылка стоит на обзорной странице. Разделы: сколько времени (таблица с бюджетом по параграфам,
  курс = 4–6 часов), на кого рассчитан (вход: Ньютон + производная; выход: вводный вуз-курс),
  как проходить параграф (7 шагов, главный — «поиграй со стендом ДО чтения вывода»),
  что делать если застрял, опыты по параграфам, материалы по теме, куда дальше. **RU + EN.**
- [x] **G2. Битые ссылки на сущности — исправлены.** Проверка показала: страниц Карно, Клаузиуса,
  Джоуля, Менделеева, Кельвина в базе НЕТ; тегов `thermodynamics`, `pressure`, `heat_engine` НЕТ;
  законов `clausius_clapeyron_relation`, `latent_heat`, `adiabatic_process`, `carnot_cycle` НЕТ.
  Все `entities` в уроках переведены на реально существующие страницы, проверено скриптом — 0 битых.
  **Реально есть:** законы ideal_gas_law, zeroth/first/second/third_law_of_thermodynamics,
  carnot_theorem, boltzmann_distribution; учёные Emile_Clapeyron, Ludwig_Boltzmann;
  теги temperature, entropy, phase_transition.
- [ ] **G3. ДОФОРМИРОВАТЬ ПУЛ (задача для общей системы).** Не хватает страниц, на которые
  курс естественно ссылался бы: **учёные** Сади Карно, Рудольф Клаузиус, Джеймс Джоуль,
  Дмитрий Менделеев, Уильям Томсон (Кельвин); **законы** уравнение Клаузиуса–Клапейрона,
  скрытая теплота, адиабатный процесс, цикл Карно/Отто; **теги** термодинамика, давление,
  тепловые машины. Пока курс ссылается только на существующее.
- [x] **G4. Единое поле прибора (E1, частично).** Сцена + ползунки + формула + график теперь
  в ОДНОЙ рамке (`.xpl`), разделены тонкими линиями — визуально один инструмент.
- [x] **G5. Наезд на приборы (A2).** В модели газа справа жёстко зарезервирована зона
  (`GAUGES=122`, `ROD=54`): сосуд с поршнем физически не может дойти до термометра и манометра.

## H. Новые требования (2026-07-17, вторая волна)

- [x] **H1. Единицы меняются на приборах стенда** — проверено перехватом отрисовки: манометр и
  термометр реально пересчитываются (бытовая `1.97 атм / 27 °C / 5.0 л` → СИ `199536 Па / 300 K / 0.0050 м³`).
- [x] **H2. Наезд подписей на манометр — устранён по-настоящему.** Настоящая причина была не в
  ширине зоны: манометр и термометр стояли В РЯД, и их подписи («27 °C» и «1,97 атм») пересекались.
  Приборы разнесены в вертикальную колонку (манометр сверху, термометр снизу), подписи термометра
  вынесены СБОКУ. Разнос по вертикали ~100 px, пересечься нечему.
- [x] **H3. Связанный бегунок (B1) — сделан.** В адиабате/изобаре температура вычисляется из объёма;
  теперь её ползунок едет сам (`syncDependentSlider`, без dispatch 'input' — иначе рекурсия).
  Проверено: V=1.5 л → T=490 K, V=10 л → T=230 K, бегунок и подпись двигаются.
  **Важно про тесты:** в headless-вкладке rAF не тикает, поэтому статика в проверке ≠ статика у юзера;
  проверять принудительным `api.render(t)` с разным t.
- [x] **H4. Кнопки режимов на странице параграфа.** Их вообще не было в новом рендерере —
  третий параграф показывал только изотерму. Добавлены табы + подсказка режима.
- [ ] **H5. Единицы должны меняться ВЕЗДЕ, включая динамические графики** (оси, подписи делений,
  маркер на кривой). Сейчас график в единицах модели.
- [ ] **H6. Чайник: нарисовать точнее, замедлить процесс закипания** (сейчас слишком быстро,
  глаз не успевает прожить плато).
- [ ] **H7. Свои иконки, карточные** — единый набор (лампочка-внимание и т.п.) вместо эмодзи.
- [ ] **H8. Движок — универсальный показ ЛЮБОГО закона** (общая цель, см. E1).

## I. Программа курса — 15 тем

- [x] **I1. Спроектировано ядро:** `data/theory/courses/CURRICULUM.md` — 15 тем в 4 блоках
  (механика → колебания/волны/тепло → электромагнетизм → современная физика), 15 × 4 = 60 материалов.
  Каждая тема: на что опирается, куда ведёт, какие стенды. Плюс сквозной математический трек
  (где какой аппарат впервые нужен) и карта перекрёстных связей (осциллятор 5→10→14,
  сохранение энергии 3→7→12→15, волна 6→11→13, вероятность 8↔13).
- [ ] **I2. Отдельный вводный материал «Что такое физическая математика»** — размерности как
  проверка, порядок величины важнее точности, предельные случаи как способ проверить формулу.
- [ ] **I3. Собирать по порядку:** тема 1 (эталон механики) → 2, 3 → 5 (колебания, даёт аппарат
  для 10 и 14) → дальше по номерам.

## J. Проход «потихоньку» — сделано (2026-07-17)

- [x] **A1. Формулы в шпаргалке** — подключён auto-render KaTeX в `memo.html`, сырых `$...$`
  не осталось (проверено: 14 KaTeX-узлов, `rawDollarsLeft: []`).
- [x] **H6. Чайник медленнее и точнее.** `timescale` 30 → 11 (закипание примерно втрое медленнее,
  плато успевает «прожиться»). Корпус перерисован: конусность к горлу, гнутый носик с расширением,
  дугообразная ручка, крышка с шишечкой.
- [x] **C4. Общий словарь констант** — `data/theory/constants.json` (13 записей: k_B, N_A, R, c, h,
  G, e, m_e, σ, g, c_воды, L, γ). Каждая: latex, значение, единицы, зачем нужна, мнемоника,
  пометка `exact` для зафиксированных с 2019 г., `derivedFrom` для производных (R = N_A·k_B).
  **RU + EN.** Урок ссылается по ключу — одна константа описана один раз на все 15 тем.
- [x] **C2. Расшифровка символов сразу под формулой.** В JSON урока `formula.symbols[]`:
  каждый символ, что означает, размерность, замечание (напр. «T ТОЛЬКО в кельвинах»).
  Рендерится прямо в карточке закона, до вывода и до блока констант.
- [x] **C3. Тултип-карточка на константу.** Наведение/клик на символ → всплывает карточка из
  общего словаря: значение, единицы, смысл, мнемоника, пометка о точной фиксации.
  Работает и в легенде символов, и в блоке констант параграфа.

## K. Ночной проход «продолжай без остановки» (2026-07-17)

- [x] **B2. Сосуд замкнут.** Эффузия стала опцией `gasModel(data, {effusion:true})`, ПО УМОЛЧАНИЮ
  ВЫКЛЮЧЕНА: в параграфе про уравнение состояния N обязано быть постоянным, иначе модель
  противоречит формуле, которую иллюстрирует. Крышка рисуется сплошной, счётчик «сбежало» скрыт,
  вместо него в строке показателей теперь N (число молекул).
- [x] **H5. Единицы на графиках.** `B42Units.raw/fmt` экспортированы из models.js; в `explorable.js`
  деления оси Y и подпись маркера пересчитываются, если ось помечена `kind` ('P' | 'T' | 'Tc' | 'V').
  Проверено на стенде газа: бытовая — ось до 10.34 атм / маркер «1.97 атм»; СИ — 1048000 Па /
  «199536 Па»; имперская — 152 psi / «28.9 psi». `Tc` — отдельный вид для чайника (он считает в °C).
- [x] **H7. Свои иконки.** `js/icons.js` — 18 штриховых SVG (lamp, flask, sigma, scale, person, tag,
  check, clock, printer, ruler, pencil, hash, book, home, star, warn, chat, history). Наследуют
  currentColor, тонкая линия — в стиле theory-статей. Эмодзи из курса убраны, включая кнопку
  тьютора (`tutor.js` берёт иконку из набора, эмодзи только как запасной вариант).
  **Важно:** `icons.js` должен грузиться ПЕРЕД `tutor.js`.
- [x] **D2. Итог параграфа сразу.** После последнего ответа показывается карточка: балл, полоса,
  вердикт («Отлично» / «пара мест требует возврата» / «стоит перечитать»), **разбор по каждому
  вопросу** (✓/✕ с текстом) и подсказка про «подведи к идее». Проверено: 2/3 → верный разбор.

**Осталось из накопителя:** C5 (сквозная нумерация формул как переиспользуемых карточек),
E1 (полная декомпозиция движка на панели), F3 (картинки для остальных тем),
G3 (доформировать пул сущностей — задача для общей системы), I2 («что такое физическая математика»),
I3 (собирать темы 1→2,3→5 по CURRICULUM.md).

## L. Эталонная доработка механики (2026-07-26)

- [x] **БАГ collision исправлен.** Тела сталкивались по таймеру (HIT=3 c), а не когда реально
  встретились: могли пройти сквозь друг друга или удариться на расстоянии. Теперь момент касания
  считается физически: зазор между поверхностями делится на скорость сближения, радиусы учитывают
  массы. Если сближения нет или его не хватит за отведённое время — удара не происходит, и модель
  честно об этом пишет. Вспышка рисуется в точке фактической встречи.
- [x] **Момент импульса добавлен** в §3 механики: два шага вывода (L = r×p = Iω; сохранение при
  нулевом моменте внешних сил), схемы angmom и skater (фигуристка), отдельный вопрос.
- [x] **Границы модели** — врезка в конце вывода: законы сохранения прочнее самой механики, потому
  что следуют из симметрий; при околосветовых скоростях импульс уже mv/sqrt(1-v²/c²), энергия покоя
  mc², момент импульса в квантовом мире квантуется порциями h-bar. «Физика не отменяет старые законы,
  а уточняет область применимости».
- [x] **Аппендикс для любознательных** (новый блок appendix в схеме урока, рендерится сворачиваемым
  details, не входит в проверку): принцип наименьшего действия -> эквивалентность уравнениям
  Эйлера-Лагранжа и F=ma -> почему формулировка через действие универсальнее (ОТО, электродинамика,
  стандартная модель) -> мостик Фейнмана: частица идёт по всем путям сразу, классическая траектория
  есть результат интерференции -> практический вывод «не заучивайте догмы».
- [x] **Геймификация.** Звёзды за качество: 3 при >=90%, 2 при >=65%, 1 при зачёте. Суммарный счётчик
  в полосе прогресса, при полном прохождении темы — плашка «открыт доступ к следующей теме».
  Проверено: 3/3 -> 3 звезды, 3/4 -> 2, 2/5 -> 1, итог 6/9.

**Осталось из этой волны:** трение отдельным блоком, бильярд как сквозной пример (сохранение+трение+
падение), больше графиков при объяснении матана (что такое интеграл — по шагам), зачатки СТО в механике,
обобщающее определение механики, «нарисованные» картинки-мнемоники.
