# DevOps

Инфраструктура: Worker, маршруты, хранилища, доступы, наблюдение.
Полная инструкция по переезду — `cloudflare/DEVOPS-HANDOFF.md` (не в git, отдаётся файлом).

Отчёты — [отчёты/devops.md](%D0%BE%D1%82%D1%87%D1%91%D1%82%D1%8B/devops.md).

---

## 0. Авторизация и учёт токенов — ВПЕРЁД ВСЕГО ОСТАЛЬНОГО

Решение владельца 2026-07-29: ни векторный поиск, ни помощник в курсе не открываются,
пока нет учёта расхода.

Что нужно:

- **Вход** — минимальный: почта и одноразовый код, без паролей. Пароли означают хранение
  и утечку; одноразовый код не хранится.
- **Счётчик на пользователя** в KV: потрачено за сутки, потрачено за неделю.
- **Бесплатная норма** — число задаётся настройкой, а не зашито в код: мы будем его крутить.
- **Читателю видно, сколько осталось.** Не «превышен лимит» постфактум, а полоска
  «осталось 40 из 50 на сегодня» до того, как он начал.
- **Общий потолок на проект** — предохранитель поверх пользовательских: если что-то пойдёт
  не так, счёт не должен уходить в тысячи.

**Зачем так строго.** Всё, что дальше в этом файле, стоит денег за каждый запрос. Пока
бесплатных лимитов Cloudflare хватает, но именно поэтому легко проглядеть момент, когда
перестанет хватать. Счётчик, поставленный до запуска, — это единственный способ узнать
о проблеме раньше счёта.

**Готово, когда:** пользователь входит, видит свою норму и остаток, при исчерпании получает
понятное объяснение и предложение подождать до завтра. В журнале видно суточный расход
по проекту целиком.

---

## 1. Векторный поиск

Приоритет владельца: даёт и заметный прирост поиску, и основу для всего остального.

- Эмбеддинги статей (заголовок плюс аннотация) — моделью на Workers AI
- Индекс в Vectorize
- Worker `/api/search`: запрос → вектор → список статей

**Зачем.** Сейчас поиск ищет совпадение слов. В науке одно и то же называют по-разному
у разных авторов, поэтому совпадение слов — плохая мерка. По смыслу читатель найдёт то,
чего искал, даже не зная нашего термина.

**Что это открывает дальше** (не делать сразу, но закладывать): связи между статьями,
тегами, законами и учёными можно строить **по векторам, а не по тегам**. Теги остаются
основой и человеческой разметкой, вектор добавляет то, чего в разметке нет.

**Готово, когда:** запрос «квантовая гравитация и энтропия» находит статьи, где этих слов
нет, а смысл совпадает. Проверено вручную на десятке запросов.

---

## 2. Помощник в учебном разделе

Код бота написан и спрятан за флагом. Открывается **только после задачи 0**.

- Ключ модели — секретом Worker (`wrangler secret put`), не в странице
- Ответ строится по нашим материалам, найденным вектором из задачи 1
- Снять флаг: `window.B42_TUTOR_ENABLED = true`

**Правило, которое не обсуждается:** ответ без ссылки на конкретный наш материал
не показываем. Возможность проверить — то, что отличает нас от болталки.

**Готово, когда:** на странице параграфа помощник отвечает со ссылками, расход виден
в счётчике, ключ в исходниках страницы не встречается.

Подробности локального теста — `cloudflare/TUTOR-LOCAL-TEST.md`.

---

## 3. Лайки и просмотры — с Supabase на D1

Сейчас реакции живут во внешнем сервисе, и его ключ лежит прямо в `js/likes.js`.

**Зачем.** Убираем чужую зависимость и ключ из кода, ускоряем отклик (база на краю сети,
а не за океаном). Данных немного — перенос несложный.

**Готово, когда:** лайки и просмотры считаются через Worker, страница ключей не содержит,
старые данные перенесены, Supabase отключён.

---

## 4. Сменить токен Cloudflare

Он передавался в переписке при настройке.

**Готово, когда:** старый отозван, новый в `.env`, публикация и `wrangler deploy` работают.
Заодно выдать право `Zone → Workers Routes → Edit`: без него привязка маршрутов падает,
и мы это уже ловили на упавшем сайте.

---

---

## Сделано

- 2026-07-29 — проверена автопубликация: `run.py stats` сам вызвал деплой в R2 (0 файлов на
  заливку, всё синхронизировано); отдельно проверено, что ручной `deploy_r2.py` при активном
  `.build.lock` отказывается работать (exit 1, понятное сообщение) — обе половины пункта 4 живые.
- 2026-07-28 — переезд с GitHub Pages на Cloudflare: R2, Worker, DNS, маршруты
- 2026-07-28 — собранный сайт снят с отслеживания в git (репозиторий похудел с 11 ГБ)
- 2026-07-28 — GitHub Pages выключен
