# Отчёты · Обучение

Формат — [README.md](README.md). Новая запись сверху.

---

## 2026-08-18 · аудит обучения, раздел 2 (критичное): квизы, шесть тем, французский

**Сделал.** Три критичных пункта аудита. Ветка `edu-audit-quiz-1` от свежего main.

**2.1 — грейдер квизов.** Правило проверки числовых прикидок жило внутри `js/tutor.js` и
понимало `tolerance` только как множитель, а авторы половины тем писали туда абсолютную
погрешность. Девять вопросов не проходились никогда — даже точным авторским ответом; «5000 эВ
± 200» принимало всё от 25 до миллиона; «10 бит ± 1» — только точное совпадение.
Правило вынесено в `js/quiz-grade.js` и понимает обе семантики явно (`tolAbs`, `tolFactor`),
а также отрицательные ответы. Все 48 прикидок переведены на явную семантику поимённо
(`tools/quiz_tolerance_fix.py`): решение по каждому вопросу с обоснованием, не эвристикой.
**Доказательство:** `tools/quiz_check.js` гоняет не копию правила, а сам `quiz-grade.js` —
тот файл, что подключает страница. На данных до правки он даёт ровно девять непроходимых
(список совпал с аудитом), после — 770 проверок на пяти языках и ноль непроходимых.
Скрипт я проверил мутацией: заведомо испорченный допуск он сначала НЕ поймал, поэтому в него
добавлена проверка ширины самого допуска.
Из того же пункта закрыты: перемешивание вариантов (верный стоял вторым в 64% вопросов и ни
разу последним), пояснение отделено от вердикта, параграф засчитывается только когда отвечены
все его вопросы, прочерк-единица больше не печатается в тексте промаха.

**2.2 — шесть тем старой волны.** `example.sanity` вместо `example.check` (блок «проверка на
здравый смысл» был написан, но не рисовался), аппендикс в форме `{title,text}` против
ожидаемых `blocks[]` (раскрывался пустым на всех языках), 24 вопроса без подсказки и шесть
свободных без порога `needed`. Всё закрыто; подсказки написаны в стиле курса — вопросом
к читателю, а не ответом.

**2.3 — французский.** Был молчаливый откат на русский: французская рамка и русский текст без
предупреждения. Появился общий помощник `B42Lang.pick`: сначала язык страницы, потом
английский, потом русский, — и один раз на страницу честная плашка «этот материал ещё не
переведён». Переведены на помощник десять учебных страниц.

**Не сделал и почему.** Подсказки в en/es/ar/fr лежат русскими заготовками: сейчас пиковые
часы DeepSeek, массовый перевод по правилу идёт в дешёвое окно — это первый пункт следующего
захода. Выводы шести тем старой волны по-прежнему тоньше медианы (4–6 шагов против 7): это
догонка содержанием, а не схемой, и она в очереди вместе с математическими врезками.
Разделы 4 (врезки, 34 темы), 7 (мелкие правки) и 3 (дыры полноты) — не начинал.

**Нашёл по дороге.** Проверять грейдер в браузере из главной папки бесполезно: локальный
сервер отдаёт главное дерево, а не ветку — правки «не видно», хотя они есть. Поднимал статику
из своей папки на 8421, это же место записано в граблях у QA.

**Ветка.** `edu-audit-quiz-1`, три коммита. Проверки: `quiz_check` — 770/770,
`course_check` — 0 ошибок и 0 замечаний, синтаксис десяти страниц проверен.

---

## 2026-07-30 · круг 3, кусок 2: тема 16 «Космология» закрыта целиком

**Сделал.** Тема доведена до `ready`: три параграфа из трёх на четырёх языках, обзор,
путеводитель, картинки. Параграф «Реликтовое излучение и инфляция» со стендом `cmb` (карта
неба, спектр чёрного тела с меткой пика по Вину, остывание графиком) и параграф «Тёмная
материя и тёмная энергия» со стендом `dark` на два режима: кривые вращения галактики и
история расширения, считаемая по уравнению Фридмана. Семь новых схем к шагам вывода на
четырёх языках. Переведено всё, включая путеводитель; добор понадобился четырём блокам.

**Причина, если чинил баг.** Ползунок усиления пятен на стенде реликта сначала входил в
подписанное число, и оно показывало «±300 000 мкК» — читатель уносил бы неверный порядок
вместо честного ΔT/T ≈ 10⁻⁵. Теперь усиление меняет только видимость карты, а размах
остаётся физическим: ±27 мкК сегодня.

**QA 4а закрыт.** У космологии и теоретической механики картинок не было вовсе, каждая
загрузка урока уходила в 404 на всех языках. Нарисованы шесть недостающих — теперь картинки
есть у всех 48 параграфов курса. Третью пришлось переснять: «прибор без деталей» FLUX
нарисовал целым, и нехватка, ради которой сюжет задумывался, не читалась.

**Не сделал и почему.** Французского содержания у темы нет — по решению владельца контент на
fr идёт отдельными кругами, следующий начинается с термодинамики.

**Нашёл по дороге.** При слиянии свежего main конфликт в словаре подписей схем: в main он уже
переписан инструментом с французским, в моей ветке 42 новые подписи в прежнем формате. Собрал
объединённую версию и добрал fr. Переводчик курса научен пятому языку и получил `--only
<тема>`: весь курс на новый язык — полтора миллиона знаков, без отбора прогон становится
многочасовым и неотменяемым.

**Ветка.** `edu-cosmology-2`, восемь коммитов, main влит. Проверено в браузере: оба параграфа
на en, es и ar — ноль кириллицы, стенд с графиком, четыре вопроса, картинка живая, ни одного
«undefined»; номера верных ответов совпадают с русской ветвью. `course_check` — 0 ошибок и
0 замечаний, пробелов перевода по 118 материалам — 0.
## 2026-07-30 · круг 3, кусок 3: пилот французского контента — термодинамика

**Сделал.** Тема термодинамики целиком на fr: три параграфа, обзор, путеводитель, интерактивный
учебник `course-thermodynamics`, словари трёх стендов темы и индекс дерева знаний. Прогон —
flash, отбор `--only thermodynamics`, около 120 тысяч знаков.

**Причина, если чинил баг.** Два модуля стенда переводились не общим словарём, а собственными
таблицами языков, и пятого языка там не было: «Сначала предскажите» и «Испытание» оставались
русскими посреди французской страницы. Добавил fr в оба и поднял версии подключения. Ещё:
заголовки статей в блоке связей откатывались сразу на русский — теперь fr → en → ru, потому
что латиница читается везде, а кириллица нет; статьи переводит не курс, и на пятом языке их
названия ещё долго будут неполными.

**Не сделал и почему.** Остальные пятнадцать тем, лекции и справочники на fr — следующие круги;
порядок в задаче такой и записан. Инструменты к этому готовы: `--only` отбирает тему за круг,
`--fill` добирает то, что прогон блоками теряет.

**Нашёл по дороге.** Перевод блока целиком снова потерял часть полей — на этот раз в двух главах
учебника; добор `--fill` взял 39 строк из 39. Это уже устойчивое поведение модели на больших
блоках, а не случайность: полагаться на один проход нельзя, проверка `--check` после прогона
обязательна.

**Ветка.** `edu-fr-thermo-1`, три коммита. Проверено в браузере: три параграфа и учебник на fr —
ноль кириллицы, стенды на месте (2 и 6 холстов), ни одного «undefined»; пробелов перевода по
теме — 0, `course_check` — 0 ошибок и 0 замечаний.

---

## 2026-07-30 · круг 3, кусок 1: пятый язык в словарях интерфейса курса

**Сделал.** 315 строк на французском: 148 записей словаря интерфейса и девять правил-шаблонов
(`js/course-i18n.js`), 175 подписей внутри схем (`js/figures-i18n.js`), названия систем единиц
(`js/models.js`). Десять страниц учебного раздела теперь принимают `?lang=fr` — список языков
был зашит в каждой странице отдельно. Появился `tools/dict_lang.py`: он вынимает значения
исполнением самих файлов, переводит батчами с английским переводом как опорой и вставляет
точечно, не трогая комментарии. Шестой язык будет стоить одну команду.

**Причина, если чинил баг.** Четыре, все всплыли на пятом языке и закрыты в коде.
`tr()` возвращал `hit[IDX[LANG]]` без проверки — на языке, которого нет в записи, в текст
страницы попадало слово «undefined»; теперь возвращается null, то есть «оставить как есть».
Дерево знаний на fr падало целиком с TypeError: подписи справочного блока откатывались на
пустой объект вместо русского. В словаре был дубликат ключа «Открытые вопросы» — в JS вторая
запись перекрывает первую, и перевод, попавший в первую, до страницы не доезжал; инструмент
теперь дописывает во все вхождения и говорит о дубле вслух. Восемь реплик тьютора
(приветствие, «Думаю…», демо-режим, затравки вопроса) жили в UI_STRINGS страницы и в словарь
заведены не были — это находка QA 4б, панель называлась «Tutor», а первая строка внутри
оставалась русской. Заодно подписи шпаргалки «К параграфу», «Схема», «Печать».

**Не сделал и почему.** Содержание уроков на fr — следующий круг, так и записано в задаче.
Сейчас на французской странице интерфейс французский, а текст параграфа русский по откату:
это видно и это ожидаемо, пустых страниц нет.

**Нашёл по дороге.** Версии подключений скриптов пришлось поднять на всех десяти страницах:
пока браузер держал в кэше старый словарь, страница на fr показывала «undefined» — то есть
беда воспроизводилась только у тех, кто уже заходил на курс. Обе регрессии (падение на новом
языке и «undefined» из кэша) внесены в `задачи/qa.md` с описанием, как воспроизвести.

**Ветка.** `edu-fr-dicts-1`, четыре коммита. Проверено в браузере: на fr переключатель
показывает FR, подписи схем и единицы французские, дерево — 16 узлов и ноль undefined,
шпаргалка — «Retour au paragraphe»; приветствие тьютора на en, fr и ar — на языке страницы;
русские версии не изменились.

---

## 2026-07-30 · круг 1: тема 16 «Космология» — обзор и первый параграф

**Сделал.** Обзор темы 16 и параграф «Расширение и красное смещение» на четырёх языках.
Свой стенд `expansion`: галактики на резиновой ленте (скорость пропорциональна расстоянию),
луч в пути с растягивающейся волной, спектр с двумя метками — где линия была в лаборатории
и куда уехала; график — сама прямая Хаббла. Пять новых схем к шагам вывода с подписями на
четырёх языках. Тема в дереве знаний, ярус IV, статус «в работе»: из трёх параграфов готов
первый. Связи с базой: тег `hubble`, закон Хаббла — Леметра, три статьи из архива.

**Причина, если чинил баг.** Три штуки, все всплыли по ходу и закрыты в коде.
`course_link.py` писал привязки в корень файла урока, а страница читает `entities`: прогон
отчитывался «привязки записаны», связей на странице не появлялось. Он же писал без перевода
строки в конце, из-за чего каждый прогон помечал изменёнными все сорок с лишним уроков —
добавил `--only <тема>`. Блок «математика» рендерится по полям what/why, а три параграфа
темы 15 написаны как h/text: читатель видел пустой заголовок и слово «undefined» абзацем.
Это моя регрессия из прошлого круга; поправил данные и сделал рендер терпимым к обоим
вариантам. Обе регрессии — в `задачи/qa.md` с проверкой в одну строку.

**Не сделал и почему.** Параграфы 2 и 3 («Реликтовое излучение и инфляция», «Тёмная материя
и тёмная энергия») и путеводитель темы — следующие круги, ветками от свежего main.
Путеводитель пишется последним: в нём таблица времени по всем параграфам, раньше её не
заполнить честно. Статус темы поднимется до `ready`, когда встанут все три.

**Нашёл по дороге.** В корне сорока с лишним уроков лежит дубль привязок от прежних прогонов
(`tags`, `laws`, `examples_from_articles` на верхнем уровне). На страницу не влияет — читается
`entities`, — но мешает увидеть, где привязок нет по-настоящему. Чистка отдельным кругом:
она трогает все уроки сразу, в ветке с содержательной правкой ей не место. Пункт в своём
файле задач. Ещё: перевод потребовал слова ведущей (70 тысяч знаков, свыше двадцати вызовов
модели) — разрешение получено, прогон шёл в пиковые часы осознанно, объём — доли цента.

**Ветка.** `edu-cosmology-1`, пять коммитов, последний `d5b460d9d`. Запушена.
Проверено в браузере: параграф на ru/en/es/ar — ноль кириллицы на не-русских, стенд считает
верно (300 Мпк → 21 000 км/с, z 0,070; 900 Мпк → 63 000 км/с, z 0,210), пять схем, 54–58
формул, четыре вопроса, ноль сырых долларов и ноль «undefined»; на 375 px горизонтальной
прокрутки нет, холст вписывается, длинные формулы прокручиваются внутри блока; дерево — 16
узлов на всех языках. `course_check` — 0 ошибок и 0 замечаний, пробелов перевода по 113
материалам — 0.

---

## 2026-07-30 · два блокера выпуска закрыты: вход в тему 15 и учебник термодинамики

**Сделал.** Оба пункта из раздела «Блокеры» отчёта QA. Ветка `fix-release-blockers` от
`14a18a091`, три коммита: `58432d0bb`, `3d79e31d9`, `0b1851af5`. В main не сливал.

**Блокер 1 — тема 15 не видна с дерева.** Причин оказалось три, и первая была не главной.
Языковая ветка `index.json` действительно затирала верхний список тем — но даже после
починки тема не появилась: я сам поставил ей ярус `V`, которого не существует, а раскладка
по ярусам молча отбрасывает всё, что не попало ни в один ярус. Тема переехала в ярус IV под
номером 10, дальше сквозная нумерация 11–14. Третья причина — обзор темы падал целиком с
`TypeError`, если не хватало одного блока: сначала `map` и `prereq`, потом `why` и `history`.
Схема `b42.course/1` против `/2`, о которой писал QA, была следствием: тема не имела
обязательных блоков новой схемы.

**Что сделано против повторения.** Темы с неизвестным ярусом больше не исчезают — собираются
в отдельный ярус в конце: настройка видна, а материал читателю доступен. Разделы обзора стали
необязательными и терпимыми к форме: десяток готовых параграфов не должен прятаться из-за
отсутствующего заголовка. Сама тема приведена к общей схеме — `why` абзацами, `history`
карточками людей от Ферма до Нётер.

**Блокер 2 — учебник термодинамики на en/es/ar.** Корень один: это единственный материал, у
которого языковые ветки лежат не наверху файла, а внутри полей `ui` и `course`. Переводчик
такой файл не видел вовсе — искал `d["ru"]`, не находил и шёл дальше. Поэтому учебник остался
без перевода, а страница падала на `data.ui[LANG].title`. Русские кнопки единиц и записка
разработчика в видимом тексте — отдельные мелочи, тоже закрыты (кнопки через
`B42Units.sysName`, как в `course.html`; записка ушла в комментарий).

**Инструмент пришлось доучить, и это главное из сделанного.**
- `course_translate.py` находит языковые ветки внутри полей, а не только наверху.
- В корне `data/theory` отбор файлов был поимённым списком — материал, в него не попавший,
  оставался русским навсегда. Так `frontier.json` (край известного) читался по-русски на
  en/es/ar: те самые 15 тысяч знаков из отчёта QA. Теперь отбор по содержимому. Переведён.
- Крупные блоки режутся по составу: главы по десять тысяч знаков одним запросом не
  возвращаются, ответ обрывался на середине JSON.
- Найден последний по счёту молчаливый откат: ключ, которого модель не вернула, писался
  русским оригиналом. Из-за него уезжали целые блоки — «Как это запомнить», таблица констант.
- Перевод блока целиком **не сходится в принципе**: каждый заход теряет пару полей в новом
  месте, а из-за одной строки на повтор уходит весь блок. Появился добор `--fill`: собираем
  строки с кириллицей, переводим списком, ставим обратно по всем местам. 146 строк из 146 за
  один проход. Он же чинит кириллические индексы в формулах (`L_мол`, `N_удар`, `кДж`).
- Перевод сшивается с оригиналом по форме: числа, флаги и `id` берутся исходные. Не
  косметика — в тесте `answer` это номер верного варианта, сдвиг был бы незаметен. Проверено
  отдельно на синтетике и на данных: номера ответов совпадают с русской ветвью на трёх языках.

**Проверено.** `learn.html` на ru/en/ar — 15 узлов, ссылка ведёт. Обзор и все три параграфа
темы 15 открываются на четырёх языках. Все 15 тем обзора прогнаны — правка никого не задела.
Учебник на en/es/ar — ноль кириллицы, 6 стендов, 18 вопросов, страница не падает. `frontier`
на en/es/ar — ноль кириллицы. Русские версии не изменились. `course_check` — 0 ошибок,
пробелов перевода по 110 материалам — 0.

**Нашёл по дороге.** В оглавлении края известного заголовок «Смысл» не был заведён в словарь
интерфейса, хотя два соседних были. Добавлен; версия словаря поднята до `v=6` на всех десяти
страницах, иначе браузер отдаёт старый файл из кэша.

**Не трогал.** Блокеры 1 и 3 из отчёта QA (обложки на не-русских версиях, `lecture.html` с
неверным номером файла по умолчанию) — не мои пункты. Пиковые часы DeepSeek обойдены осознанно
(`ALLOW_PEAK=1`) на доборе: объём — доли цента, а сторож ждал результата.

---

## 2026-07-29 · новая тема «Теоретическая механика» и тихий баг в проверке

**Сделал.** Тема 15 по ТЗ на расширение: три параграфа со всеми блоками, обзор, путеводитель,
узел в дереве знаний, связи с базой, четыре языка. Свой стенд `action`: тело брошено вверх,
концы пути закреплены, действие считается численно суммой по 240 шагам. Сверил с аналитикой —
2,468 против 2,467, минимум ровно на истинном пути.

**Причина, по которой тема нужна.** Остальной курс отвечает на «как устроено» и оставляет без
ответа, откуда берутся сами законы. Здесь они выводятся из одного требования, а сохраняющиеся
величины — из симметрий. По ТЗ: без этого квантовая механика выглядит фокусом.

**Не сделал.** Четыре темы ТЗ из пяти. Но прежде чем их писать, стоит свериться с покрытием:
лекции 07 и 08 и раздел `frontier.html` появились ПОСЛЕ написания ТЗ и уже закрывают часть
космологии, тонкой настройки и почти всю тему «Границы познанного». Писать их как полные темы
означало бы дублировать. Реально недостающие: космология и информация как физика.

**Нашёл по дороге.**
- В `tools/course_check.js` был зашит абсолютный путь к одному рабочему дереву. Проверка из
  любой ветки молча читала ЧУЖИЕ файлы — «0 ошибок» относилось не к тому, что правишь. Тихий
  и потому опасный баг: отчёт всё это время выглядел зелёным. Починено, корень берётся от
  скрипта; сразу увидела 15 тем вместо 14.
- Добавил схему с именем `noether`, а такая уже была — мой дубль её перекрыл. Убрал.
- Картинки к параграфам рисовались ДЕШЁВОЙ моделью (FLUX-1-schnell). Владелец просит дорогую
  для обучающих материалов: перерисовка всех 42 на FLUX-2-pro стоит около $0,63,
  `python course_images.py --force --preset image_quality`.
- После перевода в испанской версии осталась русская подпись внутри формулы: она стояла голым
  текстом в `latex`, а не внутри `	ext{}`, и правило промпта её не покрыло.

**Проверено.** `course_check` без ошибок на 15 темах, пробелов перевода 0, утечки русского 0,
битых ссылок 0 из 191; страницы ru/en/ar открываются со стендом, формулой и 21 схемой.

**Ветка.** `course-expansion`, коммиты `f4856a7a9` и `3ef326da2`, отправлена.

---

## 2026-07-29 · связи уроков с сайтом: ссылки вели в никуда

**Сделал.** У всех 42 параграфов теги и статьи по теме, у 40 — законы. Битых ссылок 0 из 168.
Добавлено 126 ссылок на свежие статьи архива; страница их теперь показывает заголовком на языке
читателя.

**Причина.** Задача звучала как «десять уроков без привязки», но аудит показал другое: ссылки
БЫЛИ и вели в никуда. Из 67 ссылок на теги битых 45, из 38 на законы — 19. Теги уроков оказались
внутренними ярлыками курса («mechanics», «energy»), которых в словаре сайта нет вовсе; законы
стояли под другими идентификаторами, чем в базе, хотя сами страницы существуют. То есть чинится
переименованием, а не написанием контента.

**Не сделал.** Расширение курса на пять новых тем (пункт 3) — не начинал.

**Нашёл по дороге.**
- Автоподбор `course_link.py` не годится: предлагал термодинамике тег «пульсар», волнам —
  «галактику». Архив астрофизический, совпадение подстрок тут даёт мусор. Подбирал вручную;
  если запустить его как есть, он затрёт нормальные связки худшими.
- Часть тегов существует страницей, но статей под ними ноль (`electric_field`, `interference`,
  `laser`, `isotope`). Брал только те, где статьи есть.
- `course_link.py` вообще не запускался в дереве роли: справочники живут в `lang/**`, которого
  здесь нет по правилам. Добавил поиск корня через `B42_SITE_ROOT`.
- Ссылки в строке связей слипались без разделителя — это было и до меня.
- Заголовки статей пришлось складывать на четырёх языках: иначе на английской странице курса
  появились бы русские названия. Та же ловушка, что ловил всю неделю.

**Проверено.** `tools/audit_links.py` — 0 битых из 168; 126 ссылок на статьи проверены на
существование файла; утечки русского 0; `course_check` без ошибок; страницы ru/en/ar смотрел.

**Ветка.** `edu-course`, коммит `a7382012e`.

---

## 2026-07-29 · путеводители по всем темам — хвост после чистки языков закрыт

**Сделал.** Путеводители для 13 тем (был только у термодинамики) на четырёх языках, 39 блоков
перевода. Тем самым закрыт весь пункт 1 моего файла задач.

**Причина.** Путеводитель отвечает на вопросы, которые задают ДО чтения: сколько времени займёт,
что надо знать заранее, что делать, если застрял. Без него 13 тем начинались вслепую. Собирал
не на глаз: время и порядок — из настоящих параграфов, опыты — из блоков `curiosities` с пометкой
«опыт на кухне» (те же, что уже в уроках), законы и учёные — из `entities`, они ведут на живые
страницы базы. Общие разделы («как проходить», «если застряли») взяты из образца: они описывают
устройство курса, одинаковое у всех тем. Авторское — подводка, требуемая подготовка, «куда дальше».

**Не сделал.** Ничего из пункта 1 не осталось. Следующие пункты файла — связать десять уроков
с базой сайта и расширение курса на пять новых тем — не начинал.

**Нашёл по дороге.**
- Рендерер путеводителя падал на неполных данных: требовал `library.tags` и `budget.note`
  и валился с «Не удалось загрузить материал». Ровно та же болезнь, что была у карточки-шпаргалки
  (`memo.where.forEach`) — отображение роняет страницу из-за отсутствия необязательного блока.
  Сделал раздел и каждый подраздел условными. Стоит проверить остальные рендереры на это же.
- У четырёх тем (`language`, `waves`, `quantum`, `atom`) в уроках нет ни одного закона в
  `entities` — поэтому раздел «Материалы по теме» у них пуст. Это пункт 2 моего файла
  (связать уроки с базой), а не поломка сборки.
- В словаре интерфейса не хватало «Темы» — соседние «Законы» и «Учёные» были. Третий такой
  случай за день: словарь пополняют по одному, соседние пункты одного блока разъезжаются.

**Проверено.** 14 из 14 тем открываются на ru/en/es/ar без ошибок и без кириллицы,
`course_check` без ошибок, пробелов перевода 0.

**Ветка.** `edu-course`, коммит `26fc5c7f2`.

---

## 2026-07-29 · причина всех утечек найдена в переводчике; пробелы закрыты

**Сделал.** Переведены недостающие блоки: `curiosities` не было в en/es/ar у 27 уроков,
`synthesis` у 22 — иностранный читатель не видел ни опытов на кухне, ни сквозного примера темы.
31 блок, пробелов не осталось.

**Причина.** Она в самом `course_translate.py`, и это корень всего, что я чистил последние заходы.
(1) **Молчаливый откат:** при сбое запроса в перевод писался РУССКИЙ оригинал. Худший исход из
возможных — читатель на английском видит русский текст, а файл после этого навсегда считается
переведённым. (2) Пропуск считался по наличию языковой ВЕТКИ, а не по блокам внутри: один
сорвавшийся блок — и файл больше никогда не дополняли. Обе закрыты: при сбое не пишется ничего,
сравниваются ключи.

**Не сделал.** Путеводители у 13 тем — последний пункт хвоста, не начинал.

**Нашёл по дороге.**
- Сам сломал промпт: добавляя правило про `	ext{…}`, вписал фигурные скобки в строку под
  `.format()` — они съедаются как placeholder, запрос падал с `KeyError`. Из-за отката это
  выглядело как успешный прогон: первый запуск записал русский в 31 блок. Поймал сравнением
  сканера утечки до и после, данные откатил, скобки экранировал. Вывод на будущее: после
  каждого платного прогона сверять сканер, а не верить строке «✅ переведено».
- В памяти сессий роль опять сменилась («дизайнер» вместо «DevOps», обе с сегодняшней датой).
  Каталог памяти общий на все сессии, роли перетираются — риск, что следующая возьмётся не за своё.

**Проверено.** Пробелов 0, утечки русского 0 на всех языках, `course_check` без ошибок;
на страницах видны блоки Curiosities / Curiosidades / مثير للاهتمام.

**Ветка.** `edu-course`, коммит `a53f8de45`.

---

## 2026-07-29 · подписи схем на четырёх языках — кириллицы на курсе не осталось

**Сделал.** Подписи, нарисованные внутри 56 схем к шагам вывода, переведены на en/es/ar:
словарь `js/figures-i18n.js` (148 подписей), подстановка в одной функции `txt()`. Плюс три
слова, оставшихся в формулах моделей («слева», «из», «усиление»), и пропущенные подписи
примера «Решение»/«Ответ» в словаре интерфейса.

**Причина.** Подписи рисовались прямо в SVG, а комментарий в шапке `figures.js` уверял, что
внутри схем только математические обозначения и переводить нечего. Из-за этого утверждения
их годами не замечали. Комментарий переписан: теперь в нём сказано, как оно на самом деле.

**Не сделал.** Путеводители у 13 тем и пробелы `curiosities`/`synthesis` в переводах — следующие
пункты хвоста, не начинал.

**Нашёл по дороге.**
- В словаре интерфейса были «Дано» и «Закон», но не было «Решение» и «Ответ» — соседние
  подписи одного блока, половина переведена, половина нет.
- В памяти сессий появилась запись «Моя роль: DevOps» с той же датой, что и моя роль
  «обучение». Похоже, две сессии пишут в общий каталог памяти и перетирают роли друг друга —
  риск, что следующая сессия возьмётся не за своё.

**Проверено.** Все 42 урока на английском плюс выборка на арабском и испанском — ноль
кириллицы. Русская версия не изменилась. `course_check` без ошибок.

**Ветка.** `edu-course`, коммит ниже.

---

## 2026-07-29 · единицы в формулах моделей на языке страницы

**Сделал.** Формулы и строка показателей выводят единицы на языке страницы: на английской
версии «3.87 rad/s», «6.0 cm», на русской по-прежнему «рад/с», «см». В тот же заход до этого:
убран русский из переводов уроков (1791 замена), достроены испанские и арабские словари
17 стендов (их не было вовсе), переведён переключатель единиц, починена шпаргалка на телефоне.

**Причина.** Источников было три, а не один. (1) Переводчик по инструкции не трогал формулы —
и оставлял русские слова внутри `	ext{}`; закрыто в промпте, иначе вернулось бы при следующем
прогоне. (2) Словарей es/ar у моделей не было вовсе — движок показывал сырой ключ (`xTime2`),
то есть стенд на двух языках из четырёх был нечитаем. (3) Единицы и названия систем захардкожены
в `models.js`. Правил не сотню литералов, а один выход `formula()`/`extras()` — новая модель
получает это даром. Мобильные жалобы (три штуки) тоже имели одну причину: карточка падала на
несовпадении типа поля (код ждал список, в данных строка — 304 таких поля), а падение обрывало
и набор формул.

**Не сделал.** Путеводители у 13 тем и пробелы `curiosities`/`synthesis` в переводах — не
начинал, это следующие пункты того же хвоста.

**Нашёл по дороге.**
- `js/figures.js` — 178 русских литералов: подписи к 56 схемам шагов вывода читаются по-русски
  на всех языках. Завёл задачей 1а у себя.
- В `formula()` моделей остались русские СЛОВА (не единицы): «слева», «усиление», «тележка
  стоит: трение сильнее», подписи векторов «Fцб», «тр». Туда же, в 1а.
- Ошибка перевода, найдена при слиянии: «пот» (потери мощности) переведено как «تدفق» (поток).
  Исправлено на «فقد».
- Ключевые слова арабского теста в `relativity/01` были русскими стеблями — проверка свободного
  ответа не работала вовсе. Исправлено.
- `pageLang()` в `models.js` обращался к `document` вместо `global.document`: в браузере
  работало, вне его падало.
- Предохранитель пиковых часов DeepSeek обойдён осознанно (`ALLOW_PEAK=1`): объём — доли цента,
  дешёвое окно было через два часа.

**Ветка.** `edu-course`, последний коммит `088637f93`. Отправлена. MR открыть не смог — `gh`
в системе нет: https://github.com/igogogo/bridge42worlds/pull/new/edu-course

