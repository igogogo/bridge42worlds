# Отчёты · QA

Формат — [README.md](README.md). Новая запись сверху.

---

## 2026-08-05 · приёмка большой выкатки: 3 маршрута из 7, замечания владельца разобраны

Проверял **на проде**. Блокеров не нашёл. Три маршрута прошёл, три не завершил (пометил
честно), один снял как не относящийся к делу. Отдельно разобрал пять замечаний владельца —
два подтвердились, два оказались не тем, чем выглядели, одно не воспроизвелось.

### Замечания владельца

**1. Кнопка «i» — не воспроизвелось.** На проде это аккуратная карточка: на 1280 — 340×277,
box [199, 61, 539, 338]; на 375 — 300×299, box [16, 66, 316, 366]. Текст переносится
(`white-space: normal`), за экран не выходит, страница вбок не едет, внутри ссылка
«О проекте → /lang/ru/about.html». Уезжающей вправо строки не увидел ни на одной ширине.
**Не уверен** — возможно, у тебя осталась старая версия в кэше; перепроверь с Ctrl+F5.
Если повторится — пришли ширину окна, посмотрю прицельно.

**2. Поиск: закрыть можно, но неочевидно.** Панель закрывается **тремя** способами — Escape,
клик мимо панели, повторный тап по кнопке поиска (проверил каждый). Видимой кнопки закрытия
нет, а единственный знак внутри — ✕ с `onclick="clearSearch()"` и подсказкой «Clear»,
то есть **очистка поля**, не закрытие: после него панель остаётся открытой, поле пустеет.
Жалоба на понятность справедлива: на телефоне «клик мимо» и Escape не подсказаны ничем.
Заодно подсказка кнопки английская («Clear») на русской странице. **Не блокер.**

**3. Панель поиска уезжает за левый край.** На 375 (ширина подтверждена): панель
box [-52, 64, 248, 151] — **52 px вне экрана**, поле ввода box [-37 … 189] — **37 px
вне экрана**, то есть начало набранного не видно. Страница вбок не прокручивается (0),
значит это не «уехало», а **отрезано**. Якорь `.search-bar` [224, 248], панель 300 px,
`right: 0` → left = 248 − 300 = −52, арифметика сходится. Это моя находка от 29 июля,
она не закрыта. **Не блокер, но чинить.**

**4. Раздел на карточке — ссылка есть, но выглядит мёртвой.** Это
`<a class="card-cat" href="#" onclick="filterByCategory('astro-ph.CO'); return false;">`,
то есть фильтр по разделу заложен и работает. «Только тултип» — не совсем так. Но `href="#"`
и отсутствие видимого перехода делают действие неочевидным: читатель не понимает, что лента
под ним отфильтровалась. Предложение: вести на `/lang/<яз>/sections/<код>.html` (страница
раздела существует) либо явно показывать состояние «показан раздел X, сбросить». **Не блокер.**

**5. Строка сводки действительно обрезана.** На 375: содержимое 554 px при видимых 343 —
**211 px спрятано**, `white-space: nowrap`, `overflow: auto hidden`. Пальцем прокрутить можно,
но выглядит как обрыв. Текущий текст: «2162 статей · 145 законов · 363 тегов · 156 разделов ·
201 учёных · 18k авторов · 5 языков». Даты сборки в нём сейчас нет вовсе — та же поломка,
что я описывал 31-го: `renderSiteStats()` зовётся дважды и второй вызов затирает span с датой.
**Не блокер.**

### Про «Популярно» — претензия по сути верна

Обрыва текста нет ни в данных, ни в вёрстке: из **2162 текстов × 3 уровня ноль** обрывается
без знака препинания в конце; на проверенной карточке `scrollHeight == clientHeight`,
line-clamp отсутствует. Твой пример `2607.27587v1` во всех трёх уровнях кончается точкой.
Обрыв на «становят» воспроизвести не смог — **не уверен**, похоже на кэш.

А вот регистр — да, не тот. На той же статье:

- **Просто:** «Обычный мяч при сжатии деформируется, и его податливость можно описать числом…» —
  образ первым, термин в скобках, вывод понятный.
- **Популярно:** «Приливные числа Лява показывают, насколько сильно небесное тело
  деформируется…» — **начинается с термина**, дальше «многосоставных бозонных звёзд»,
  «электрическое приливное число», «меньше по абсолютной величине». Ни одного образа.

То есть «Популярно» здесь ближе к «Подробно», чем к «Просто». Грубая прикидка по всей ленте:
**78 текстов из 2162 открываются образом**, **257 — термином или методом**. Примеры начал
популярного уровня: «Анализ операторного роста в конформной теории поля с симметрией W3…»,
«Вихревой поток воздуха с частицами в трубе моделировался с помощью осреднённых уравнений
Навье-Стокса…». Оговорка: оставшиеся 1827 моя проверка не классифицировала, поэтому **точную
долю назвать не могу** — но 257 явно научных начал достаточно, чтобы сказать: требование
маршрута 2 «начинаются образом» массово не выполняется. Это к промпту популярного уровня.

### Маршруты

**1. Французский — сходится.** 10 статей вразнобой по индексу: заглушек 0, заголовки
французские, маркеров тегов 6–28 на статью. **Видимой кириллицы 0 на всех десяти.**
Уточнение: в `/lang/fr/articles-index.json` **2114** записей, а заявлено 2124 — расхождение 10.
Ключи «Ключевых чисел» моими селекторами не нашлись — **не проверено**.

**2. Карточки с аналогиями — частично.** Тексты целиком, не обрываются (см. выше). Требование
«начинаются образом» не выполняется. **Не блокер.**

**3. Облако формул — не завершено.** `/lang/{ru,en,es,ar,fr}/formulas/` отдают 200,
заголовки переведены («Формулы», «Formulas», «Fórmulas», «الصيغ», «Formules»), по 1642 элемента.
KaTeX, мосты сверху и мобильную живьём **не домерил** — панель браузера подвисла на длинной
странице. **Не проверено.**

**4. Панель совета — не завершено.** `/council.html` отдаёт 200, но это оболочка в 509 байт:
страница собирается скриптом, а мой замер без исполнения JS показывает 7 знаков текста —
цифра недостоверна. Переключатель языков, «У меня есть ключ» и арабский RTL **не проверены.**

**5. Народная наука — сходится.** `/lang/ru/community/` → 200, титул «Для авторов», 7,5 КБ.
Почту в ней не домерил — **не проверено**.

**6. Свежие статьи 08-03 — не проверено.**

**7. Регресс главной — частично.** Карточки, строка сводки и поиск проверены (см. выше).
Меню «ещё» на 10 пунктов, календарь и три формы тегов-учёных-законов — **не проверено.**

### Ложные тревоги, которые снял сам

Пишу их отдельно, потому что каждая выглядела как находка.

- **«Кириллица на французских статьях»** — насчитал по 338 знаков на каждой из 10, одинаково.
  Одинаковое число выдало общий блок: все 338 сидят в `<script>` (служебный словарь).
  В видимом тексте **ноль**. Считал `body.textContent`, куда попадает содержимое скриптов.
- **«/community/ и /formulas/ отдают 404»** — короткие пути без языка действительно 404,
  но настоящие адреса языковые, и меню строит именно их: `'/lang/' + lang + '/formulas/'`
  (`js/search.js`). Ссылок на короткие пути на сайте нет. Не находка.
- **«Панель поиска нечем закрыть»** — закрывается тремя способами, см. пункт 2.
- **«Панель поиска шириной 0 и высотой 1406»** — первый замер шёл при `innerWidth = 0`
  (панель браузера не была разложена). Все размеры в том замере — мусор; переснял.

**Ветка.** `qa-wave2` (правок кода нет, только отчёт).

---

## 2026-07-31 · ВОЛНА 2, маршрут по ссылкам: НЕ ПРИНЯТ. Четыре БЛОКЕРА

**Вердикт: БЛОКЕР ×4.** Маршрут пройден целиком (пункты 1–7), плюс регрессия дашборда
и приёмка сегодняшнего. Регрессия «пустой дашборд» закрыта чисто, араб и og-карточки —
тоже. Но заявка «французский собран целиком» не соответствует тому, что на диске,
а смысловой поиск и кнопка перевода тратят норму впустую.

Проверял на локальной статике свежей сборки, headless Edge через CDP, в каждом замере
печатался реальный `innerWidth` и сверялся `location.href`. Из 17 находок 15 подтверждены
отдельным проверяющим на опровержение, 2 отсеяны — они ниже, в разделе «не находки».

### БЛОКЕР 1. «Французский собран целиком, 2111 статей» — на самом деле 133

На диске 2110 французских страниц статей, но **1977 из них — заглушки** «Cet article
n’est pas encore traduit dans cette langue», а настоящих французских текстов **133**.
Контроль тем же скриптом: es — 2110 страниц, 0 заглушек; ru — 0; ar — 1.

Важно: **сами заглушки честные**, они прямо говорят читателю, что перевода нет, и индекс
на 133 записи им соответствует (пересечение index∩stub = 0). Дашборд тоже не врёт —
показывает «133 articles» и полосу FR 6%. Врёт **заявка о готовности**, и на ней уже
построены решения.

Лента на `/lang/fr/index.html` доезжает до 133 карточек и встаёт — это не «одна статья»,
как было ночью, но и не 1862.

### БЛОКЕР 2. Счётчик на странице тега обещает 509 статей, показывает 23

`/lang/fr/tags/dark_matter.html`: в шапке «Tag / matière noire / **509**», в списке после
прокрутки до упора — **23 карточки**. Счётчик берётся из общего справочника тега, лента —
из усечённого французского индекса, и правило разъехалось.

На русском та же пара врёт мягче, но врёт: **509 против 372 карточек**. То есть это не
французская частность, а общий дефект счётчика.

Следствие того же корня: в курсе на fr блок «Articles sur ce sujet» отдаёт **английские**
заголовки, тогда как на ru/en/ar — на языке страницы.

### БЛОКЕР 3. Смысловой поиск шлёт запрос на КАЖДУЮ нажатую букву

Набор «чёрная дыра» посимвольно с шагом 140 мс: **11 символов → 11 запросов** к `/api/search`
(`?q=ч`, `?q=чё`, `?q=чёр`, …). Первый уходит с одной буквы.

Анонимная норма — 3 запроса в сутки. Она сгорает **на третьей букве**, до того как читатель
дописал слово. Причина: слушатель `input` в `js/search-ui.js:213` зовёт `run()` на каждый
символ, и каждый вызов уходит в `fetch('/api/search')` (`js/site-search.js:211`) — ни задержки,
ни отмены предыдущего.

Там же второе следствие: **карточки ожидания копятся стопкой** — 11 штук одновременно,
высота блока выдачи 2772 px при окне 900. Каждый вызов заводит свой узел, снимает его только
своя развязка.

И третье, помельче: у `fetch` нет ни таймаута, ни `AbortController`, а отрисовка ждёт
`Promise.all` вместе с локальными понятиями. Если Worker зависнет, а не ответит ошибкой,
читатель не увидит даже готовую локальную часть выдачи.

### БЛОКЕР 4. Кнопка «перевести» на французской статье не может сработать никогда

`js/order-translate.js:110` шлёт `POST /api/order` **без поля `turnstile`**, а
`cloudflare/worker.js:472` в `handleOrder` обязательно зовёт `turnstileOk(env, body.turnstile, ip)`,
который возвращает `false` и при отсутствии токена. Ответ — `403 captcha_failed`.

Клиент такого кода не знает: он ловит `quota_exceeded`, `limit`, `no_token`, `unauthorized`,
а воркер шлёт `day_limit`, `week_limit`, `project_limit`, `captcha_failed`, `too_fast` —
**пересечение пустое**. Читатель увидит «La file est indisponible». Перевод не заказывается
никогда, при этом снаружи всё выглядит рабочим — наши грабли 2 в чистом виде.

Заодно клиент не шлёт `body.lang`, и воркер запишет заказу `lang="ru"` по умолчанию.

---

### Что сходится

- **Пункт 1** (мой): тяжёлые файлы стартуют на 647–663 мс позже ленты, 404 — ноль.
- **Пункт 2**: подсказки на телефоне переворачиваются вверх и помещаются целиком у всех
  четырёх краёв, включая арабскую зеркальную раскладку.
- **Пункт 3б**: тап по левому краю чипа **сменяет** набор, а не дополняет.
- **Пункт 5**: арабский `about` с разделом про репетитора и звания — язык чистый; блок связей
  в курсе арабский («درجة الحرارة», не «temperature»).
- **Пункт 7**: `og:title`/`description`/`image` на месте; **у статьи без обложки `og:image`
  отсутствует вовсе**, а не битый; у статьи с обложкой файл отдаёт 200.
- **Регрессия «пустой дашборд» закрыта**: верхний счётчик статей, тепловая карта и счётчик
  авторов не нули — на ru, en и fr, на 1280 и 375. Консоль чистая.
- Новые блоки дашборда наполнены, подписи французские, испанские и арабские.

### Находки помельче

- **Аналитика на fr целиком английская**: вкладки, подсказки и все 24 названия кластеров —
  «Project map», «Black Hole Mysteries». На ru та же страница полностью русская. Молчаливый
  откат на en, ошибок в консоли нет.
- **Утечка языка в карточку ожидания**: на английской странице русское «44.1 ×10¹³ **Гс**»
  и русский факт целиком; на русской — английский. `facts_build.py:92-108` копирует
  `fun_fact`/`key_numbers` из данных без проверки языка — известный слой утечки доехал
  до нового экрана.
- **«0 баллы», «1 баллы», «14 баллы»** — `js/waiting.js:87` клеит число и слово без склонения.
  У остальных языков форма нейтральна.
- **Строка статистики на главных потеряла дату сборки**: `renderSiteStats()` зовётся дважды,
  второй вызов перезаписывает `innerHTML` и стирает уже добавленный span с датой.
- **«Языковое покрытие» против KPI на той же странице**: FR 132 против 133, RU полных 601
  против 602 — файл покрытия отстал на одну сборку. Одно число из двух источников.
- **Плашка молодого языка исчезла** — не потому что починили число, а потому что 133 ≥ порога 20
  и блок удаляется. Числа она сейчас не показывает никакого.
- **Дашборд тянет 16,6 МБ**: индекс 6,3 МБ + `authors-graph.json` 10,3 МБ — ради одного
  числа «17 672 автора». Те же два файла незадолго до этого убрали с других страниц ради телефона.
- **Две обложки тяжелее 5 МБ** (6,96 и 7,55 МБ) — выше порога, при котором X/Twitter собирает
  карточку без картинки.
- **Пустой `og:description`** у 2607.27622v1 на всех пяти языках (из 10 550 страниц — только он).
- **KaTeX сыплет предупреждениями** на арабские единицы внутри формул — визуально формула
  цела, но движок работает без метрик шрифта.
- **fr-справочники чуть беднее**: 361 тег и 155 разделов против 363 и 156 у остальных.
- **Зона касания значка «i» — 25×45 px**: по высоте порог 44 выдержан, по ширине нет.
  Это осознанный компромисс после правки наезда на логотип, фиксирую как наблюдение.
- **Последние 2 пикселя логотипа** (x=122–123) открывают поповер вместо перехода. Перекрытия
  слоёв нет, это остаток подгонки под палец.
- **Слой «+» чипа из соседнего ряда**: тап в середину и низ визуально второго чипа добавляет
  ЧУЖОЙ раздел вместо переключения. Ту же ошибку чинили по горизонтали — она переехала
  на вертикаль: ширину слоя сузили с 44 до 24 px, а высоту оставили 44 при высоте ряда 21,8.

### Что оказалось НЕ находкой

- **Расхождение «обработали 2 095» против KPI «2 110»** — числа процитированы верно,
  но причина названа неверно: это разные **окна** подсчёта, а не отставание `corpus-stats.json`.
- **«Заглушки отдаются как готовые»** — формулировка снята: заглушки честные, прямо сообщают
  об отсутствии перевода, и индекс им соответствует. Числа при этом подтвердились полностью
  и вошли в блокер 1.

**Ветка.** `qa-wave2`.

---

## 2026-07-31 · круг 3, ФАЗА Б: НЕ ПРИНЯТА. Три БЛОКЕРА, публиковать нельзя

**Вердикт: БЛОКЕР ×3.** Французский раздел собран наполовину, а ре-перевод дней 17–24
до сборки не доехал. Хорошее тоже есть, и его много — но выпускать в таком виде значит
показать французу сайт с дырами, а англичанину, испанцу и арабу — русские заголовки.

### БЛОКЕР 1. Ре-перевод дней 17–24 не доехал: заголовки остались русскими

Именно то, что задание просило проверить. Прошёл заголовки `article-title` на построенных
страницах: **из 63 проверенных 54 русские** — на английских, испанских и арабских версиях.
Примеры с английских страниц: «Звёздные повара ранней Вселенной: азотный избыток»
(2026-07-17/2607.15515v1), «Чёрная дыра-антенна: электромагнитное эхо испарения»
(2026-07-18/2607.16880v1), «Космический колокол: как затухают пульсации от Солнца до звёзд»
(2026-07-19/2607.17359v1). Затронуты все дни с 17 по 24, все три языка.

То есть ре-перевод либо не отработал по этим дням, либо отработал уже после того, как
сборка прочитала данные. Второе вероятнее: ре-перевод шёл фоном, сборка стартовала в 22:52.

### БЛОКЕР 2. У француза не собрались три раздела, а плашка ведёт в один из них

| Раздел | ru | fr |
|---|---|---|
| `laws/` (законы) | есть | **НЕТ** |
| `graph/` (граф знаний) | есть | **НЕТ** |
| `analytics/` (аналитика) | есть | **НЕТ** |
| tags, scientists, sections, favorites, about, index | есть | есть |

`/lang/fr/laws/` отдаёт «Error response» — страницы просто нет. При этом **данные готовы**:
`lang/fr/data/laws.json` собран. Значит это сбой сборки, а не пробел в переводе.

Хуже того: **плашка молодого языка, которую добавили по моей находке, ведёт читателя
в несобранный раздел.** Её текст (`js/search.js:1010`) — «…<a href='/lang/fr/laws/'>145 lois</a>…»,
то есть первое, что видит француз на главной, приглашает его на 404.

### БЛОКЕР 3. На французских страницах нет индексов «свежего» — 404 на каждой

`lang/fr/` содержит `articles-index*.json`, но **не содержит ни одного `articles-latest*.json`**
(на ru все три: latest, latest-simple, latest-advanced). Отсюда `404 /lang/fr/articles-latest.json`
на каждой французской странице, которую я открыл.

Видимое следствие: плашка молодого языка сообщает «**1** articles traduits» при 1862 собранных
французских статьях. Число берётся из того самого индекса, которого нет.

---

### Что при этом сделано хорошо — и мои прежние находки закрыты

- **Плашка молодого языка рисуется и по-французски** (моя находка 3 фазы А закрыта):
  «La section française vient d'ouvrir…», видимость подтверждена вычисленным стилем.
- **Пять языков и подсветка текущего** (находка 4 закрыта): на французских страницах
  строка `RU EN ES AR FR` и подсвечен FR.
- **Тема 16 доведена**: `status: ready`, все три параграфа открываются — «Расширение
  и красное смещение», «Реликтовое излучение и инфляция», «Тёмная материя и тёмная энергия»,
  у каждого картинка на месте (1/1), у обзора 3/3. Моя находка 4а (пустая `img/`) закрыта.
- **Термодинамика на fr — пилот удался**: 36 357 знаков, заголовок «Thermodynamique : des
  balles dansantes au moteur en marche», кириллицы **ноль**.
- `learn.html?lang=fr` — «Arbre de la connaissance», обвязка французская.
- `about` на fr — «bridge42worlds en cinq minutes», французский.
- Французских статей собрано 1862 против 2084 на остальных языках — это ожидаемо, прогон
  24–29 стартовал до включения fr, решение владельца «по запросу» это покрывает.

### Находки помельче

- **`arxiv-categories-fr.json` не создан** — для ar, es, ru есть, для fr нет. Каждая
  французская страница ловит 404 на справочник разделов arXiv и уходит в запасной путь.
- **Подсветка текущего языка не работает на `about`** — ни на французской, ни на русской
  версии активный язык не выделен, хотя на остальных страницах выделяется.
- **Облако тегов на fr озаглавлено по-английски** — «Tags», и перед словом висит
  осиротевший вариационный селектор (U+FE0F) от убранного эмодзи.
- **`js/waiting.js` нет в git** — ни в `origin/main`, ни в моей ветке, но собранные страницы
  просят его с версией. На машине ведущей файл есть, поэтому вживую работает; в репозитории
  его нет, и любой новый worktree или чистый клон соберёт сайт со сломанной ссылкой.
  Это ровно наша болезнь «единственный экземпляр на одной машине».

### Что перепроверил, чтобы не завести лишнего

404 на `js/waiting.js` сначала выглядел дефектом сборки — оказалось, файла нет именно
в git, а на машине он есть. Оставил как находку про git, а не как поломку страниц.

**Не сделал.** Регрессии сверх выборочных: время ушло на разбор блокеров. Тёмная тема,
обложки и mini закрыты в прошлых кругах, отдельно не перепроверял.

**Ветка.** `qa-krug4-2`.

---

## 2026-07-30 ночь · круг 3, ФАЗА А: прод принят, французская обвязка на месте. Две находки в исходниках

**Вердикт фазы А: принята.** Прод живой и ведёт себя честно; французская обвязка корневых
страниц на месте. Две находки — в исходниках, пересборка их не закроет. Фаза Б ждёт строки
«сборка готова» на доске.

**Главное: вход и тьютор наконец проверены живьём.** В круге 2 я не смог до них добраться —
Cloudflare 1010 резал клиент. Сегодня браузер наружу пустили, и всё, что висело
«не подтверждено», проверено путём читателя:

| Что | Результат |
|---|---|
| Норма отдаётся ДО вопроса | `dayLeft 3 / dayLimit 3 / dayUsed 0`, `signedIn false`, проект 1986 из 2000 |
| Тьютор отвечает, норма 3→2 | ответ пришёл, в теле `leftToday: 2` |
| 4-й вызов — честный отказ | `429 {"error":"day_limit","dayLeft":0,"weekLeft":2}` |
| Отклонённое не списывается | после трёх отказов `dayUsed` остался 3, `projectUsed` 18 |
| Поиск по смыслу | арабский «الثقوب السوداء المتبخرة» → релевантная статья, score 0.61, заголовок переведён на арабский |
| Кэш переводов | повтор того же запроса **5297 мс → 83 мс**, и норму повтор не списал |
| Заказ анонимом | `429 day_limit`, в очередь не встаёт |
| Заказ в UI | не торчит: два совпадения слова «order» оказались строкой «in any order» и заголовком статьи «Hidden Order» |

**Находка 1 (прод, закроется публикацией). Тьютора читатель не видит.** Бэкенд выкачен
и работает, а живая `course.html` — предыдущей сборки: грузит `tutor.js?v=3`, `models.js?v=31`,
`figures.js?v=10`, `course-i18n.js?v=6`, тогда как в `main` уже v=4 / 32 / 11 / 7. Старая версия
модуля была скрыта по умолчанию, поэтому на странице нет ни одного элемента `tb-*`. Витрина
вечера снаружи не видна — закроется, как только страница уедет с пересборкой.

**Находка 2 (продуктовая). Тьютор и поиск делят одну норму 3 в сутки.** Два вопроса тьютору —
и на смысловой поиск у читателя остаётся один запрос на весь день. Недельная норма 5 при
суточной 3, то есть на второй день доступно только 2. Не баг, но обещание «поиск по смыслу
на четырёх языках» читатель за день не проверит — я сам не смог: норма кончилась на третьем
запросе, проверил ru и ar, es и fr не успел. Новую сессию заводить не стал — это ровно та дыра
с неподписанной cookie, которую я же завёл в круге 2.

**Французская обвязка — на месте:** `config.json` отдаёт пять языков; строка языков на корневых
показывает `RU EN ES AR FR`; `lang/fr/data/about.json` собран, 93 ключа — столько же, сколько
у ru и en; словарь подписей схем `figures-i18n.js` содержит 175 французских ключей;
`document.documentElement.lang` = `fr` на всех десяти страницах, консоль чистая везде.
Русский текст на них — контент, и это заявленная норма выпуска; обвязка вокруг него
действительно французская, видно по смешанным строкам: «repose sur : Как физика описывает мир»
на `learn.html`, «Термодинамика · paragraphe 1» на `memo.html`.

**Находка 3 (дизайнеру). Плашка молодого языка не рисуется.** `.young-lang` объявлен
в `css/style.css:2308`, но по всему дереву исходников — `*.py`, `*.js`, `*.html`, `*.json` —
ни одного упоминания класса, кроме самого CSS. Разметку не выдаёт никто, на `?lang=fr`
её в DOM нет. Стиль выглядит готовым, поэтому по коду задача кажется закрытой.

**Находка 4 (ведущей). Карточка языков в about знает четыре языка.**
`templates/about.html:185` — четыре ссылки RU/EN/ES/AR, пятой нет; правило подсветки текущего
языка (строки 51–54) перечисляет те же четыре, так что на французской странице не подсветится
ничего. Это **исходник**, пересборка не поможет: `lang/fr/about.html` соберётся с переключателем,
в котором нет самого французского.

**Не сделал.** Фаза Б целиком — ждёт сборки. Поиск на es и fr — упёрся в норму. Регрессии
взял выборочно: тёмная тема и обложки закрыты в прошлых кругах, mini у свежих уйдёт в фазу Б
вместе с ре-переведёнными днями.

**Нашёл по дороге.**
- Карта сайта в about по-прежнему без аналитики и архива — те же 12 ключей `map_*`, теперь
  и на пятом языке. В задачах ведущей это уже стоит (круг 3-4), повторно не завожу. Но по её же
  правилу «выпуск с новой функцией не проходит QA, если about её не отражает» это основание
  придержать выпуск: about не описывает ни аналитику, ни архив, ни порядок ленты, ни уровни
  на карточках, ни тему 16, ни поиск.
- Меню сайта подписано английскими ключами (`main`, `tags`, `laws`…) на всех языках, включая fr.
- Ложная тревога, которую снял сам: в `course.html:321` мне сначала показалось, что на проде
  тьютор зовёт `127.0.0.1:8787`. Соседняя строка 320 — проверка `location.hostname === 'localhost'`,
  всё закрыто. Читать надо было на строку выше, прежде чем пугаться.

**Ветка.** `qa-krug4-1`.

---

## 2026-07-30 вечер · круг 2: принят частично. Вход живьём подтвердить не смог

**Вердикт: шесть пунктов из восьми приняты, один не проверен, один — устаревшая формулировка.**
Возвраты мелкие, круг ничто не блокирует. Отдельно: моя находка круга 1 по картинкам закрыта —
у `2607.23119v2` теперь 34 jpg и 34 webp, изображения грузятся на ru/en/ar.

| Пункт | Итог |
|---|---|
| Вход живьём (норма, Turnstile, отказ заказа) | **не подтверждён** · все шесть запросов получили Cloudflare 1010; три дыры по коду → DevOps |
| Google/почта отвечают `*_not_configured` | **по коду да** (worker.js:234 и :291), живьём — тот же блок |
| Поиск круг 2 | **принято** · формат, приставки, 8а живьём на ru/ar/es; две дыры релевантности → разработчик |
| Диалог бота, 375 | **принято** · панель помещается, переполнения нет; кнопка закрытия 12×20 → дизайнер |
| Контраст вторичного текста | **принято** · невидимого нет ни на одной из десяти страниц |
| Glossary переводится | **формулировка устарела** · принятое решение круга обратное, см. ниже |
| Кодировка ДОСКА.md | **принято** · utf-8, ноль символов-замен, 23 строки таблицы, история цела |
| Выгрузка эмбеддингов | **прочитано** · 2050 заявлено, в архиве уже 2066 |

**Причина, по которой вход не закрыт.** Шесть запросов к `bridge42worlds.academy` из питон-клиента
вернули **Cloudflare 1010 «Access denied»** — край режет клиент по признакам браузера, до Worker
запрос не доходит. Браузер в моей песочнице наружу не пускают. Подделывать признаки браузера,
чтобы обойти бот-защиту, я не стал. По нашему правилу «смёржено — не проверка» это значит
«не подтверждено», а не «принято»: пусть DevOps приложит вывод четырёх запросов или даст мне
способ попасть внутрь периметра.

**Что при этом видно по коду — три дыры, и все про деньги** (подробно в `задачи/devops.md`, п.8):
`turnstileOk` возвращает `true`, если секрет не задан (worker.js:203) — тихий откат в защите;
Turnstile проверяется только в `handleCodeRequest`, то есть на единственном пути, который сейчас
отвечает `mail_not_configured`, а платные `/api/order` и `/api/tutor` его не проверяют; cookie
`b42s` принимается по одному формату без подписи, поэтому новый случайный номер даёт новую
анонимную норму. Предохранитель `dayProject` 2000/сутки на месте — поэтому важно, но не блокер.

**Про glossary.** Задание просит проверить, что он переводится. Принятое в этом же круге решение
промпт-инженера — обратное: `metaphor` и `glossary` внесены в `_INTERNAL_FIELDS` (gen_llm.py:630)
и намеренно исключены и из перевода, и из валидатора, `translate_glossary` убран везде. Проверил
по коду — так и есть. Заодно: `api/*-en.json`, которые названы в задании как место проверки,
не существуют вовсе — на диске только `-ru` (3340 файлов) и `-ru_r` (2182). Строку задания стоит
переписать под принятое решение, иначе следующий проверяющий пойдёт искать то, чего нет.

**Не сделал.** Ничего не пропустил, но два пункта закрыты чтением кода, а не глазами — вход
и `*_not_configured`. Отмечаю честно, чтобы это не читалось как приёмка.

**Нашёл по дороге.**
- Поиск не знает аббревиатур: `CMB` на английском даёт ноль при существующем теге
  `cosmic microwave background`; `white dwarfs` не находит тег `white dwarf` (множественное
  против единственного). Обе дыры закроет вектор, но отрисовка — уже круг 3.
- В поле `a` индекса поиска попал мусор: 10% псевдонимов на en и 15% на es — не имена
  («η Carinae» у Цвикки, «Kepler» у Хаббла). Проверил выдачу: **не всплывает**, Кеплер на
  «Kepler» настоящий. Не находка, но пусть знают до смены весов.
- Выгрузка эмбеддингов отстала на 16 статей: заявлено 2050, в архиве 2066 — архив рос
  во время прогона.
- Кнопка закрытия диалога бота 12×20 при пороге 44.

**Ошибка в моём измерителе, для памяти.** Первый прогон контраста показал «22 невидимых блока»
на `hypotheses.html` и 10 на `course-thermodynamics.html`. Это была ошибка не сайта, а моя:
функция сравнивала текст с **полупрозрачным** фоном плашки `rgba(46,125,50,0.13)`, не накладывая
её на светлую карточку. После наложения альфы на предков — ноль невидимых на всех десяти
страницах. Чуть не вернул дизайнеру его же аккуратную работу; проверять измеритель до находки.

**Ветка.** `qa-krug3-1`.

---

## 2026-07-30 · круг 1 спринта 2: принят с возвратами. Один возврат тяжёлый

**Вердикт: круг 1 принят**, семь пунктов задания пройдены. Ничто не блокирует круг целиком,
но один возврат стоит закрыть до следующей публикации — статья Конвейера 2.0 выложена
без единой картинки.

**Сделал.** Прошёл всё задание из `задачи/qa.md`, по порядку, на своей папке (`b42-qa`,
сервер 8440, сборка через junction на `lang/`), измерения — headless Edge по CDP с проверкой
реального `innerWidth` в каждом замере.

| Пункт | Итог |
|---|---|
| Тема 16 «Космология», 4 языка | **принято** · видна в дереве (16 узлов), схема `b42.course/2` — ловушка темы 15 не повторилась; обзор и параграф открываются, стенд `expansion` рисует 2 холста на всех языках, формул 54–58, кириллицы в тексте и подписях SVG на en/es/ar — ноль. Два хвоста → обучение |
| Единый поиск, круг 1 | **принято** · размеры индекса ru 367 · ar 332 · es 287 · en 261 КБ ровно как заявлено, потолок 400 не пробит; типы tag 363 · law 145 · sci 201 · sec 156 · art 1973–2043; разделов действительно 156, а не 64; сборка стоит до публикации, проверки её пропускают. Три вещи → разработчик |
| Тёмная тема, регрессия | **чисто** · 12 замеров: frontier/learn/lecture × системная тёмная и светлая × 375 и 1280, нечитаемых блоков ноль |
| Конвейер 2.0, статья 2607.23119v2 | **возврат** · ни одной картинки ни на одном языке и уровне → ведущая |
| Мокап диалога бота | **принято** · открывается, ноль ошибок консоли, все обещанные переключатели на месте |
| Схема очереди DevOps | **прочитана** · решение «одна таблица на троих» здравое, пять дыр → DevOps |
| Glossary / промпты | **принято** · `glossary` нет ни в `templates/`, ни в `js/`, ни в собранных страницах — заявление подтверждается |

**Причина** (по самой тяжёлой находке). У статьи `2607.23119v2` страница просит `ai.webp`,
`t_ai.webp`, `0.webp`…`17.webp` — 20 запросов, все 404, а на диске 34 файла и все `.jpg`.
Читатель видит статью вообще без иллюстраций, включая обложку, на всех четырёх языках и всех
четырёх уровнях. Это не общая поломка: за последние 12 дней в архиве 215 статей с картинками,
у 214 число `webp` равно числу `jpg`, и ровно у одной — этой — 34 и 0. Значит шаг конвертации
в новом конвейере не отработал, а рендер про это не знает и всё равно просит `webp`. Наша давняя
ловушка «webp против jpg» в новом месте.

**Не сделал.** Ничего из задания не пропустил. Сознательно не заводил известное: арабский хвост
`key_numbers` на 2607.23119v2 (промпт-инженер починил валидатор, перегенерация — платная,
за ведущей), русские заголовки статей с плашкой «ещё не переведена» (проверил 2607.23140v1 —
плашка на месте, случай штатный), 11 битых ссылок, блёклый вторичный текст. Сырые маркеры
`[tag:…]` на новой статье повторно не заводил — они уже в `задачи/разработчик.md`, пункт 5;
отмечаю только, что на статье нового конвейера они по-прежнему воспроизводятся.

**Нашёл по дороге.**
- Учёные в индексе поиска только латиницей: 0 из 201 с именем на языке страницы на ar и ru,
  при том что теги в том же арабском индексе переведены 362 из 363. Поиск по имени учёного
  на своём языке не сработает — сказал разработчику до того, как он напишет браузерную часть.
- Список команд-проверок в `run.py` теперь живёт в трёх местах с разным поведением: `READONLY`
  в публикации, такой же литерал внутри сборки индекса поиска и **отсутствует** в бэкапе —
  то есть `run.py check` и `links` по-прежнему льют исходники в облако.
- У темы 16 пустая папка `img/` (у прежних тем по три картинки, у `analytical` тоже ноль),
  поэтому `01-expansion.jpg` даёт 404 на каждой загрузке на всех языках.
- Приветствие тьютора остаётся русским на en/es/ar, хотя заголовок панели переведён.
- На каждой английской странице статьи по-прежнему два 404 на локализованную таксономию arXiv.

**Возвраты по ролям.** Обучение — 2 (картинки темы, приветствие тьютора). Ведущая — 1
(webp, тяжёлый). Разработчик — 3 (учёные латиницей, поле `c` у статей, нулевые счётчики)
плюс тройная копия списка проверок. DevOps — 5 вопросов к схеме очереди, не блокеры.

**Ветка.** `qa-krug1-1`.

---

## 2026-07-29 · баг с мобильной от владельца: тёмные прямоугольники вместо текста. Починен

**Сделал.** Воспроизвёл, нашёл причину, починил в ветке и проверил числами. Задето было три
страницы, не одна: `frontier.html` (та, что показал владелец), `learn.html` и `lecture.html`.

**Причина.** У этих страниц **своя политика темы**, отличная от сайтовой. В каждой лежит
собственная палитра и блок `@media (prefers-color-scheme: dark)`, который её переворачивает
по системной настройке телефона. А общий `css/style.css` про системную тему не знает вовсе —
`prefers-color-scheme` не встречается в нём **ни разу**, тема переключается только атрибутом
`data-theme`. При этом `style.css` подключён **после** локального `<style>` (frontier.html:87),
и у обоих селектор `:root` — одинаковая специфичность, побеждает поздний.

Дальше арифметика. Читатель открывает страницу с телефона в ночном режиме, темы руками
не выбирал, `data-theme` не выставлен. Переменные разъезжаются:

| переменная | откуда берётся | значение |
|---|---|---|
| `--paper-raised` (фон плашки) | только из страницы, системный блок сработал | `#1B2128` — тёмный |
| `--ink` (цвет текста) | из `style.css`, он системную тему игнорирует | `#0F1626` — тоже тёмный |

Тёмный текст на тёмном фоне: контраст **1.11** у `.q-status` и **1.07** у `.card` — плашка
выглядит сплошным прямоугольником. На `learn.html` так же обнулялись **28 блоков, включая
плитки тем**, то есть дерево курса читалось как пустые прямоугольники; на `lecture.html` —
13 абзацев текста лекции.

Почему текст появлялся после переключения темы туда-обратно: нажатие ◐ выставляет `data-theme`
явно, и обе палитры начинают отвечать из одного блока — расхождение исчезает.

Это наши грабли 3 в чистом виде: правило про тему живёт в двух местах и разошлось. Плюс
грабли 2 — ошибки нет, просто нечитаемо.

**Как починил.** Убрал из трёх страниц блок `@media (prefers-color-scheme: dark)`, оставив
переключение темы там же, где оно у всего сайта — по `data-theme`. Правку не размазывал:
порядок подключения стилей и общий `style.css` не трогал, чтобы не задеть шапку и работу
дизайнера. На месте блока оставил комментарий с причиной, чтобы его не вернули обратно.

**Проверил.** headless-браузер, 375 px, `Emulation.setEmulatedMedia` с системной тёмной:

| страница | было (система тёмная) | стало | система светлая | по кнопке ◐ |
|---|---|---|---|---|
| `frontier.html` | `.q-status` 1.11, `.card` 1.07 | 16.82 и 14.14 | без изменений | 13.12 и 11.49 |
| `learn.html` | 28 блоков с контрастом 1.07 | 0 нечитаемых | 0 | 0 |
| `lecture.html` | 13 блоков с контрастом 1.07 | 0 нечитаемых | 0 | 0 |

Ручная тёмная тема по кнопке работает как прежде — проверял отдельно, после правки.

**Не сделал.** `memo.html` — один блок с контрастом 1.25 (`<b>Внимание.</b>`), причина другая
(своего блока `prefers-color-scheme` там нет), не трогал. Ещё на всех десяти корневых
страницах в системной тёмной остаётся блёклый вторичный текст с контрастом 2.24–2.73
(подзаголовки, логотип): читается, но бледно. Это уже решение дизайнера — либо общий
`style.css` учит системную тему, либо страницы перестают держать свои палитры. Отдельной
правкой не делал: девять страниц изменили бы вид, а это не зона QA.

**Нашёл по дороге.** Порядок подключения стилей на корневых страницах разный: у `reference`,
`mathkit`, `hypotheses`, `discoveries` ссылка на общий файл идёт **до** локального `<style>`
(так правильно), у `learn`, `memo`, `course`, `frontier`, `lecture` — **после**, и локальные
правила проигрывают общим. Само по себе это не ломает, но это тот же класс расхождения
и следующий такой баг придёт оттуда.

**В чек-лист** [qa.md](../qa.md) добавлена строка регрессии: проверять контраст при
`prefers-color-scheme: dark` **без** нажатия ◐ — именно в этом состоянии открывает страницу
читатель с телефона, и именно оно у нас не проверялось.

**Ветка.** `qa-release-check`.

---

## 2026-07-29 · приёмка выпуска: 18 пунктов таблицы + сквозной прогон. Вердикт — не публиковать

**Вердикт.** Выпуск не принимаю. Из 18 пунктов таблицы сходятся 6, частично 8, не сходятся 4.
Сверх таблицы подтверждено ещё около сорока находок, из них четыре читатель увидит на первом
же экране не-русской версии.

Ничего не публиковалось: `cloudflare/.r2-manifest.json` последний раз менялся в 17:11
(тот самый случайный выпуск проверкой ссылок), а пересборка закончилась в 18:37 — значит после
неё в R2 не уезжало ничего. Проверял на этой сборке.

---

### Как проверял

Локальная сборка, свой сервер на порту 8429. На 8420 и 8421 висели серверы других сессий —
если бы взял их, мерил бы чужое дерево и не заметил.

Мерил headless-браузером через CDP: панель превью рапортует ширину 375, а `innerWidth`
остаётся десктопным, и на этом у нас уже был заведён ложный баг. В каждом замере печатался
реальный `innerWidth`; замеры, где он не совпал с заказанным, выбрасывались и переснимались.

Каждая находка проходила отдельного проверяющего с заданием **опровергнуть**. Из 57
перепроверок подтвердились 55, две отсеяны — обе попали в раздел «что оказалось не находкой».

**Сборка была свежая и полная:** прогон 17:27 → 18:37, 1981 статья, шаблоны и CSS от 17:20,
замка `.build.lock` не было. То есть проверял результат слияния, а не промежуточное состояние.

---

### Таблица выпуска

| # | Что | Вердикт | Коротко |
|---|---|---|---|
| 1 | Переключатель порядка ленты | **сходится** | по умолчанию «вперемешку», подписи переведены на 4 языка, кириллицы в en/es/ar нет |
| 2 | Порядок запоминается | **сходится** | `b42_sort` в localStorage; ключ общий на все языки — считаю умыслом, порядок чтения не свойство языка |
| 3 | Переключатель скрыт в избранном/разделе/дате | частично | скрыт в избранном и под фильтром даты; **виден на тегах, законах, учёных, разделах**, а в чипах разделов и в поиске ещё и врёт: активна «сначала старые» при сортировке по убыванию |
| 4 | Статья не показывает саму себя | **сходится** | 62 страницы, себя в списке — 0 |
| 5 | «Следующая» не предлагает прочитанное | **сходится** | пройдено подряд, повторов нет |
| 6 | Кнопки шапки ≥ 44 px на 375 | **сходится** | на всех типах страниц, включая звезду и гамбургер |
| 7 | Путеводители у всех тем, 4 языка | частично | путеводители работают; **из 360 ссылок раздела «Материалы по теме» 136 битые**. И тем не 14, а 15 |
| 8 | Подписи схем на языке страницы | частично | 57 схем из 59 переведены; в двух кириллические индексы «Qг/Qх/Tг/Tх» вшиты прямо в словарь на всех языках |
| 9 | Единицы в формулах на языке страницы | **не сходится** | формула под стендом переведена, **сама сцена — нет**: «a = 1.00 м/с²», «тр = 40 Н» на арабской странице. 8 моделей из 18 |
| 10 | Связи уроков ведут на живые страницы | частично | починен только слой параграфов; **обзорная глава темы — первое, что открывает читатель — по-прежнему ведёт в 404**, 15 мёртвых адресов × 4 языка |
| 11 | Иконки вместо эмодзи | **не сходится** | набор работает, но эмодзи остались в разметке генератора: 💡 на тегах/законах/учёных, 🎯 в статьях, 🔍 и 📅 в about, осиротевший U+FE0F в заголовках облаков |
| 12 | Шкала касания | частично | шкала работает на части сайта (97 из 100 контролов на странице тега); поштучные значения не удалены, у шкалы вторая копия в about.html и она уже разошлась; аналитика и учебные страницы вне шкалы, контролы 16–34 px |
| 13 | Правый кластер шапки | частично | сам кластер `nowrap` и одной высоты на 72 замерах — сходится; расхождения в соседней геометрии вынесены отдельно |
| 14 | Тема 15 «Теоретическая механика» | **не сходится** | **из дерева недоступна ни на одном языке**; обзорная страница падает |
| 15 | Меню сайта на 10 учебных страницах | **не сходится** | **на memo.html меню нет вообще**; в самом меню два пункта ведут в 404 |
| 16 | Аналитика: шапка, легенда, «Напряжения» | частично | все три требования выполнены на ru/en/ar; не выполнено «консоль без ошибок» — два 404 на английской версии |
| 17 | Обложки 496/496, 363/363, 145/145 | частично | **счётчики честные, а на сайте обложек нет на en/es/ar** |
| 18 | Кириллицы нет на `learn.html?lang=ar` и `reference.html?lang=en` | **сходится** | было 102 и 38 слов, стало 0 и 0. Проверил сам, отдельно от агентов |

---

### Блокеры: это увидит любой читатель

**1. Обложек статей нет на английской, испанской и арабской версиях.**
Лента ar: 0 живых картинок, 8 битых, 8 скрытых обёрток. То же на en и es, на ru — 8 живых.
[js/search.js:821](../../js/search.js) строит путь по языку страницы, а картинки лежат один
раз под `ru`; оба запроса дают 404, блок картинки прячется. Правило про обложки живёт в трёх
местах и разошлось во всех трёх: `search.js` берёт язык страницы, [js/scroll.js:89](../../js/scroll.js)
берёт язык по умолчанию (верно), [templates/article.html:10](../../templates/article.html)
подставляет `$authors_lang` = `en` — поэтому **og:image битый во всех 160 проверенных
страницах**, то есть ссылка на статью в мессенджере и соцсети приходит без картинки на всех
четырёх языках, включая русский.

Именно из-за этого пункт 17 и прошёл по счётчикам: файлы на месте, 496/496, модель у всех
`FLUX-2-pro` — а на сайте их не видно.

**2. Тема 15 недоступна.** `/learn.html` на всех четырёх языках показывает 14 узлов, узла
`analytical` нет. Причина — список тем хранится в `index.json` дважды: наверху 15 тем,
в каждой языковой ветке по 14, и языковая ветка затирает верхнюю. Обзорная страница темы вдобавок
падает с исключением: у неё схема `b42.course/1`, у остальных четырнадцати `/2`. Материалы
и стенд сделаны и работают — попасть в них нельзя.

**3. `lecture.html` не открывается ни на одном языке.** По умолчанию запрашивается `01-gr`,
а файл называется `02-gr.json` — сервер отдаёт HTML вместо JSON, и читатель на всех языках
видит русскую строку «Не удалось загрузить лекцию: SyntaxError».

**4. `course-thermodynamics.html` — флагманский интерактивный учебник — сломан на en/es/ar.**
Вместо содержания `TypeError`, плюс видимые русские кнопки единиц «СИ · бытовая · техническая ·
имперская · планковская» и внутренняя записка разработчика.

---

### Язык: седьмой слой утечки

К шести известным слоям добавился седьмой — **словарь без ключа `es`, молча падающий
на английский дефолт**. Найден в трёх независимых местах:

- [generate.py:687](../../generate.py) — вся обвязка статьи: «Related articles», «Feedback»,
  «Next article →», модалка «I am the author» на **100 % из 7961 испанской страницы статьи**.
- Словари страницы тега, страницы закона, облака и страницы учёного — плейсхолдеры поиска
  по-английски на испанских страницах.
- Подписи боковых блоков связей: на **всех 709 испанских страницах справочников** стоят
  «Discovered by», «Related concepts», «Related laws». Испанских подписей — ноль.

Отдельно по страницам:

- **`frontier.html` — целиком по-русски на en/es/ar**: 15 810 кириллических символов.
  `data/theory/frontier.json` содержит только ключ `ru`, а [frontier.html:123](../../frontier.html)
  делает `d[LANG] || d.ru` — молчаливый откат вместо ошибки.
- **`memo.html`** — «К параграфу», «Схема», «· интерактивный учебник» по-русски на всех языках.
  На арабской карточке это выглядит как «شجرة المعرفة К параграфу طباعة».
- **Сплошной скан 23 883 построенных страниц**: кириллица в видимом тексте в 549 файлах,
  107 уникальных статей. Крупнейший слой — русские единицы внутри LaTeX (48 статей),
  например `g/2\pi = 118\text{ МГц}`.
- **24 статьи с русским H1 на не-русских страницах** — механизм заглушки «ещё не переведено»
  работает и статьи исключены из ленты, но заголовок остался русским.
- **Сырые маркеры `[tag:…]`, `[scientist:…]` в видимом тексте**: 1419 файлов, 138 статей.
  [generate.py:386](../../generate.py) `trivia_html()` не прогоняет «занимательный факт»
  через `parse_markers`.
- **Арабская подпись одной и той же кнопки уровня разная в двух местах**: на статье «متقدم»
  (сервер), на карточке ленты «مفصّل» (клиент). А подсказка кнопки «Популярно» сравнивает
  её саму с собой: «مبسّط — أغنى من «مبسّط»».
- **Испанская плашка отката уровня без диакритики** на 2968 страницах: «Anadelo a favoritos»
  вместо «Añádelo», «version» вместо «versión». Та же ошибка уже заготовлена для французского.

---

### Меню и навигация

- **`memo.html` без меню сайта.** [js/sitenav.js:12](../../js/sitenav.js) ищет `.top-bar`,
  а у шпаргалки шапка называется `.bar` — скрипт подключён и молча выходит. Уйти со страницы
  можно по одной ссылке. Это ровно тот сценарий «с урока некуда уйти», ради которого
  `sitenav.js` и писали.
- **Меню ведёт в 404**: `/theory/` на всех четырёх языках (раздела нет — об этом прямо написано
  в комментарии `search.js:1338`) и `/authors/` на ru, es, ar (авторы собраны только по-английски).
  7 мёртвых пунктов из 44. `run.py links` их не видит: меню собирается в браузере.
- **Три разных меню с тремя разными составами**: шаблоны дают 9 пунктов, `sitenav.js` — 11,
  `about.html` имеет собственную разметку с 8. Карта сайта не совпадает сама с собой.
- **Аналитики и архива нет в карте сайта** `lang/*/data/about.json`, хотя оба раздела
  существуют и открываются. Составы карты между языками при этом совпадают — это единственная
  хорошая часть.
- **Корневой `/index.html` убивает выбранный язык**: [index.html:31](../../index.html) —
  безусловный `window.location.replace('/lang/en/index.html')`. Читатель, который вчера читал
  арабскую версию, сегодня с домена попадает в английскую. Выбор флагом и подгрузка `config.json`
  ниже по файлу — недостижимый код.
- **`?lang` не передаётся во внутренние ссылки корневых страниц**: свежий браузер по ссылке
  вида `/course.html?t=thermodynamics&p=01-molecules` получает русский.

---

### Шапка и ширина: грабля 9 закрыта не везде

- **`mathkit.html`: разъезд 224 px на 1280.** `.top-bar` и `.langs-row` с 446-го пикселя,
  текст — с 222-го. Это буквально тот баг, который в правилах описан как вылеченный.
  Причина: `alignToContent()` берёт `querySelector('#root, main, .wrap, .container')`, а в
  mathkit `#root` — вторая колонка грида, а не контейнер страницы.
- **RTL: знак поправки перевёрнут.** `sitenav.js` пишет `marginInlineStart = cur + delta`,
  но в RTL это margin-right, и увеличение двигает блок влево — коррекция удваивает ошибку
  и накапливается с каждым resize. Шапка уезжает за край экрана.
- **Облачные страницы собранного сайта**: `.top-bar` упирается в потолок 620 px, а контент
  занимает 868 — правый край меню на 248 px раньше края страницы. В арабской версии зеркально.
- **Страница статьи в ar**: заголовок и текст на 40 px внутрь от края шапки. `.article-main`
  держит физический `padding … 0` под правую колонку в LTR, правила для `dir="rtl"` нет.
- **`lecture.html` и `frontier.html`**: шапка лежит внутри `.wrap` и равняется по внешней
  рамке — 16 px в ltr, 64 px в rtl.

---

### Мобильная: что вылезает и что теряется

Отдельная просьба владельца. Агент, которому этот кластер достался, не вернулся — прогнал сам:
17 типов страниц × ru/ar × 375 и 320, каждый замер с проверкой реального `innerWidth`
и повтором при несовпадении.

**Хорошее сразу:** горизонтальной прокрутки нет нигде — `scrollWidth` равен `innerWidth`
на всех страницах, обеих ширинах и обоих языках. Шапка везде в 2 ряда (у шпаргалки 3).

**Строка сводки обрезается на главной и в избранном.** На 375 видно
«2016 статей · 145 законов · 363 тегов · 156 разделов · 2», а дальше обрыв: «201 учёный ·
17.4k авторов · 4 языка» стоит на x = 434…539 при экране 375 — то есть **за экраном,
и доскроллить до неё нельзя**, потому что страница не прокручивается вбок. На 320 за краем
уже три числа вместо двух. В арабской версии зеркально: x = −170…−16. Воспроизведено
6 раз подряд, подтверждено скриншотом. Причина — `white-space: nowrap` на строке сводки.

**Навигация внутри статьи пропадает на телефоне.** `#section-nav` («↑ наверх · Граф · Отклик ·
Похожие статьи», 4 ссылки) на 1280 `display:block`, на 375 `display:none`, и замены нет —
из пяти якорных ссылок остаётся одна, и та из блока графа. То же на арабском. Возможно, так
задумано, но именно на телефоне страница самая длинная, и «↑ наверх» там нужнее всего.

**Текст не влезает в кнопки** (`scrollWidth > clientWidth`):
«Следующая статья: Почему…» 350 против 171 (ar: 350 против 171), «i» 28 против 12,
«ещё ▾» / «آخرون ▾» 40 против 36, «أنا المؤلف» 41 против 38.

**Что НЕ находка**, хотя выглядело похоже: `div.nav-links` исчезает на 375 на всех страницах —
это десктопное меню, спрятанное за гамбургер, так и задумано. `a.card-img-wrap.img-ready`
«пропадает» — на самом деле это ленивая загрузка: на узком экране до части картинок просто
не дошла очередь, при повторном замере 12 из 12 на месте.

---

### Поиск

- **На `/tags/` (363 тега), `/sections/` (156 разделов), `/archive/` и `/analytics/` поиска
  нет вообще.** У соседних `laws-cloud.html` и `scientists-cloud.html` блок поиска есть,
  у `tags-cloud.html` — нет.
- **На `/laws/` поиск мёртв у нового посетителя**: вид по умолчанию — «структура», а
  `filterLaws` фильтрует скрытый контейнер `#law-cloud`. Человек вводит запрос и не видит
  ничего.
- **Избранное: поиск подменяет избранное всем сайтом.** При пустом избранном ввод «чёрные»
  выдаёт 20 статей со всего сайта.
- **Поиск ищет не по тому тексту, что показан на карточке**: фильтр идёт по `description`,
  а карточка выводит `abstract`. Отсюда и пропуски (ru «чёрные» — 28 статей не находятся,
  en «quantum» — 119), и нерелевантная выдача (9 карточек из 20 без видимого совпадения).
- **Учёные ищутся только по латинице.** Арабский читатель, набрав «أينشتاين», получает
  «العالم غير موجود», хотя Эйнштейн в базе есть — поле `name` не переведено ни в одном языке.
  Плюс арабский артикль ломает поиск по подстроке: «ديناميكا» находит закон, «الديناميكا» — нет.
- **Пустой результат в облаках молча оставляет чистый лист** — ни «ничего не найдено»,
  ни подсказки. На `/authors/` сделано правильно, в двух других местах — нет.
- **Панель поиска вылезает за край экрана на 375**: выступ 55 px влево, в поле видно «ntum»
  вместо «quantum». На 320 выступ меньше — то есть на более широком экране обрезка больше.
- **Пока едет полный индекс (1.5–2.5 c локально), поиск отвечает по 60 свежим статьям
  и не помечает, что выдача неполная.** На телефоне через мобильную сеть человек увидит
  «почти ничего не найдено».

---

### Связность графа: тег ⇄ закон ⇄ учёный

Считал по `data/knowledge-graph.json`, критерий — наличие ребра к узлу **другого типа**
из тройки тег/закон/учёный (рёбра к разделам arXiv не в счёт).

| Тип | Всего | Есть межтиповая связь | Только свой тип | Без связей |
|---|---|---|---|---|
| Теги | 363 | 303 | **60** | 0 |
| Законы | 145 | 145 | 0 | 0 |
| Учёные | 201 | 201 | 0 | 0 |

Рёбер на несуществующие узлы — 0. Мягкие дыры: 18 законов без учёного, 33 учёных без закона.

**60 тегов-сирот** — это в основном био/мед и ML-теги свежих циклов (crispr, antibody, virus,
genomics, neuron, transformers, large_language_models…): слой законов и учёных чисто физический
и их не покрывает. Но среди них есть и настоящие пробелы физики: `casimir_effect`,
`aharonov_bohm_effect`, `instanton`, `zeta_function`, `wigner_function`.

Причём **у трёх из них парный узел уже существует в графе и просто не связан**:
`t:casimir_effect` ↔ `l:casimir_effect` (совпадение имени точное),
`t:aharonov_bohm_effect` ↔ `l:aharonovbohm_effect` (расходится нормализация дефиса),
`t:wigner_function` ↔ `s:Eugene Wigner`. Это сломанный линковщик, а не пробел в данных.

Дальше по графу:

- **Экран противоречит сам себе**: на 36 русских страницах тегов блок «Учёные» пуст, а мини-граф
  под ним показывает до 17 учёных. Страница читает `tags.json`, граф — `tags-graph.json`.
- **90 упоминаний учёных из 71 уникального имени выбрасываются молча** при сборке графа: имени
  нет в справочнике, а `add()` пропускает без записи в лог. Шесть имён записаны кириллицей —
  «Альберт Эйнштейн», «Макс Планк», «Евклид», «Пифагор», «Леонард Эйлер», «Архимед» — при том
  что `Albert Einstein` и `Max Planck` в справочнике есть. Это и есть главный источник
  «18 законов без учёного».
- **Срез до фильтра съедает последнюю живую связь**: `generate.py:1729` берёт первые шесть имён
  и только потом отбрасывает несуществующие. У `ideal_gas_law` из семи валидно седьмое —
  блок «Открыли» не рисуется вовсе.
- **Связи не переводятся, а копируются в каждый `lang/*/data/laws.json`** и уже разошлись:
  блок «Открыли» есть на 126 русских страницах законов, 117 английских, 117 испанских,
  122 арабских. У `archimedes_principle` по-русски есть Архимед, по-английски — нет.
- **В испанском и арабском `laws.json` id тегов переведены**: `'agua'`, `'الجاذبية'` вместо
  канонических — 21 битая ссылка на es и 35 на ar, 7 испанских и 12 арабских законов остаются
  вообще без блока тегов.
- **Мини-граф закона пуст на 18 страницах** из 145: одна точка, ноль связей.
- **Граф одной и той же статьи разный на разных языках** — разошёлся у 19 статей из 20:
  ru/es дают 96 узлов, ar — 104, en — 72.
- **Легенда не совпадает с цветами узлов**: кружки в разметке `#6C5CE7/#D64545/#2FA84F`,
  узлы на холсте `#2E8AA0/#C77F3A/#3E8E5A`. Ни один цвет легенды на холсте не встречается —
  при том что комментарий в генераторе прямо утверждает обратное.
- **На 375 подписи узлов режутся краем холста**: 10 из 50.
- **На десктопе показывается мобильный текст** «лёгкая мобильная версия — крупный граф
  на телефоне тормозит» при ширине 1280.
- **Испорченный регистр в именах тегов**: `lIGO`, `vLBI`, `cRISPR`, `eHT` — похоже на
  «первую букву в нижний регистр», применённое к аббревиатуре. Идёт в подпись узла и в облако.

---

### Целостность и выпуск

- **`run.py links`: 11 битых целей** против прежних 17. Все из известного списка, кроме
  `/lang/en/sections/cs_PL.html` — та же причина (раздел arXiv вне периметра), просто шестая
  категория, а в списке значилось пять.
- **`run.py check` не проходит: 347 «недопечённых» папок** — скачано сырьё, страницы нет.
  Читателю не видны и в ленте не значатся. Но `deploy_r2.py` не исключает `.webp`
  и `references.txt`, поэтому при выпуске **уедет ≈290 МБ обложек и списков литературы
  к статьям, которых не существует**. На диске под ними ещё 1,35 ГБ PDF (эти не публикуются).
- **Защита от «публикации как побочного эффекта» закрыта наполовину.** Список `READONLY`
  поставлен на публикацию, а [run.py:920](../../run.py) `_backup_to_r2()` вызывается после
  **любой** команды без всякого списка — то есть `run.py check` и `run.py links` по-прежнему
  льют исходники в приватный бакет. Свои прогоны делал с `SKIP_R2_BACKUP=1`.
- **Формулы висят на внешнем CDN с молчаливым откатом.** `reference.html:116` и
  `mathkit.html:107`: `if (!window.katex) { node.textContent = latex; return; }`. Если
  `cdn.jsdelivr.net` недоступен или медленный, читатель видит 268 кусков сырого LaTeX и ни
  одного сообщения об ошибке. Для кувейтского университета это не гипотетический риск.
  Сам сайт свои `js/` отдаёт локально — KaTeX можно положить туда же.
- **На 74 % архива уровни дают один и тот же текст** (1482 статьи из 1994 — все экспресс).
  Молчаливого отката тут нет, над текстом честно стоит плашка, но переключатель уровня
  на трёх четвертях архива почти ничего не меняет.
- **198 статей из 1980 без обложки вовсе**, из них 119 даже не помечены `image_pending` —
  то есть её никогда не получат.
- **Два 404 на каждой английской странице**: `/data/arxiv-categories-en.json` и
  `-descriptions-en.json`; для английского файл лежит без суффикса. Откат честный,
  но шум в консоли на всём английском разделе.

---

### Что оказалось НЕ находкой

Пишу отдельно, чтобы не искали дважды.

- **Сырой LaTeX на `reference.html` и `mathkit.html`** — артефакт стенда. В headless-браузере
  запрос к `cdn.jsdelivr.net` вообще не уходит (`performance` показывает ноль запросов
  к katex), хотя с машины CDN отвечает за 0.7 с. Дефект не завожу — завожу только риск
  внешнего CDN, см. выше.
- **Логотип учебных страниц ведёт на русскую главную** — в разметке действительно
  `/lang/ru/index.html`, но `fixHomeLink()` в [js/course-i18n.js:277](../../js/course-i18n.js)
  переписывает его под текущий язык. Проверил в браузере: ведёт куда надо.
- **Переполнение 1510 px на главной** — не воспроизвелось. Замер попал на неустоявшуюся
  раскладку; при повторе `scrollWidth` = `innerWidth` на всех языках и обеих ширинах.
- **Обложки лежат один раз под `ru`** — это умысел ради экономии места, а не промах.
  Промах — что путь к ним строится по языку страницы.
- **Авторы существуют только по-английски** — осознанное решение (экономия ~600 МБ).
  Для арабского читателя это всё же разрыв пути: со страниц ar в английские авторские
  ведут 6330 ссылок.
- **Ссылки собранного сайта в чужой язык** — их нет: обход 3734 файлов и 1 129 146 ссылок
  дал ровно 6 на язык, и все шесть — переключатель языка.
- Из 57 перепроверок отсеяны 2: пересчёт сирот по другому критерию (я привёл свой критерий
  явно) и завышенная оценка битых картинок в теле статей (там 977 внутренних целей, все 200).

---

### Что мешало проверке

**В дереве параллельно работала другая сессия.** С моей точки старта прилетело 14 коммитов,
статей стало 1997 вместо 1980, страницы переписывались в 19:34–19:39 — то есть часть счётчиков
снята с движущегося дерева. Выводы по коду это не трогает, счётчики на финальной сборке стоит
переснять. Заодно чужие коммиты легли в мою ветку `qa-release-check`, потому что дерево общее.

**Харнесс дважды подключился к чужому headless-браузеру** (соседняя сессия заняла тот же
отладочный порт) — признак был в том, что `location.href` не совпадал с запрошенным. Все
спорные замеры переснимались с проверкой href и `innerWidth`.

**Один агент из 43 не вернулся** — и это оказался основной обход мобильной версии. Прогнал
этот кластер сам, результат в разделе «Мобильная». Итого проверено 42 из 43 задач второго
круга плюс мой прогон вместо выпавшего.

**Своё дублирование убрал:** завёл было `задачи/QA-ЧЕКЛИСТ.md`, но ведущая сессия в те же часы
превратила `задачи/qa.md` в такой же растущий чек-лист. Держать два — ровно те грабли «одно
правило в двух местах», которые мы ловим. Свой файл удалил, уникальное дописал в `qa.md`.

---

### Что предлагаю

Вернуть авторам, публиковать после четырёх блокеров: обложки на не-русских версиях (и заодно
`og:image` на всех четырёх), доступность темы 15, `lecture.html`, `course-thermodynamics.html`
на en/es/ar. Остальное — по важности, но эти четыре видны сразу и на первом экране.

**Ветка.** `qa-release-check` (в ней же чужие коммиты — дерево общее).
