# Отчёты · Ведущая сессия

Формат — [README.md](README.md). Новая запись сверху.

---

## 2026-07-31 ~14:00 · Волна 2 слита, блокеры QA: два закрыты и на проде, два зафиксированы

**Слито 6 веток волны 2** (все «готово» за ~40 минут работы команды): devops
(/api/ask живой, замок деплоя predeploy_check.py), дизайнер (подписи сущностей —
один источник, формы кнопок 6→3, чипы по шкале), разработчик (статья не тянет 6,4 МБ,
класс B42Refs закрыт целиком, разбор цепочки заказа), промпты (math-seeds трёх слоёв +
аудит мёртвого словаря), QA (маршрут: БЛОКЕР ×4), стратегия (механизм совета).

**Вердикты по четырём блокерам QA:**

1. **«fr собран целиком» → заявка исправлена, код честен.** На диске 133 настоящих
   fr-текста + 1977 честных заглушек с кнопкой заказа; индекс/лента/дашборд им
   соответствуют. «Целиком» относилось к ИНФРАСТРУКТУРЕ fr (laws/graph/analytics/
   feed) — так и записываем впредь: «инфраструктура fr готова, контента 133 (6%)».
   Наполнение — воронкой по заказу читателя, это и есть задуманная механика.
2. **Счётчик тега (509 при 372/23 карточках) — ПОДТВЕРЖДЁН, не чинил наспех.**
   Корень найден: шапка берёт article_count из knowledge-graph (глобальный, по всему
   корпусу, безъязыковый — generate.py:1607), лента — из языкового articles-index.
   Правильная правка — счётчик от языка страницы (и заодно блок «статьи по теме»
   в fr-курсе, который отдаёт английские заголовки — тот же корень). Задача на
   следующую волну разработчику/мне, влезать в генератор в конце смены не стал.
3. **Поиск жёг норму посимвольно — ЗАКРЫТ, на проде.** Дебаунс 350 мс, короче 3
   символов — только локально, AbortController + таймаут 8 с (зависший Worker не
   держит локальную выдачу). Проверено посимвольным набором: 11 букв → 1 запрос,
   карточки не копятся. Коммит + publish.
4. **Кнопка «перевести» не могла сработать — ЗАКРЫТ, на проде.** Клиент шлёт пропуск
   Turnstile (общий помощник js/b42-turnstile.js + ленивый догруз для уже собранных
   страниц) и body.lang; коды ошибок клиента = кодам сервера. Коммит + publish.
   Вдогонку по находке разработчика: постоянного исполнителя очереди НЕ БЫЛО —
   поднял queue_worker --loop процессом (умрёт с перезагрузкой), DevOps'у задача №0:
   Scheduled Task b42_queue. Очередь была пуста, заказы не потеряны.

**Мелочь из отчёта QA (не блокер):** интерфейс fr-аналитики английский (вкладки/
подсказки) — данные я уже перегнал с fr-трактовками (на проде), остался словарь
интерфейса в js — в волну 3 дизайнеру/дашборду.

**Разбор отчётов волны 2 по ролям — при следующем подъёме команды** (владелец свернул
день). Вопросы, ждущие ответов: devops→ML (вектора), devops→дашборд (журнал),
разработчик→devops (три находки queue_worker — ответ на №2 я дал: чинить обязательно).

---

## 2026-07-31 день · Волна 1 разобрана, выпуск запущен, волна 2 разложена по файлам

**Слито 4 ветки:** bot-guard-4 (worker выложен — 304 живой, четыре запроса в отчёте;
три дыры QA закрыты fail-closed; предел по адресу; fix_encoding.py в git — чинит
pre-commit у всей команды), dash-coverage-1 два новых захода (темп/рост/охват + языки/
связность/машинное время; fr — 132 из 2110, ar — 2109), ml-diamonds-1 (записка
«бриллианты» — принята, разбор в его файле), design-unify-labels-5 (доска заново;
конфликт fix_encoding решён в пользу main-версии; **унификация владельца не начата** —
осталась главной задачей дизайнера). Разработчик и QA волну не сдали — их задачи
перенесены в волну 2 без изменений.

**Выпуск: решение принял сам** (владелец делегировал) — запущена полная пересборка
с публикацией хвостом. Причина полной, а не точечной: dev-bot-ui-4 менял шаблон статьи
(кнопка «перевести»), это все страницы; заодно уезжают js дашборда (пустые счётчики на
проде), стили и весь утренний реюз. QA примет выпуск в волне 2 по своему списку.

**Волна 2 разложена по файлам ролей** — `задачи/<роль>.md`, раздел «ВОЛНА 2» у каждого:
разбор его отчёта + задачи. Копировать ничего не нужно, старт — команда владельца.
Первый пункт у всех: перечитать ПРАВИЛА-РАБОТЫ.md. Состав: DevOps — /api/ask, замок
деплоя, журнал прогонов + алерт; дизайнер — унификация (главное); разработчик — вес
статьи + картина цепочки заказа перевода; дашборд — карта проекта, журнал прогонов
с DevOps; ML — мерка по OpenAlex (добро на этап 1, платного ноль); промпты — мёртвая
половина словаря тегов (180 из 363); QA — маршрут ночи + приёмка выпуска волны.

**Хвосты за мной:** поле даты апгрейда в data.json (для воронки на дашборде); удалить
слитые ветки после пересадки авторов; живой прогон апгрейда и ре-перевод 3 упрямых
тиров — по добру владельца (вопросы стоят).

---

## 2026-07-31 утро · Архитектор принял смену: слияния, волна, реюз экспресса

**Главное.** (1) Слиты dash-coverage-1 и dev-bot-ui-4 — второй ночная лента ошибочно
числила слитым, git показал 4 живых коммита (интерфейс бота, кнопка «перевести», дыры
релевантности). Доска переписана и сведена к git-правде, задвоенные блоки убраны.
(2) Волна разослана — `задачи/ВОЛНА-2026-07-31.md`, блоки у DevOps (выкладка worker.js
первым делом), дизайнера (сквозная унификация владельца), разработчика, дашборда, ML, QA.
(3) **Реюз оплаченного при regen сделан** (af76aa72a) — главный техдолг воронки закрыт.

**Реюз — что изменилось.** regenerate_article больше НЕ удаляет папку статьи: генерация
идёт поверх, при падении статья остаётся живой. build_article при force читает старый
data.json и реюзит: переводы аннотации и подписей (их источники не меняются), FLUX-обложку
с полем image_model (раньше поле терялось, pro-обложка молча заменялась кадром из PDF),
короткий текст экспресса — он становится mini полной, его переводы остаются по языкам.
Новое: `run.py regen <id> --lang fr` — полная на языке запроса, остальные языки не платят
за перевод и сохраняют экспресс-контент (полезнее заглушки) либо честную заглушку с кнопкой
заказа. Упавший в русский откат перевод тоже подменяется старым хорошим — ещё один
молчаливый откат закрыт. Попутно webp_convert пересобирает webp при jpg свежее webp —
без этого неудаляемая папка молча оставляла бы читателю старую картинку.
Проверено сухим тестом со стабами LLM/PDF (5 проверок: abstract, mini, es/ar-контент,
только-fr перевод, обложка+флаги). Живой платный прогон апгрейда — по решению владельца:
кандидат — любая экспресс-статья 30 июля с FLUX-обложкой.

**Попутно, классом (правило «чини тип»):** печать эмодзи роняла скрипты на cp1252-консоли —
guard добавлен в 10 файлов (упало сразу после слияния corpus_stats); в двух инструментах
ночного снятия хардкода языков (theory_index_i18n, scan_models_i18n) скобочный
AttributeError — падали при каждом запуске; удалены 4 папки-обломка 2026-07-30 без
data.json; в analytics_build.py остался хардкод LANGS без fr — выделено отдельной задачей
(чип), не стал трогать мимоходом.

**Параллельная активность.** Владелец работает второй сессией в главном дереве:
a6a97ccdc (ПРОЕКТ.md) и c7f99dc41 (страница «изнутри», council.html) легли между моими
коммитами. Конфликтов нет, но перед каждым коммитом сверяюсь с логом.

**Вопросы владельцу.**
1. Математике дать слот в дневном экспрессе? Сейчас 0 из 25 (доли в tools/daily.cmd).
2. Токен Kaggle (kaggle.json) для tools/update_arxiv_dump.py — без него обновление дампа
   не автоматизировать; после токена заведём напоминание раз в квартал.
3. Живой прогон апгрейда экспресс→полная (см. выше) — дать добро на одну статью?

**Не сделал.** Ветки dash-coverage-1 и dev-bot-ui-4 после слияния не удалены — заняты
worktree-папками авторов; удалю, когда пересядут на новые. Ре-перевод русских заголовков
(13 дней, команда готова в прошлой записи) не запускал — платный, в один заход с решением
владельца по прогону апгрейда.

---

## 2026-07-31 ~04:30 · Ночь: вес первого визита, карточка ссылки, аккуратность

**Главное.** Два обхода агентами (мобильная аккуратность и «первое впечатление
партнёра») дали 20+ подтверждённых находок. Закрыл всё, что моё; Worker отдал DevOps
первым пунктом — выкладка не моя.

**Что реально чинил, а не полировал:**

- Подсказка (тултип) **улетала в подвал документа на любом устройстве**: правило ниже
  по файлу давало `position: relative` и при равной специфичности било `fixed`, а js
  кладёт туда координаты вьюпорта. Заодно переписано позиционирование — был предел по
  ширине 330 при реальных 280 и без нижней границы (на 320px выходило `left: -10px`),
  а по вертикали предела не было вовсе.
- **Слои касания 44px перехватывали чужие нажатия**: «+» в чипе раздела ложился на
  край следующего чипа (палец добавлял ЧУЖОЙ раздел), «i» у логотипа съедал последние
  13px названия сайта.
- **304 не работал ни в одном браузере** (Worker): Cloudflare при сжатии ослабляет наш
  ETag до `W/"…"`, а сравнение было строгое. Json и html живут с max-age=300 — значит
  каждые пять минут вернувшийся читатель заново качал 5,2 МБ индексов. Правка в git,
  выкладка за DevOps.
- **5,36 МБ на первый визит** при 317 КБ, нужных первому экрану: девять запросов
  стартовали одновременно и делили канал. Тяжёлое (индексы соседних уровней 2,5 МБ,
  граф авторов 375 КБ = 10 МБ разбора) ушло в простой. Замер после правки: 394 мс
  вместо 38 мс, склейка трёх уровней 6015 записей без дублей.
- **Карточка ссылки врала**: у 222 статей og:image указывал на несуществующий файл,
  заявленным размерам 1440×960 соответствовали 27%, а у 216 непереведённых страниц
  og:title оставался РУССКИМ (en 81, es 61, ar 72) — притом что тело честно писало
  «не переведено». У главной, гида и учебных страниц og не было вовсе: партнёр получал
  голую ссылку.
- **Исходник гида был под ignore-правилом** `lang/**`: 99 строк ручного текста на пяти
  языках существовали только в рабочем дереве. Взят в git цепочкой правил.
- **Рабочие файлы лежали в публикуемой data/** — карта наших же слабых мест уехала бы
  на публичный адрес. Перенёс к скрипту, над списком папок написал предупреждение.

**Вес страниц — второй заход, после первого отчёта:**

- Справочники качались ВТОРЫМ комплектом на каждой странице тега, закона, учёного
  и графа: мини-граф дописывал к адресу метку времени, из-за чего кэш не срабатывал
  никогда. 1,61 МБ лишнего трафика и 6,99 МБ лишнего разбора на показ. Метку убрал,
  а справочники теперь берутся из общего обещания search.js. Замер: запросов 6 → 3,
  разбор 13,3 → 6,7 МБ.
- Индекс статей грузился и на СТАТЬЕ, и на карте проекта, где списка нет вовсе.
  На статье это 6,3 МБ разбора поверх того же файла, который вторым заходом берёт
  scroll.js. Теперь индекс только там, где есть список: на статье файл запрашивается
  один раз вместо двух, на /analytics разбор 37 → 7,2 МБ.
- `immutable` на год ставился и адресам БЕЗ версии — корень и одиннадцать учебных
  страниц подключали style.css без `?v=`, и у вернувшегося читателя там навсегда
  застревал старый файл (Ctrl+R не помогает, immutable запрещает и условный запрос).
  Правка Worker + `tools/stamp_assets.py`, который проставляет версию по хэшу файла.
- Кнопки сортировки на телефоне всё ещё уезжали во второй ряд: снятия подписи не
  хватало шести пикселей. Убавил поля, проверил — один ряд.

**Новое, не только починка:**

- Экран ожидания подключён к поиску по смыслу — встроенной карточкой, а не шторкой
  (порог 700 мс, кэшированный ответ не мигает). Проверено на 375px по-арабски.
- `tools/silent_watch.py` — страж молчаливых откатов, наш главный класс багов.
  Базовая линия 87, рост даёт ненулевой код возврата.
- `tools/names_build.py` — справочник id→имя (16–27 КБ на язык). Связи в арабском
  курсе перестали быть латиницей: «درجة الحرارة» вместо «temperature».
- Хвост `run.py` из одного вызова превращён в список DERIVED_ASSETS — производные
  файлы расходятся с корпусом молча, и забыть один вызов легко.
- Гид описал поиск по смыслу, бота-репетитора и звания — на всех пяти языках.

**Пятый язык.** Снят хардкод языков в пяти инструментах (читают config.json), fr
дописан в семь словарей интерфейса.

**Что НЕ сделал и почему:**

- Ре-перевод русских заголовков — трогает data.json, а всю ночь шла пересборка. Обошёл
  ВЕСЬ архив, а не только июль: 91 тир, 58 пар статья×язык, 13 дней (список —
  `temp/cyrillic-titles.txt`). Команда готова:

  ```
  python tools/retranslate_days.py 2026-01-10 2026-04-20 2026-06-29 2026-06-30 2026-07-01 2026-07-02 2026-07-03 2026-07-06 2026-07-22 2026-07-25 2026-07-26 2026-07-27 2026-07-28
  ```

  После неё — точечная пересборка этих дней (`run.py html --only <дата>`), полная не нужна.
- Ширины блоков на /analytics свёл к стандарту (было пять правых краёв, стало два).
  **Видимое последствие, посмотри и скажи:** карточки кластеров теперь в два столбца,
  а не в три — три помещались только потому, что карточки торчали правее сцены на 220px.
  Вернуть три — одна строка, комментарий в css её называет.
- Разделы arXiv переведены на французский (155 названий, 64 описания) — и не руками,
  а `tools/translate_categories.py`: прежние файлы для ru/es/ar делались разово, поэтому
  про них и забыли, когда появился пятый язык.
- Пара правок в generate.py легла ПОСЛЕ старта пересборки и поэтому в неё не попала —
  подхватятся следующей: проверка обложки по webp вместо jpg и глушение предупреждений
  Pillow в логе. Ничего срочного.
- Ряд находок аудита отклонил осознанно: пилюлю типа закона (у 111 из 145 законов это
  НЕ «закон», пилюля несёт информацию), подписи «Узлы/Связи» в эксплорере (без них
  десять чекбоксов сливаются в ленту), зелёный цвет соавторам (он закреплён за узлом
  «учёный», покрасили бы — сломали код цвета).

**Ждёт тебя.** Пересборка стартовала в 00:29, публикация идёт её хвостом
автоматически. Проверить утром: `git log --oneline -15` и маршрут по ссылкам ниже.

### Маршрут по ссылкам (проверять в этом порядке)

Локально сайт поднимается так:

```
python -m http.server 8420
```

**1. Первый экран и вес.** <http://localhost:8420/lang/en/index.html> — открой инструменты
разработчика, вкладку «Сеть», и обнови. Тяжёлые файлы (`articles-index-simple`,
`articles-index-advanced`, `authors-graph`) должны стартовать ЗАМЕТНО позже ленты,
а не вместе с ней. Красных строк 404 быть не должно вовсе.

**2. Подсказки на телефоне.** <http://localhost:8420/lang/ru/tags/> в режиме телефона
(375px). Тапни любую плашку у нижнего края экрана — карточка должна перевернуться ВВЕРХ
и целиком помещаться. Тапни плашку у левого края — не должна уезжать за экран.

**3. Слои касания.** <http://localhost:8420/lang/ru/index.html> на 375px. Тапни в самый
конец слова «bridge42worlds» — должен открыться сайт, а НЕ поповер «о проекте». Тапни
левый край второго чипа раздела — должен смениться набор, а не добавиться чужой раздел.

**4. Француз.** <http://localhost:8420/lang/fr/index.html> — статей должно быть около 1862,
а не одна. Ссылки «laws», «graph», «analytics» в верхнем меню должны открываться.

**5. Араб.** <http://localhost:8420/lang/ar/about.html> — новый раздел про бота-репетитора
и звания, весь текст арабский. <http://localhost:8420/course.html?lang=ar> — открой параграф,
блок связей внизу должен быть арабским: «درجة الحرارة» вместо «temperature».

**6. Экран ожидания.** <http://localhost:8420/lang/ru/index.html>, нажми поиск, переключи
на «по смыслу» и введи запрос. Если ответ дольше 0,7 с — на месте выдачи появится факт
или мини-опрос из наших же статей, с баллами и званием.

**7. Карточка ссылки.** Открой исходник <http://localhost:8420/lang/en/index.html> —
в `<head>` должны быть og:title, og:description и og:image. То же на
<http://localhost:8420/learn.html> и на любой статье. У статьи без обложки og:image
не должно быть вовсе (вместо битой картинки — карточка-строка).

## 2026-07-30 ~01:00 · Выпуск опубликован

**Сделал.** Слил всё готовое в main; полная пересборка (0 ошибок); критичный проход
по решению владельца вместо финального круга QA: арабская лента 375 (обложки 11/11,
кириллица 0, скролл 0, тач-таргеты в норме, сортировка по-арабски), испанские законы
(кириллица 0, словари на месте), og:image → ru, mini у новых статей. `run.py publish` —
первый выпуск по новой дисциплине, осознанный.

**Догон:** 60 полных статей за 17–24 июля, ошибок 0, валидация переводов гоняла ретраи
(кириллица, потерянные числа) — работает.

**Не сделал.** День 21-е: отбор вернул 0 из 75 кандидатов без ошибки — похоже на
молчаливый откат в select_best, разобрать. Русский <title> у непереведённых свежих
статей (случай №24 QA) — не блокер, тело заглушка, в лентах их нет.

**Ждёт.** Ветка fix-release-blockers образователя (тема 15, термодинамика, frontier) —
сторож стоит, солью вторым заходом.

---

## 2026-07-29 · Упражнение: сам прошёл цикл статьи по промптам, сравнил с DeepSeek

**Сделал.** Взял статью 2607.14450 (широкие двойные и MOND), у которой сохранены обе
версии — до шлифовки и после. Прочитал вход (popular), промпты generate-simple и
refine-simple, прогнал текст глазами модели и сравнил с фактическим выводом DeepSeek.

**Находки, по убыванию важности.**

1. **Промпт generate-simple содержал ДВЕ несовместимые схемы ответа** — старую (с полями
   formulas/main_tag) и новую (с mini), склеенные при доработке 27-го. Модель следует
   последней, но это лотерея. Слил в одну схему, вход честно назван Popular (в шапке
   стояло «из Advanced-версии»). Плюс ссылка на несуществующий glossary → заменена
   правилом скобок.
2. **Шлифовка в текущем виде ВРЕДИЛА simple**, и вот механизм: п.2 промпта приглашал
   «сделать метафору неожиданнее» → модель меняла образ (канат → эластичный жгут),
   расходясь с popular той же статьи; п.5 «сократи, ~150 слов» → сжатие до плотности
   advanced (в отшлифованном тексте ПОЯВИЛИСЬ металличность, эволюционная стадия,
   численные симуляции — которых в simple-версии не было); п.6 «добавь удивительную
   деталь» → прямое приглашение дописать факт, конфликт с правилом добросовестности.
   Все три места переписаны: метафору не менять (общая с popular), шлифовать язык
   а не плотность, ничего не добавлять от себя.
3. **Что DeepSeek делает хуже меня** (то есть куда смотреть контент-менеджеру): открывает
   popular с «Представьте двух фигуристов» — запрет уже закрыт промпт-инженером 29-го;
   клише «С детства нас учили»; деталь-якорь не переведена в бытовое сравнение («в сотни
   раз слабее земных» повисает абстракцией); кольцо (возврат к образу в финале) чаще
   отсутствует. Что делает хорошо: врезки-callout, разметка тегов, структура абзацев.
4. **Мёртвый код:** gen_llm.generate_simple (стр. 336) больше не вызывается — живой путь
   generate_simple_mini. Прибрать при случае.

**Вердикт по шлифовке.** Нужна, но не в прежнем виде. Стиль она правила хорошо (клише
убирала), а суть портила. С новым промптом — прогнать пробную партию в 10 статей
и отдать контент-менеджеру на сравнение до/после; включать в конфиг только после его
вердикта. Расход на шлифовку — два лишних вызова на статью, при подтверждённом качестве
это дешёвая прибавка.

**Ветка.** Правки промптов в qa-release-check (текущее дерево), закоммичены.

---
