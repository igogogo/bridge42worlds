# Волна 17 августа · после второго аудита: чистка перед инвестицией

Контекст один на всех: пришёл второй внешний аудит (`АУДИТ-2026-08-16.md`, читать
целиком — он честный и по делу). Вердикт: инженерная половина обещаний закрыта
образцово, продуктовая — на четверть. Главное число: **вернувшихся читателей — ноль**.
Новая большая цель проекта: **20 возвращающихся читателей к концу сентября**.
Каждая задача этой волны работает на неё.

Ведущая уже закрыла P0 (краны: валидация тегов на записи, дотяжка и related-vec в
плане фабрики, транзакционные заготовки) — коммит 48cf285a740. Не дублировать.

---

## ML

1. **Аналитика, фаза 2 — эмбеддинги вместо TF-IDF.** Вектора всех статей уже есть
   (bge-m3, экспорт `embeddings_export.py`). UMAP + HDBSCAN вместо TF-IDF+t-SNE.
   Аудит: мегакластер #21 — 793 статьи (16%), четыре «бульона» с одинаковыми топами,
   кап k=24 — «на 10 000 статей вырастут». Это самый большой скачок качества карты
   на вложенный час.
2. **Фиксация координат по паттерну «мира»**: новые статьи подсаживаются в
   существующую проекцию, полная переукладка — редкая «пересъёмка». Сейчас t-SNE
   переукладывает облако каждый день, читатель через неделю не узнаёт карту. Для
   возвращающегося читателя узнаваемость карты — не косметика, а суть.
3. **Склейка синонимов тегов на билде** (находка 4): «реионизация ⟷ реионизация»
   (reionization/cosmic_reionization), «активное ядро · активное ядро». Пары ищутся
   вектором (косинус + пересечение пула статей), склейка — таблицей соответствий,
   на входе билда аналитики и графа.

## Разработчик

1. **`--interpret` в фабрику + кеш трактовки кластеров** (находка 1 аудита, срочно:
   следующий пуш увезёт «#21» вместо имён кластеров в прод). `tools/factory.py:427`
   зовёт `run.py analytics` без `--interpret` вопреки докстрингу `run.py:792`.
   Кеш — по сигнатуре кластера (топ-теги): совпала сигнатура — берём готовое имя,
   DeepSeek не дёргаем. Иначе ежедневная трактовка съест бюджет.
2. **fr-словари в js/analytics.js** (находка 2): L/FLY/HEAT/V/CTL без fr —
   французская страница с английскими вкладками.
3. **Строка LANG** (находка 5): `js/analytics.js:8-19` — индекс `[LANG]` до
   объявления `var LANG` (хойстинг → undefined), CTL-кнопки по-английски на всех
   языках. Починка — перенос одной строки, начать с неё.
4. **Регистр в словаре тегов** (находка 3): «дНК», «lIGO», «рНК», «мРТ». Найти,
   кто портит регистр (похоже на lower() первой буквы при нормализации), починить
   причину и прогнать чистку по словарю.

## DevOps

1. **Смок-тест в pre-commit** (обещание (г) аудита): лёгкий — `node --check` по
   изменённым js, `py_compile` по изменённым py, счётчик карточек через
   `page_watch.py` НЕ сюда (он после выкладки). Хук должен работать за секунды.
2. **Чистка git-истории от ключей** (обещание (к), 11 дней на доске без коммитов):
   либо сделать (git filter-repo, координация с ведущей — история перепишется,
   всем ролям пересоздать worktree), либо честно снять с доски с новым сроком и
   обоснованием. Зомби-строка хуже честного переноса.
3. **Лимит Cloudflare**: замерить эффект правки «не переписывать неизменённое»
   (сравнить запросы/сутки 16-го и 18-го). Если всё ещё у порога 100k — готовить
   переход на Workers Paid $5/мес (владелец согласен).

## Стратег (секретарь совета)

1. **Вернуть потерянные 6 вопросов первого заседания в повестку 23.08** — включая
   монетизацию, вопрос №1 совета. Файл 16.08 был создан из пула, перенос не
   выполнен — аудит прав, это потеря содержания.
2. **Второй держатель доступов** — вопросом в ту же повестку (обещание (в)).
3. **Повестка 23.08 целиком на английском** (решение владельца 15.08), инструмент
   `tools/agenda_to_en.py` готов.
4. **Кимбл и кавычки цитат** (обещание (ж)): ошибка на месте, 35 учёных с
   синтезированными цитатами. Правило: цитата без источника не публикуется как
   цитата — переписать как пересказ без кавычек.

## Правила волны

- Ветка на задачу от origin/main, писать только в своей зоне.
- lang/**, индексы, графы руками не коммитить — пересборку делает ведущая.
- Отчёт коммитом в свою ветку + строка в ДОСКА.md своей папки.
- Деньги: DeepSeek подорожал вдвое (пик по UTC 01:00-04:00 и 06:00-10:00 — по
  местному 04:00-07:00 и 09:00-13:00). Массовые прогоны — только вне пика.
- `output_guard` подключает ведущая (этика — не делегируется).
