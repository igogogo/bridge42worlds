# Контекст для новой сессии архитектора · 2026-07-31 ~06:15

Сжатая передача от ведущей ночной сессии. Прочти это, потом `задачи/отчёты/ведущий.md`
(две верхних записи), потом `задачи/ДОСКА.md`. Правила — `ПРАВИЛА-РАБОТЫ.md` (два новых:
«чини тип, а не место» и «общие файлы только UTF-8»).

## Состояние прода — здоровое, всё опубликовано

- Полная пересборка 41 696 страниц + публикация прошли (02:04). Французский собран
  ЦЕЛИКОМ: laws/graph/analytics/feed/latest — всё 200 (три блокера QA закрыты).
- Русских заголовков на en/es/ar было 91 по архиву — осталось 3 (одна упрямая статья
  2607.23140v1, её валят латех и маркеры, не числа).
- День 2026-07-30: **25 экспресс-статей со страницами на проде** (решение владельца:
  полные ежедневно НЕ делаем, экспресс по 20-25 в день). Доли: astro 6, cond-mat 4,
  quant 3, q-bio 2, gr-qc 2… математики 0 — спросить владельца, дать ли ей слот.
- 4 папки-обломка в lang/ru/archive/2026-07-30/ без data.json (упали до контента,
  не оплачены) — можно просто удалить.

## Решения владельца за утро (все уже в деле)

1. **Экспресс каждый день, полных потоком нет.** tools/daily.cmd обновлён (25 статей,
   упор на естественные науки, math/ML в меньшинстве). Задача планировщика b42_daily
   существует и Ready.
2. **Охота за бриллиантами** — задача ML поставлена (задачи/ml.md): сначала записка
   со способами и меркой честности, не код.
3. **«Чини тип, а не место»** — правило в ПРАВИЛА-РАБОТЫ.md. Владелец хочет отдельную
   сквозную задачу: унификация подписей (закон/автор/…), формы кнопок, ориентация на
   мобильном — по всему сайту. НЕ РОЗДАНА — включить в спринт.
4. **Ритм спринта волнами**: закончить блок до результата → рассылка → все пишут
   вопросы/отчёты в файлы → разбор разом, когда все закончили. Новый спринт собирает
   архитектор (эта новая сессия).
5. **Оркестратор**: Pentaho/Airflow пока НЕ ставим (мой разбор владельцу дан: Pentaho
   не про нас, Airflow дорог для двух прогонов). Вместо этого — журнал прогонов +
   алерт в бот. Порог для Airflow: «третий регулярный прогон с зависимостями».
6. **Обновление дампа Kaggle**: проверено API — снапшот от 2026-07-25, 5.1 ГБ, CC0;
   metadata отдаётся без ключа, download требует kaggle.json (сейчас кредов НЕТ —
   попросить владельца завести токен). План: tools/update_arxiv_dump.py = скачивание
   по API + существующий arxiv_bulk_chunk.py (он атомарный). «В облаке» пока не надо:
   Worker 5 ГБ не переварит; на дашборд — строку «дамп от N, чанки до M».

## Незакрытые ветки команды

- **dash-coverage-1 ГОТОВА** (новичок-дашборд, 2 коммита): отчёт о причине пустых чисел
  покрытия. Слить, проверить, пересобирать не нужно, но corpus_stats будет в
  DERIVED_ASSETS (решение в задачи/дашборд.md). Новичок влился, промт ему выдан.
- Сторож веток (Monitor b2mteidmz) — перезапустить в новой сессии, если нужен.

## Главный техдолг блока «воронка» (мой недоделанный кусок)

**Реюз экспресса при апгрейде НЕ работает** — доказано обследованием (детальный план
с файлами:строками — в task-output wf_54e05560-001, копия выводов ниже):
- regenerate_article СНАЧАЛА удаляет папку статьи (с оплаченным api/express-ru.json
  и обложкой), потом гонит полный каскад; старый data.json не читается нигде.
- mini полной генерится заново вместо реюза короткого текста экспресса; переводы
  переоплачиваются; express=false затирается.
- Генерации на одном языке НЕТ (LANGUAGES жёстко из config) — «полная на языке
  запроса» требует параметра only_langs в build_article (targets, строка ~3377).
- План правки (проверен адверсарно): читать старый data.json в начале build_article
  при force; реюзить mini/переводы/abstract; не удалять папку при апгрейде; беречь
  FLUX-обложку по полю image_model (само поле тоже теряется — добавить в payload
  save_data_json ~1186-1202).

## Починено за ночь (не трогать повторно)

Вес первого визита (тяжёлое в простой, B42Refs, индекс только где список), 304/ETag
и immutable-без-версии в worker.js (выкладка за DevOps — задачи/devops.md пункты 0
и 0б!), og-карточки везде, тултипы (позиционирование + бейджи экспресса на таче),
сторожа: silent_watch, fix_encoding (в pre-commit), stamp_assets и corpus-ours —
в DERIVED_ASSETS хвоста run.py; замок второй сборки теперь ОСТАНАВЛИВАЕТ; PDF-таймаут
и суррогаты в references больше не роняют статьи; NameError записи страниц починен
(терял 17 оплаченных статей — вернул бесплатной пересборкой).

## Быстрые команды

- Статус: `python run.py stats` · `git log --oneline -20` · доска
- Экспресс дня руками: см. строку в tools/daily.cmd
- Точечная пересборка: `python run.py html --only <дата>`
- Сторожа: `python tools/silent_watch.py` · `python tools/fix_encoding.py --check`
