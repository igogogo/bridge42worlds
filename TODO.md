# TODO — bridge42worlds (мастер-список)

## 🟡 2026-07-19 — ДОДЕЛАТЬ: 5 новых законов (данные готовы, перевод/страницы/коммит — нет)
Добавлены в `lang/ru/data/laws-list.json` + описаны + прошлифованы (`law_describe.py --ids
"rutherford_scattering,blochs_theorem,schwinger_effect,curies_law,hoyle_process" --refine`),
закрывают часть sci_no_law-пробела (Резерфорд/Блох/Швингер/Кюри/Хойл). **НЕ ДОДЕЛАНО**:
`reference_translate.py` дважды подвисал на таймаутах DeepSeek API (похоже на нестабильность/пик
у провайдера в моменте, не баг кода) — RU-контент точно готов, en/es/ar — под вопросом (es
дошёл до "уже полный (107)", en/ar не подтверждены). Дальше: перепроверить
`lang/{en,es,ar}/data/laws.json` на все 5 id, доперевести если нужно, точечно
`generate.generate_law_page(id, lang)` на все 4 языка, `generate.build_knowledge_graph_data()`,
коммит+пуш. Не запускать `run.py html` целиком ради этого — точечно быстрее.

## 🔴 2026-07-18 — ПРОДОЛЖИТЬ В НОВОМ ОКНЕ (актуально прямо сейчас, читать первым)

**⛔ КРИТИЧЕСКИЙ БАГ, НАЙДЕН И ОБОЙДЁН (но НЕ ПОЧИНЕН в коде) — НЕ ЗАПУСКАТЬ `law_describe.py`/
`tag_describe.py` НИ В КАКОМ РЕЖИМЕ (даже `--only`, даже обычный top-up без флагов) ПОКА НЕ
ПОЧИНЕНО:**
Финальный цикл в `main()` обоих скриптов перестраивает `graph["graph"][id]["scientists"]`
БЕЗУСЛОВНО для КАЖДОГО тега/закона из реестра (не только для описываемых в этом запуске!) —
берёт значение из `ru.get(id,{}).get("scientists")` (узкий список из `tags.json`/`laws.json`,
обычно 1-3 «настоящих первооткрывателя»), полностью игнорируя то, что реально накопилось в
графе через `run.py evolve` (там теги обрастают ДЕСЯТКАМИ учёных, напр. у `black_hole` было 59).
`tag_describe.py` использует `desc.get("scientists", prev.get(...))` — выглядит как safe-fallback,
но НЕ работает: `.get()` берёт default ТОЛЬКО если ключа нет, а ключ `"scientists"` есть у каждого
уже описанного тега (даже пустой `[]`) — значит prev/evolve-обогащение теряется всегда, когда
запускается любой прогон этих скриптов, не только `--only`.
**Симптом, который это уже вызвало сегодня**: мой тестовый `tag_describe.py --only higgs_boson
--refine` (и `law_describe.py --only law_of_universal_gravitation --refine`) обрушили
`data/tags-graph.json` с 1533 связей тег↔учёный до 259 (закон-граф пострадал меньше). **Данные
восстановлены** командой `git checkout HEAD -- data/tags-graph.json data/laws-graph.json
data/knowledge-graph.json` (в HEAD была последняя добрая версия) — если открываешь новое окно и
видишь diff на этих 3 файлах, СНАЧАЛА проверь `git diff --stat` на предмет обвала числа
`scientists`-ссылок (см. метод проверки в чате/памяти), не коммить не глядя.
**Правильный фикс (не сделан)**: цикл перестройки графа должен либо (а) трогать ТОЛЬКО те id, что
реально в `to_describe` в этом запуске, оставляя остальные узлы графа как есть (`prev` целиком,
не мержить поля по одному), либо (б) объединять множества (`set(prev_scientists) | set(desc_scientists)`)
вместо перезаписи. См. `law_describe.py` (~строка 210-220 в `main()`) и `tag_describe.py` (~строка
222-234) — сейчас это простой словарь-присвоение `graph["graph"][lid] = {...}` внутри `for x in
laws_in:`/`for t in (active+edu):`, без учёта `to_describe`/`args.only`.

**В процессе / сделано сегодня:**
- [x] Сайдбар статьи (`.article-side`) поднят вровень с заголовком — `<h1>`+meta перенесены
  ВНУТРЬ `.article-main` (было снаружи `.article-wrapper`, из-за чего колонка стартовала ниже).
  `templates/article.html`.
- [x] Страницы тега/учёного (`templates/tag.html`, `templates/scientist.html`) переведены на тот
  же паттерн `.entity-wrapper` + `.article-side` (справа), что уже был у закона (`law.html`) —
  related-теги/законы/учёные вынесены из "подвала" в правую колонку через новый
  `entity_side_html` (`generate.py`: `generate_tag_page`/`generate_scientist_page`, переиспользует
  существующий `side_chip_group()`). Добавлен `articles_label`/loc-ключ `"articles"` для нового
  заголовка секции со списком статей (заменяет старый неверный `related_label` над списком).
  Протестировано точечной генерацией (`generate_tag_page('higgs_boson','en')`,
  `generate_scientist_page(...)`, `generate_law_page(...)`) — без ошибок substitute.
  Визуально проверено в браузере на higgs_boson: колонка сайдбара `position:fixed;top:245px`
  (тот же CSS-паттерн, что у article/law) — выравнивание с заголовком тега неидеальное
  (мешает AI-обложка перед `<h1>`, сдвигающая заголовок вниз), но это ТОТ ЖЕ механизм, что уже
  был принят для law.html — не регрессия, а известное ограничение паттерна.
  **Полный `run.py html` запущен в фоне** (`html-regen-sidebar-layout.log`) — проверить, что
  завершился (`grep EXIT: html-regen-sidebar-layout.log`), и закоммитить:
  `generate.py`, `templates/article.html`, `templates/tag.html`, `templates/scientist.html`
  + весь диффнутый `lang/**/*.html` от регена.
  Также несвязанные, но ранее не закоммиченные правки в этом же рабочем дереве: `--only` флаг
  в `law_describe.py`/`tag_describe.py` (точечная переописание одного закона/тега без `--force`).

**НАЙДЕНО, НЕ СДЕЛАНО — теги отстают от законов по бэкфиллу mini/practical_application:**
- Юзер верно заметил: теги переделывались НЕ так системно, как законы. Текущие цифры
  (`lang/ru/data/{tags,laws}.json`, проверено 2026-07-18):
  **tags.json: 20/196 без `mini`/`practical_application`. laws.json: всего 3/102.**
  Законы почти все забэкфиллены (частью через целевой `--only`-тест, частью — побочным эффектом
  прогона `reference_translate.py`, который заодно доливал недостающие поля другим законам).
  Тегам такого широкого прохода не было — сделан только один точечный тест (`higgs_boson`).
  **Список из 20 непочиненных тегов (id из lang/ru/data/tags.json)**:
  `hubble_space_telescope, gravitational_waves, large_hadron_collider, wave_particle_duality,
  quantum_computer, nuclear_fusion, standard_model, time_dilation, wormhole, spacetime_curvature,
  quantum_superposition, quantum_teleportation, cosmic_dust, fast_radio_burst, kilonova, magnetar,
  active_galactic_nuclei, tidal_disruption_event, globular_cluster, axion`
  **План на новое окно**: догенерировать эти 20 через `tag_describe.py` (без `--only`, т.к. их
  много — обычный insufficient-mini fallback путь, или добавить `--only-missing-mini` флаг),
  затем `reference_translate.py` для en/es/ar, затем точечный `generate_tag_page(...)` на все 4
  языка для каждого, БЕЗ полного дорогого `run.py html` пока не накопится больше правок.
  Также стоит перепроверить `higgs_boson.scientists: []` (пусто, хотя Peter Higgs есть в реестре
  148 учёных) — не выяснено, баг промта или реальное отсутствие связи в данных.

## 🌅 2026-07-16 → ЗАВТРА (2026-07-17) — план финиша, ничего ниже пока НЕ делать без явного добра

**Что сделано сегодня (2026-07-16), для контекста:**
- AR-перевод: найден и починен silent-fallback баг в `translate_scipop`/`translate_captions` (gen_llm.py,
  retries+лог `translation-failures.log`); полный корпус-бэкфилл (`ar_backfill.py`, 902/903 успешно,
  1 честная ошибка — `2508.01648`, errno 22, см. `ar-backfill-failed.json`).
- Обложки: quality-preset (FLUX-2-pro) обложки для 18 отобранных «топовых» статей, старые PDF-обложки
  сохранены как `ai-pdf-original.jpg`, `cover_image_model`/`cover_image_prompt` в data.json.
- Education/theory (17 статей × 4 языка = 68 файлов):
  - Починен `alt=`/`aria-label=`/`title=` — были на русском во ВСЕХ 17 арабских статьях (шапка ★
    «Избранное», TOC «Содержание», все подписи анимаций-картинок). **Но только для AR** — независимая
    проверка (см. ниже) подтвердила, что тот же баг есть и в ES (все 17 статей, 265 вхождений) и,
    скорее всего, в EN — их НЕ трогали.
  - Починен видимый текст ВНУТРИ SVG-анимаций (не только alt/aria, а `<text>` прямо в разметке + JS-строки
    типа кнопок/юнитов) — тоже только AR, 14/17 файлов, 79+41 замена.
  - Верстка: `.hero-diagram` не имел `max-width` (был 820px, шире чем `.theory-img-pair`/текст 680px) —
    рваный левый край. Починено во ВСЕХ 68 файлах (все 4 языка) — `max-width:680px`.
  - Тайминг анимаций: почти весь сайт уже на едином продуманном 6-секундном цикле (комментарии в коде
    "держим темп сайта"). Нашёл и починил 1 реальный «рваный» ролик — компас в ampere_faraday (снап
    0.15→0.35, все 4 языка). Нашёл и починил 1 случай «мелкого» использования пространства — труба
    Бернулли использовала только треть высоты кадра, увеличена вдвое (все 4 языка).
  - НЕ доделано: аналогичный аудит «мало места» для остальных ~15 диаграмм (скриншот-инструмент
    зависал в конце сессии, чинить вслепую не стал) — кандидаты по грубой эвристике: coulomb_ohm,
    gravity_kepler, vertical_wheel.

**Независимая проверка (от юзера, 2026-07-16 вечер) — ПОДТВЕРДИЛА и РАСШИРИЛА масштаб находки:**
- Баг «диаграммы Claude пишет по-русски, копируются в локали как есть» — **системный**, не пара
  случайных статей: 17/17 испанских theory-статей содержат русский текст (265 вхождений general +
  44 русских `alt=`). Арабская — то же самое (уже частично почищено сегодня, см. выше, но SVG-текст
  внутри диаграмм на AR был найден и мной независимо позже в этой же сессии).
- **EN не проверялся явно, но по логике пайплайна («копируется как есть») почти наверняка тоже затронут.**
- Вёрстка в остальном ОК: нет горизонтального переполнения, SVG в RTL не зеркалится (и это правильно —
  графики/схемы зеркалить нельзя), кремовый фон education — by design.
- Рекомендация ревьюера: в генераторе theory нужен отдельный проход перевода SVG `<text>` + `alt=`
  (сейчас переводится только видимая проза) — чинит разом во всех локалях на будущее, а не только патчит
  уже написанные 17 статей. Ревьюер предлагал показать точное место в generate.py/gen_*.py — не смотрели.
- Мелкие находки от ревьюера (не проверены, не тронуты):
  - Шапка на theory-страницах — тот же 2-строчный перегруз (лого-сирота сверху), общий баг для всех
    страниц сайта, не specific to education.
  - Меню на theory-страницах — по-английски + `authors` ссылается на `/lang/ru/authors/` (не
    локализовано под текущий язык страницы).
  - Слайдер уровней (mini/simple/advanced/popular) на theory-страницах — бутафория, у education-статей
    нет этих регистров, слайдер ничего не переключает по существу.
  - `vertical_wheel` — кандидат в «битую» статью (у неё было больше всего русских `alt=` — 6 шт,
    ПОЧИНЕНО сегодня для AR) — но ОТДЕЛЬНО от этого: интро в испанском индексе — сухой arXiv-abstract
    («La configuración experimental hace girar la rueda…»), выбивается стилистически из кураторского
    нарратива остальных 16 статей («En 1687…»). Похоже, попала в подборку по ошибке — нужно решить,
    оставлять/переписывать/убирать.

**Вторая и третья независимая проверка (от юзера, 2026-07-16 поздний вечер) — сайт-вайд аудит вёрстки/шапки/RTL,
только чтение (CSS+исходники) + живой замер на `/lang/es/tags/` через JS, БЕЗ правок:**

- **Шапка (`.top-bar`) — подтверждено живыми цифрами, не только по коду.** На `/lang/es/tags/`, десктоп 1280px:
  высота 83px, ДВЕ строки — лого один на первой строке (y 24-53, пусто справа), меню+слайдер версий целиком
  провалились на вторую строку (y 61-91). Причина: контенту нужно ~728px (лого 121 + меню 358 + слайдер 195 +
  гэпы), а `.top-bar` зажат в `max-width:680px` (сужен в этой же сессии раньше сегодня/ранее). CSS сам себя
  выдаёт — цепочка комментариев «не умещается… сжато ещё раз 10→6px… about убрать» — латание симптома, не причины.
  Горизонтального переполнения НЕТ (`scrollWidth 1265<1280`), только перенос строк. Побочка: `sticky`-шапка на
  83px + JS-логика scroll-offset якорей (`top<100`) — при 2-3-строчной шапке клик на `#feedback` может уводить
  контент под шапку. **Рекомендация ревьюера**: свернуть меню в 3-4 главных пункта + «…»/бургер или иконки,
  либо не жать общую ширину шапки до 680px.
- **Поиск на главной — тяжёлый, 2 строки** (`.search-row` + `.search-hint` с `# @ !` + `.express-filter`).
  Рекомендация: свернуть в 🔍-иконку по образцу уже существующего паттерна календаря (`.cal-bar` → выпадашка).
- **RTL (арабский) — реальные баги логики, не только косметика:**
  - Кнопка избранного `☆ избранное`, тултипы реакций (`title="Нравится"/"Не нравится"/"Супер!"/"В избранное"`),
    бейдж `⚡ экспресс`+его `title` — всё по-русски прямо на арабской странице.
  - `title="Избранное"` на ★ по-русски — **это ТА ЖЕ строка, что я сегодня уже чинил в theory-шапке** (была
    зашита в общем HEADER_NAV в generate.py) — но чинил только для 68 theory-файлов; на ОСТАЛЬНЫХ типах
    страниц (статья/тег/закон/учёный/архив/граф — тысячи файлов на ar И es) этот же хардкод не тронут.
  - Подписи РЁБЕР мини-графа — по-русски (`тег↔закон`, `тег↔учёный`...), а подписи УЗЛОВ рядом переведены
    (`وسوم/قوانين/علماء`) — явный частичный пропуск в шаблоне/JS.
  - Слайдер версий НЕ зеркалится под RTL — `.vs-fill`/`.vs-thumb`/`.vs-dot` всегда позиционируются через
    `left:%`, прогрессия «просто→сложно» рисуется слева-направо независимо от направления письма.
  - Стрелка «следующая статья» не зеркалится — текст `المقال التالي →` (стрелка вправо), хотя в RTL «дальше»
    визуально должно быть влево; мозаичные стрелки уже флипаются (`scaleX(-1)`), текстовые — нет; вдобавок
    `.next-top{justify-content:flex-end}` в RTL кладёt кнопку в левый край — вместе с правой стрелкой выходит
    разнонаправленно.
  - Микс RTL+LTR в одной flex-строке фильтров мини-графа (арабские `وسوم/علماء` + русские `тег↔закон` в одном
    `direction:rtl` `flex-wrap`) — визуально путается порядок/выравнивание.
- **Кросс-язык утечка ссылок — подтверждена на ДВУХ языках (ar И es)**: `authors`/`theory` (и на es — ещё и
  служебная ссылка-аудит) ведут на `/lang/ru/...` независимо от текущего языка страницы. Не ломает вёрстку,
  но рвёт языковую целостность интерфейса. Нужно решить — баг или намеренное упрощение (страница авторов
  вообще не локализуется по дизайну)?
- **НОВОЕ, отдельное от переводов**: на публичной `/lang/es/tags/` торчит служебная иконка `🖼 Аудит картинок`
  (`shown:true`, живой JS-замер), ведёт на `/lang/ru/tags/audit.html` — внутренний инструмент проверки
  картинок утёк в прод. Нужно спрятать полностью (не просто перевести/перелинковать).
- **Меню целиком по-английски на всех языках** (main/tags/laws/scientists/authors/graph/theory/about,
  переключатель List/Graph) — это **НЕ новая находка**, это ровно то, что уже висит в Трек 4 #20
  («Локализация → единый ui-strings.json + loc_for(section,lang); осталось: title реакций, ★ Избранное,
  ~9 loc-словарей, nav») — просто теперь есть больше конкретики (список конкретных строк) для этого пункта.
- **Что уже хорошо на es (для баланса, не только ругать)**: теги отсортированы по локализованному имени
  (не по латинскому id), облако тегов ровное колонками, счётчики статей показаны только у крупных тегов.

**СДЕЛАНО 2026-07-17 (утро/день, до пика DeepSeek и во время него):**
- [x] SVG-текст/alt/title-очистка theory-статей — теперь и на **ES и EN** (34 файла), тем же методом,
  что вчера для AR. 0 остаточной кириллицы в обоих языках (проверено регэкспом после прогона).
- [x] AR-бэкфилл-ошибка `2508.01648` устранена — `lang/ar/articles-index.json` (popular-тир) показывал
  сырой английский arXiv-заголовок вместо арабской express-locked заглушки (у RU/EN/ES там правильно
  локализованная заглушка); точечно исправлено под тот же текст, что уже верно стоит в advanced-индексе.
- [x] Служебная ссылка `🖼 Аудит картинок` убрана из `templates/tags-cloud.html`/`laws-cloud.html`
  (публичная кнопка), сама `audit.html` осталась на диске для прямого доступа по URL. Облака тегов/
  законов перегенерены на всех 4 языках точечными функциями (без полного regen).
- [x] **Хардкод `title="Избранное"` + тултипы реакций + бейдж «экспресс» + бейдж «отшлифовано» — починено
  сайт-вайд** через JS (`js/search.js`), не только для theory: расширен уже существовавший `UI_STRINGS`
  словарь (там уже были готовые переводы `express`/`expressTip` для карточек ленты, просто не были
  подключены к серверно-рендеренному бейджу на самой странице статьи) новыми ключами `favTitle`/`like`/
  `dislike`/`superlike`/`refineTip` на всех 6 языках (ru/en/es/zh/fr/ar) + функция `localizeStaticUI()`
  на DOMContentLoaded переписывает `title`/`textContent` на лету. **Работает мгновенно на уже
  существующих страницах, без полного regen** (проверено вживую на `/lang/ar/archive/2025-08-03/
  2508.01648/simple.html` — бейдж «⚡ سريع», тултипы лайков на арабском, звезда «المفضلة», всё живьём
  через getComputedStyle/JS-замер). Пересекалось с Трек 4 #20 — сделано как часть той же работы.
- [x] Кросс-язык утечка `theory`-ссылки — была ЖЁСТКО завязана на `$authors_lang` (=DEFAULT_LANG=ru)
  во ВСЕХ 12 шаблонах + архивной f-string в generate.py, ровно как и `authors`-ссылка. Но, в отличие
  от `authors` (юзеры не переведены, там ru-канонизация — намеренно), theory-статьи ПОЛНОСТЬЮ
  локализованы на все 4 языка (см. вчерашнюю/сегодняшнюю работу) — значит `$authors_lang` для theory
  был просто скопирован по шаблону от `authors`-ссылки без переосмысления, когда theory обзавёлся
  переводом. Заменено на `$lang` везде (13 мест) — требует полного `run.py html` для применения к уже
  существующим страницам (запущен фоном, `html-regen-20260717.log`).
- [ ] `authors`-ссылка на `/lang/ru/...` — оставлена как есть (подтверждено намеренной: авторы не
  переводятся, единая ru-страница — осознанное решение, не баг).
- [x] **Подписи РЁБЕР мини-графа на RTL/всех языках — починено.** `GRAPH_LABELS` (generate.py) уже имел
  переводы для узлов (tags/laws/scientists), но не для рёбер — добавлены ключи `edge_tag_law`/
  `edge_tag_sci`/`edge_law_sci`/`edge_tag_tag`/`edge_law_law`/`edge_sci_sci`/`edge_law_influence`/
  `preset_core`/`preset_all` на всех 6 языках (**заодно добавлена целиком отсутствовавшая ветка "es"** —
  граф-эксплорер на испанском раньше падал в английский фоллбэк целиком, не только по рёбрам).
  `mini_graph_filters_html()` (мини-графы на статьях/тег/закон/учёный) и `templates/graph-explorer.html`
  (большой граф-эксплорер, 8 строк — чекбоксы + пресет-кнопки) переведены на использование `loc[...]`
  вместо хардкода. Протестирован офлайн-регеном на всех 4 языках без ошибок. Требует `run.py html`
  для применения к уже существующим страницам (тот же фоновый прогон, что и для theory-ссылки).
- [x] **Найден и починен реальный краш-баг**: `regenerate_all_html()` уронило ВЕСЬ `run.py html`
  (1263+ статей) на одном транзиентном `OSError [Errno 22]` при записи ОДНОГО файла
  (`lang\en\archive\2026-01-22\2601.16015\index.html`) — та же природа ошибки, что вчера у
  AR-бэкфилла (`2508.01648`), но там был try/except на уровне статьи (ThreadPoolExecutor), а тут
  голый цикл без защиты. Добавлен `_write_text_retry()` (3 попытки, 0.3с бэкофф) на оба места записи
  HTML в `regenerate_all_html()` — транзиентный сбой ФС/антивируса больше не топит весь прогон.

- [x] **Аудит «мало пространства» hero-диаграмм — завершён, пройдены все 17 тем.** Скриншот-инструмент
  ожил (не воспроизвёл вчерашнее зависание). Живьём проверены визуально все hero-диаграммы: реально
  «мелкими» оказались только **coulomb_ohm** (кружки зарядов увеличены 22px/18px→32px/28px радиус,
  шрифт знака «+» пропорционально, все 4 языка, зазор при анимации притяжения проверен математически —
  не пересекаются) — остальные 14 (ampere_faraday/blackbody_radiation/conservation_laws/
  doppler_effect/elasticity_archimedes/eulers_formula/gravity_kepler/ideal_gas/least_action_noether/
  maxwells_equations/schrodinger_equation/snell_bragg/thermodynamics/vertical_wheel) уже используют
  кадр нормально — вчерашняя грубая координатная эвристика давала ложные тревоги (не учитывала
  реальную геометрию `<ellipse>`/`<rect>`, только `cx`/`cy` точечных элементов). doppler_effect
  оказался ложной тревогой другого рода — анимация волновых колец реальна, просто не успела вырасти
  к моменту снятия скриншота сразу после загрузки страницы.

**Решения юзера (2026-07-17):**
- [x] `vertical_wheel` — **оставить как есть**, не трогать. Технический тон нормален для этой статьи.
- [x] Подстраховка от непереведённых SVG в будущих theory-статьях — **чек-лист себе** (не автоскрипт),
  сохранён в память `project-education-articles.md` (5 пунктов: видимый текст → SVG `<text>` → JS-строки
  → alt/aria/title → финальный regex-скан на кириллицу вне `<style>`/комментариев).
- [x] **Шапка сайт-вайд — свёрнута в «…».** Через JS (`collapseNavOverflow()` в search.js), не
  правкой всех 13 шаблонов — переносит tags/laws/scientists/authors/graph в выпадашку, main/theory/★
  остаются на виду. Работает мгновенно на уже существующих страницах (не ждёт `run.py html`).
  Проверено вживую: LTR (`/lang/es/tags/`) — шапка одной строкой, выпадашка открывается/закрывается
  корректно; RTL (`/lang/ar/theory/newton_laws.html`) — порядок зеркалится сам (about ★ theory ⋯
  main), выпадашка открывается в правильную сторону.
- [x] **Стрелка «следующая статья» (верхняя кнопка) — починена.** `next_arrow` уже вычислялся
  правильно (← для RTL) в generate.py и уходил в нижнюю кнопку (в фидбэк-виджете), но верхняя кнопка
  (`templates/article.html`) хардкодила «→» напрямую, игнорируя переданное значение. Однострочный фикс
  — `$next_label →` → `$next_label $next_arrow`. `.next-top{justify-content:flex-end}` уже был
  direction-aware (флексбокс), доп. правки не нужны.
- [x] **`dir="$dir"` добавлен на `<html>`** — раньше вообще не было атрибута `dir` (только
  `html[lang=ar]{direction:rtl}` в CSS), из-за чего `[dir="rtl"]`-селекторы не на что было ловить.
  Новый хелпер `dir_for(lang)` в gen_base.py, подключён во все 14 `.substitute()` вызовов + архивную
  f-string в generate.py. Theory-статьи (68 файлов) уже имели `dir="rtl"` — не трогал.
  status.html (внутренний дашборд, всегда en) — не трогал, не нужно.
- [x] **Слайдер версий (`.vs-fill`/`.vs-thumb`/`.vs-dot`) зеркалится под RTL.** Точки — CSS-оверрайд
  (`html[dir="rtl"] .vs-dot:nth-child(N)`, просто переставлены проценты). Заливка — CSS-оверрайд
  (`right:0` вместо `left:0`, ширина не менялась). Бегунок — JS, зеркалю ПРОЦЕНТ (100-pct) вместо
  смены anchor+transform (проще и безопаснее — вся существующая логика центрирования переиспользуется
  как есть). Драг мышью/тачем — тоже зеркалится (`nearestIdx`). Стрелки ←→ на клавиатуре НЕ менял —
  оставил value-based поведение (Right всегда увеличивает сложность), как у нативных `<input
  type=range dir=rtl>` в браузерах. `run.py html` завершился успешно (`html-regen-rtl.log` — 20208
  страниц). **Визуально подтверждено** на `/lang/ar/tags/index.html` (перегенерённая, не theory):
  `html[dir="rtl"]` присутствует, `.vs-fill` реально анкерится справа (`computedRight:0px`), бегунок
  зеркалится (`thumbLeft:66.67%` вместо исходных 33.33%) — скриншот подтверждает, слайдер и шапка
  рендерятся без искажений.

**СДЕЛАНО 2026-07-17 (после пуша RTL/шапки) — свёрнут поиск на главной:**
- [x] Поиск на главной (поле+подсказка+фильтр экспресс) свёрнут в 🔍-кнопку рядом с 📅 —
  юзер выбрал этот вариант из 3 предложенных (объединить в 1 строку / поиск в иконку / просто
  переставить календарь). `.langs-row` теперь: языки слева, `.hdr-icons` (🔍+📅) справа —
  вместо одинокого календаря + отдельных 2 строк поиска ниже. `templates/index.html` (общая
  разметка index/favorites.html — один и тот же файл), CSS `.hdr-icons`/`.search-bar`/
  `.search-panel` (по образцу `.cal-bar`/`.calendar-panel`), JS `initSearchToggle()`
  (search.js) — открытие/закрытие независимо от загрузки searchIndex (в отличие от календаря).
  Точечный regen `generate_index_page()` на 4 языках (ru/en/es/ar), без полного `run.py html`.
- [x] **Побочный баг найден и починен**: обе выпадашки (🔍 и 📅) в RTL улетали за левый край
  экрана — `right:0` анкерился на `.hdr-icons`, который сам съехал влево при `direction:rtl`
  (флекс мирится). Это был скрытый баг и у ОДИНОКОГО календаря (никто не тестировал 📅 в RTL
  до сегодня) — теперь оба анкерятся `left:0` под `html[dir="rtl"]`.
- [x] **Второй баг**: обе кнопки-переключателя имели `e.stopPropagation()`, из-за чего клик по
  одной не долетал до `document`-слушателя другой — обе панели могли быть открыты одновременно.
  Убрано, добавлена симметричная логика взаимного закрытия + закрытие по клику вне обеих панелей.
- [x] Слайдер уровней на theory-страницах — проверено 2026-07-18 (юзер: "скрой"): на текущих
  theory-страницах его физически НЕТ ни в HTML, ни инжектится JS (top-bar theory-страниц не
  содержит `$version_toggle_html` вообще, в отличие от статей/тегов). Пункт был устаревшим,
  закрыт без правок — нечего было скрывать.

**СДЕЛАНО 2026-07-17 (граф статьи перегружен + учёные не показывались):**
- [x] **Реальный баг найден**: `article_graph_ids` в `gen_article_html()` собирался как
  `теги + законы + учёные`, обрезка `[:8]` — у статьи обычно 5-9 тегов, они одни съедали весь
  лимит ДО того, как в список попадал хоть один закон/учёный. Порядок сборки инвертирован
  (законы+учёные первыми, теги добора́ют остаток) — учёные теперь реально появляются в графе
  статьи (юзер: "кажется учёные вообще не отображаются" — подтвердилось).
- [x] **Дефолт графов сайт-вайд — только законы+учёные**, теги по чекбоксу вручную (юзер:
  "граф оказывается перегружен"). `mini_graph_filters_html(lang, None)` (статьи + все 3 облака
  тегов/законов/учёных) и хардкод-чекбоксы в `templates/graph-explorer.html` — оба default
  `{law, sci}` вместо `{tag, law, sci}`, ребро `law-sci` вместо всех 3 кросс-рёбер.
- [x] **Побочный баг в JS найден и починен**: центры мульти-графа статьи (`js/mini-graph.js`)
  были исключены из фильтра типов ВСЕГДА (задумано для одно-центровых страниц тег/закон/учёный —
  не прятать сам предмет страницы) — из-за этого тег-центры статьи оставались на графе, даже
  когда чекбокс «теги» выключен по умолчанию. Исключение сужено до `centers.length <= 1`
  (одно-центровый режим) — на статье (много центров) чекбокс теперь реально прячет тег-центры.
- [x] **Предупреждение про большие графы** (>100 узлов) — `js/force-graph.js` (общий движок,
  накрывает статьи/облака/эксплорер разом), текст на 6 языках, ссылается на уже существующую
  кнопку ⛶ (полноэкранный режим). Юзер: "формирование графа может занять время... рекомендуем
  полноэкранный режим". RTL (AR) не зеркалит плашку — как и сам ⛶, физически всегда в углу справа.
- [x] **AR title-баг — найдена настоящая причина, шире, чем вчерашний 1-статейный патч**: 5 старых
  экспресс-статей (express_tiers={mini,simple}) имели ПОЛНОСТЬЮ ОТСУТСТВУЮЩИЕ (не «неправильные» —
  именно отсутствующие) `data["popular"]["ar"]`/`data["advanced"]["ar"]` записи (возникло, когда
  статья была сгенерена ДО добавления AR как языка, а `ar_backfill.py` заполнил только реальные
  тиры (mini/simple), не тронул locked-заглушки) — `version_scipop()`'s фоллбэк на DEFAULT_LANG
  тихо подставлял РУССКИЙ текст в `<title>`/`og:title` арабских страниц. Бэкфилл: 5 статей ×
  2 тира патчены через `express_locked_scipop(simple_ar, "ar")`, HTML точечно перегенерен.
  Заодно найдены (но НЕ тронуты — это не тот же баг, просто косметика) 78 случаев старой
  LLM-переведённой (не через статический словарь) арабской заглушки с разной формулировкой
  («قيد التحضير» vs «قيد الإعداد» и т.п.) — все на арабском, не кросс-язык, не приоритет.
- [x] **Простой без формул vs Мини с формулами — разобрано, это НЕ баг**: промт Простого
  намеренно почти всегда пропускает формулы («формулы почти всегда лишние», ради доступности),
  Популярного — почти всегда даёт 1-2 (проверено на данных: 207/207 популярных vs 18/207
  простых). Юзер решил не трогать промты, просто переиспользовать: `version_scipop()`
  (gen_base.py) — Просто без своих формул наследует их у Популярного при чтении (не мутирует
  data.json, безопасно для мест, которые потом сохраняют его на диск). Работает и на СТАРЫХ
  статьях (не только новых) — фикс на чтении, не на генерации.
- [x] `config.json → article_workers`: 6 → 10 (юзер вспоминал, что хотели 10; было тихо снижено
  до 6 в какой-то момент). НЕ поднял сразу до предложенных юзером 20 — риск рейт-лимитов DeepSeek
  неизвестен, разумнее шаг за шагом (можно поднять ещё, если 10 пройдёт чисто).
- [x] **«Плавающий баг» смены тира при переходе между сущностями — НАЙДЕН И ПОЧИНЕН.**
  Не язык (протестировал отдельно — везде корректно, см. ниже), а именно тир (Просто/Популярно/
  Подробно/Мини). Корень: `js/search.js`'s версия-слайдер имеет 2 режима — `isLinkMode` (страница
  статьи, точки это `<a href>` на реальные simple.html/advanced.html/... — навигация без JS) и
  JS-режим (тег/закон/учёный/главная, точки это `<button>`, содержимое переключается через
  `.tag-ver` display). В `isLinkMode`-ветке `setActive()` НИКОГДА не вызывался — страница статьи
  красила своё начальное состояние по серверному `active`-классу, но НИ РАЗУ не писала текущий
  тир в `localStorage.b42_version`. Значит: читаешь статью в любом тире → кликаешь тег/закон-чип
  → JS-страница подхватывает `b42_version` из localStorage — а там осталось ЧТО УГОДНО с прошлой
  JS-страницы (или дефолтный `popular`, если ещё не трогал), НЕ тир, в котором только что читал
  статью. Фикс: `isLinkMode`-ветка теперь тоже пишет `currentVersion`/`localStorage.b42_version`
  при загрузке страницы статьи. Проверено живьём: `popular` в localStorage → открыл статью в
  Подробно (ES) → localStorage стал `advanced` → перешёл на связанный тег → тег открылся тоже в
  Подробно (было бы `popular` до фикса). JS-only, применяется мгновенно, `run.py html` не нужен.
  Язык отдельно протестирован тем же сценарием (тег↔закон↔учёный↔статья, ES) — `lang` в URL и
  `getLang()`/`getDefaultLang()` (фоллбэк `ru`, не `en`) везде корректны, бага не нашёл.

**СДЕЛАНО 2026-07-17 (locked-тиры + дешёвые обложки-фоллбэк):**
- [x] **Locked-тиры экспресс-статей (Popular/Advanced, когда реален только Mini+Simple) —
  редизайн.** Раньше `express_locked_scipop()` подменяла title/text/context на generic-заглушку
  «Полная версия готовится» — юзер-фидбек: "смущает, что название подменяется... лучше оставить
  как у простой, но сверху написать это простой вариант". Теперь: реальный контент (тот же, что
  у Simple) остаётся как есть, `gen_article_html()` рендерит locked-тиры через SIMPLE_LIKE-ветку
  независимо от номинальной версии, баннер `<p class="express-locked-banner">` prepend'ится
  сверху текста ("Showing the Simple version — Popular isn't ready yet..."). Новый
  `EXPRESS_LOCKED_BANNER` (6 языков) заменил старые `EXPRESS_LOCKED`/`EXPRESS_LOCKED_HINT`.
- [x] **Бэкфилл СУЩЕСТВУЮЩИХ статей** — код чинит только НОВУЮ генерацию (locked-scipop уже
  запечён в data.json на момент первой генерации). Нашёл масштаб: 1071 статья / 8567 locked-
  записей во всём корпусе имели старую заглушку. Пересобрал каждую через
  `express_locked_scipop(simple_tier_real, lang)`, `run.py html` применил на все страницы.
- [x] **Дешёвые AI-обложки для статей без картинки вообще.** 447/1278 (35%) статей — ни PDF-
  картинок, ни AI (`pick_cover_image()` возвращал None, `backfill_images()` просто ничего не
  писала). Карточки не ломались (юзер-фидбек #75 уже прятал пустой `.card-img-wrap`), но выглядели
  визуально непоследовательно. `backfill_images()` получил AI-фоллбэк — тот же паттерн, что у
  `backfill_tag_law_images` (dry-run по умолчанию, `gen_images=True` реально тратит,
  `image_pending`/`image_model` честно записываются). Прогнан с `preset=image_cheap`
  (FLUX-1-schnell, ~$0.002/шт) — 353/447 сгенерировано (остальные — флап промпта, можно
  перезапустить `--force` позже). `run.py images` теперь передаёт `--gen-images`/`--preset`
  в бэкфилл статей тоже, не только тегов/законов.

**Фон:** `run.py bulk-generate` (очередь 833, экспресс) продолжает через самовозобновляющийся
скрипт `bulk_generate_loop.py` (ретраит каждые 20 мин, пока не пройдёт пиковые часы DeepSeek) —
183/833 готово на момент последней проверки, лог `bulk-generate-20260716.log`.

---

> Актуально 2026-07-11 (историческая часть ниже, не обновлялась под события 2026-07-13…16 — актуальный
> статус этих дней см. в блоке выше и в auto-memory Claude, не здесь). Метки: 🟢 junior · 🟡 middle · 🔴 senior/крупное.
> **2026-07-11**: справочники выросли 30→38→48→60→70/72/60/60 (tags active/edu, laws, scientists) — теперь
> через `run.py evolve [--rounds N]` (пробел-осведомлённый рост, Трек 3 Итерация 2 — РЕАЛИЗОВАНА, см. ниже).
> Найден и починен более глубокий баг в `tag-describe.txt`/`law-describe.txt` (не только `*-refine.txt`,
> как думали раньше) — секции промта были подписаны НЕ ТЕМ уровнем сложности, чем поле, которое они
> заполняют (SIMPLE-секция писала в `description_popular`, POPULAR-секция — в `description_simple`,
> и наоборот). Все теги/законы перегенерены (`--force --refine`) с исправленным промтом + новыми полями
> `mini`/`practical_application`. Новое: `run.py daily/range --express` — дешёвый режим генерации статей
> по одной авторской аннотации (без PDF-текста, с урезанным списком тегов), см. Трек 7 ниже.
> Грабли: кэш `?v=N` теперь АВТО (`generate.asset_ver()` — sha256 всех css/js, не руками, см. changelog 07-10 #2);
> главная регенерится только через `generate.generate_index_page`; литеральный `$` в inline-JS шаблонов ломает `string.Template`;
> **UTF-8-правки НЕ через perl** (двойное кодирование ломает эмодзи/тире молча) — только Python/Edit-тул.
> Модель текста — **deepseek-v4-pro**. Картинки — **разошлись по типам сущностей 2026-07-10 вечером** (см. changelog):
> статьи — обложка из PDF (крупнейшая картинка, `pick_cover_image`), FLUX для статей БОЛЬШЕ НЕ используется (дорого,
> визуально однотипно для космоса — «шар по центру»); теги/законы — по-прежнему FLUX-2-pro, но БЕЗ авто-траты
> бюджета: `run.py images` без `--gen-images` только готовит промпт и честно метит `image_pending=True` (плейсхолдер
> `.ai-cover-ph`), реальная генерация — только явным `--gen-images`. `size: "1440x960"` (3:2, ⚠️ FLUX-2-pro отклоняет
> width>1440 400-ошибкой, не поднимать обратно к 1536). Справочники: `config.json` → tags.active_count/educational_count=30,
> laws.count=30, scientists.total=30 — активный ярус тегов НЕ идемпотентен (перегенерится заново при каждом `run.py tags`).

---

## 🐛 ТРЕК 0 — Апробация: находки по ходу (юзер смотрит прод, репортит)
Юзер пишет что видит → чиню сразу если ясно, иначе фиксирую сюда.
- [x] **Разошлись картинки статей vs тегов/законов, 2026-07-10 вечером** — юзер проверил счёт DeepInfra ($7.94 за
  529 картинок FLUX-2-pro), заодно визуально заметил, что космические темы (чёрная дыра/звезда/планета) почти
  всегда рендерятся как «один большой шар по центру» — вариация по свету/ракурсу/палитре/стилю (уже была) не
  спасает, потому что не трогает КОМПОЗИЦИЮ. Решение в два шага: 1) добавил в `IMG_VARIATIONS` (gen_llm.py) новое
  измерение `composition` (макро-текстура, деталь на краю кадра, силуэт, прибор вместо объекта и т.п.) — на будущее,
  если FLUX где-то ещё нужен. 2) Для СТАТЕЙ отказались от FLUX вообще — `pick_cover_image()` берёт крупнейшую (по
  пиксельной площади) картинку из уже извлечённых из PDF (`save_images` и так отсеивает мелочь <40KB) и копирует
  её в `ai.jpg`. Дешевле (0₽) и разнообразнее (это реальные иллюстрации из статей). `backfill_images()` и основной
  пайплайн (`write_article_pages`) переведены на неё. Для тегов/законов (у них нет исходных картинок) FLUX остался,
  но БЕЗ авто-траты: `run.py images` теперь по умолчанию только готовит промпт и честно метит
  `entry["image_pending"]=True` (рендерится существующим CSS-плейсхолдером `.ai-cover-ph`, без нового файла-заглушки)
  — реальная генерация только с явным `--gen-images`, чтобы будущий рост справочников (10→50 каждому, Трек 3) не
  сжигал бюджет молча.
- [x] `/lang/ru/archive/2026-07-06/2607.04594v1/advanced.html` — не было AI-картинки (промт трижды флапнул на генерации). **Исправлено** 2026-07-09: перегенерил промт вручную (сработал), картинка сгенерена, `run.py html` пересобран.
- [x] **Текст статьи наезжал на сайдбар тегов справа** (авторы/заголовок/описание) + **сайдбар «залипал» у самого верха** вместо старта на уровне заголовка. Причина: мой более ранний фикс `.article-side { position: fixed }` на широких экранах — выпадает из flex-потока, `.article-main` не сужается, плюс fixed не учитывает натуральную позицию контента. **Исправлено**: откатил на `position: sticky` (было и так корректно, `fixed` был лишним/вредным).
- [x] **Всё после блока комментариев (реакции/фидбэк/next/related/support/license) было не по ширине текста**. **Исправлено**: `.actions`/`.feedback`/`.next-divider`/`.support`/`.license` → `max-width: 620px` (как текст). `.related` (карточки похожих статей) сознательно НЕ трогал — они по ширине карточек ленты, не текста.
- [x] Действует на всех версиях статьи (popular/simple/advanced/mini) — общий шаблон/CSS, отдельно чинить не нужно.
- [x] **AI-картинки обрезались** (`object-fit: cover` в чужую пропорцию резало FLUX-картинки). **Исправлено, дважды**: сначала сделал квадрат 1:1 (под тогдашние 1024×1024) — юзер поправил: квадрат съедает высоту, лучше 3:2. **Финально**: `config.json → agents.image.size = "1536x1024"` (3:2), `.ai-cover`/`.card-img-wrap` → `aspect-ratio: 3/2`.
- [x] **Картинка в карточке ленты была на 2-4px выше текста категории** (оптическое несовпадение — invisible line-height над мелким текстом лейбла). **Исправлено**: `margin-top: 4px` на `.card-img-wrap`.
- [x] **Тултипы тегов/законов на списках статей показывали не тот регистр** (всегда advanced для тегов / popular-ish для законов, игнорируя переключатель Просто/Популярно/Подробно). **Исправлено**: новая `descByVersion()` в search.js — тултип теперь читает `description_popular`/`description_simple`/`description` по текущей `currentVersion`. Проверено: при выбранном «Просто» тултип показывает именно `description_simple`.
- [x] **«Я автор» — плашка, крупновата**. **Исправлено**: убрал border/фон/паддинг, 12px, подчёркнутая ссылка вместо кнопки-пилюли.
- [x] **Мини-версия: клик по карточке сбрасывал на popular** (URL карточки не учитывал currentVersion='mini'). **Исправлено** в `cardHTML()` (search.js) — в мини-режиме ссылка ведёт на `mini.html`.
- [x] **Вкладка «Мини» была не везде** (только на главной/статье — 3 vs 4 таба = дёргалась ширина шапки). **Исправлено**: `include_mini=True` везде (все `version_toggle_spans()` вызовы). На тег/закон-страницах (где нет отдельной мини-версии) — вкладка «Мини» подсвечивается активной, но контент показывает «Просто» (`displayVer` alias в `setVer()`).
- [x] **Страница автора — не было списка его тегов/законов**. **Исправлено**: `author_tags_html`/`author_laws_html` в `update_all_authors()` (законы — через пересечение тегов, как везде), 2 новых блока `.related-tags` в `author.html`.
- [x] **Граф на странице автора был узкий (620px) вместо ширины списка статей**. **Исправлено**: класс `.mini-graph-wide` (только на author.html) — `max-width: none`, проверено = 868px, совпадает с `.article-card`.
- [x] **Все элементы карточек тег/закон/учёный одной ширины** — уже было (620px везде из предыдущего прохода), доп. аудит новых блоков (author) сделан в этом же проходе.
- [x] **Формулы на странице закона рендерились с мусором** (`\\hbar` вместо `\hbar` — LaTeX ломался, `\\` парсится как перенос строки). Причина: промт `laws-describe.txt`/`tags-describe.txt` содержал инструкцию «экранируй обратные слэши», которая толкала LLM на двойное экранирование. **Исправлено**: убрал инструкцию, уточнил — одинарные слэши, ВСЕГДА расшифровка переменных + констант (название+значение). Дополнительно: одноразовый скрипт схлопнул уже существующие двойные слэши в `lang/{ru,en}/data/laws.json` (5 формул).
- [x] **Граф знаний: поиск показывал только теги** (реально: один общий datalist на ~60 узлов, браузер показывал первые совпадения по порядку вставки — теги шли первыми в данных). **Исправлено**: 3 отдельных поля+datalist (тег/закон/учёный), у каждого свой словарь имён — юзер прав, «общее решение с типизацией» было нужно.
- [x] **Кнопка «Следующая статья» — текст почти не виден** (хардкод `background:#fafafa`, светлый текст поверх светлого фона в тёмной теме). **Исправлено**: убрал фон, hover через `var(--tag-bg)`.
- [x] **Карточка в ленте — пустота внизу при короткой картинке**. **Исправлено**: `.card-actions { margin-top: auto }` — прижимает реакции к низу, если текста меньше высоты картинки.
- [x] **Контент на карточках не совпадал по ширине с картинкой** (заголовки/мозаика/формулы/секции растягивались на всю ширину колонки и торчали правее обложки, неровно). **Исправлено**: `.article-main > *` и `.tag-header` → `max-width: 620px` — весь контент теперь вровень с `.ai-cover`.
- [x] **Плашки в списке статей подсвечивались при наведении** (`.article-card:hover` — box-shadow+lift, лишний акцент). **Исправлено**: убран, оставлены только смена цвета заголовка и лёгкий зум картинки.
- [x] **Граф знаний: поля поиска не кросс-фильтровались** (выбор тега не сужал варианты в поле закона/учёного, только очищал их). **Исправлено**: `js/knowledge-graph.js` — выбор сущности сужает datalist двух ДРУГИХ полей до её прямых соседей по графу; своё поле остаётся полным; глубина `+/-` работает независимо (не влияет и не зависит от кросс-фильтра). Проверено: тег «чёрная дыра» → законы 20→4, учёные 20→4, все релевантные.
- [x] **В описаниях тегов/законов через одно — «представьте себе…»**. **Исправлено**: `tags-describe.txt`/`laws-describe.txt` — явный запрет на клише-открытие, просьба разнообразить (факт/вопрос/сравнение без вводных). Действует на новые описания вперёд; старые не переописаны (нужен `--refine`, отдельное решение).
- [x] 🐛 **Критично: AI-обложки тегов/законов в прод-режиме молча не генерились** — `agents.image.size: "1536x1024"` превышал лимит FLUX-2-pro (width ≤ 1440), КАЖДЫЙ вызов падал 400-ошибкой; промпты сохранялись (текстовый вызов дешёвый и не падал), картинки — нет. **Исправлено**: `size → "1440x960"` (те же 3:2, влезает в лимит). Перегенерено: 15 статей (были квадрат 1024×1024 с давнего прогона, устарели после смены на 3:2), 65 тегов, 30 законов — всё 1440×960.
- [x] Догенерены справочники до цели: **30 активных тегов + 30 образовательных (66 описано с ротацией) / 30 законов / 30 учёных**, `knowledge-graph.json` 126 узлов/762 ребра.
- [x] Догружены статьи за 2026-07-01…07-06 (`run.py range`): +47 статей (62 всего в архиве); 07-03 вернул 0 (все кандидаты отсеялись по лицензии — штатно, не баг).
- [x] Добавлен испанский язык (`run.py add-lang es`) — тест механизма добавления языка на полном объёме контента (справочники + весь архив).
- [x] 🐛 **Учёный: «ключевые открытия» и «области» рендерились посимвольно** (LLM иногда вернул `fields`/`key_discoveries` строкой вместо JSON-массива, `", ".join(str)`/`for d in str` разбивали её на буквы). Наступало у 9/30 учёных (30%!). Пример: `/lang/ru/scientists/Arno_Penzias.html`. **Исправлено**: `common.as_list()` — нормализует строку→список при рендере (`generate.py`) И при сохранении новых (`scientist_list.py`); существующие 9 записей нормализованы на диске одноразовым скриптом.
- [x] 🐛 **Длинная формула упиралась в скролл** (`.formula` был зажат в 620px вместе со всем текстом статьи). Пример: `2607.05335v1`. **Исправлено**: `.article-main > .formula { max-width: none }` — формулы получили всю ширину `.article-main` (718px), большинство теперь помещаются без скролла; совсем гигантские всё ещё скроллятся (это ок, запасной вариант).
- [x] **Поиск и список статей под тегом/законом/учёным были шире текста шапки** (868px vs 620px шапки). **Исправлено**: новый враппер `.entity-body { max-width: 620px }` в tag.html/law.html/scientist.html вокруг реакций/related/мини-графа/поиска/списка; картинка карточки в этом контексте сужена 300→180px (иначе тексту почти не остаётся места).
- [x] На главной не было вступительного текста. **Добавлено**: `.home-intro` (720px, центр) — объясняет миссию сайта и 4 уровня сложности словами самих вкладок; локализовано на все 6 языков (ru/en/es активны, zh/fr/ar про запас).
- [x] **Дизайн-ревью (Horizon 1), всё внедрено 2026-07-10**: архив — унифицировано меню (было `main tags laws graph authors scientists archive about` без ★, стало как везде + без лишнего пункта «archive», раз его нет в меню больше нигде); испанский получил родные подписи вкладок версий (`VERSION_LABELS`/`MINI_VERSION_LABEL` — не было ключа `es`, показывались английские); og:image статей теперь AI-обложка (было `0.jpg` — случайная картинка из PDF, часто график; так же в JSON-LD); og-теги + `<meta name="description">` добавлены на страницы тегов/законов/учёных (раньше только у статей); RSS/Atom (`feed-{lang}.xml`, ≤50 последних popular-статей, autodiscovery-тег + ссылка в футере); подсказки (`title=`) на вкладках Просто/Популярно/Подробно/Мини — поясняют регулятор глубины изложения; `loading="lazy"` на картинках карточек в ленте (search.js).
- [ ] 🔴 **Отложено сознательно**: WebP-генерация обложек (нужен новый шаг в пайплайне картинок, Pillow уже есть). Взять отдельной сессией.
- [x] **2026-07-11: аннотации статей обрезались многоточием на бо́льшей части корпуса** (45/62 popular,
  35/62 simple, 35/62 advanced) — код-подстраховка `_cap_text()` резала по границе слова с «…», модель
  систематически превышала лимит на 10-20%. Исправлено: обрезка теперь ищет границу ПРЕДЛОЖЕНИЯ (без
  многоточия, читается естественно), код-лимит поднят с запасом (350/550/900 → 500/750/1200) — цель
  промта не менялась, это подстраховка на редкий случай, а не де-факто ограничитель. Полная перегенерация
  62(→139) существующих аннотаций через `run.py abstracts --force` — ОТЛОЖЕНА по решению юзера.
- [x] **Заголовок статьи поднят в шапку** (был внизу блока авторов/меты, сейчас первым — заголовок →
  список авторов → «Я автор» (без эмодзи/подчёркивания) → мета). Плюс компактная кнопка «Следующая
  статья →» продублирована вверху страницы (полная — по-прежнему внизу), обе синхронно обновляются
  JS'ом. Найден и починен по пути баг: `.next-btn-top` наследовал `width:100%` от общего `.next-btn`.
- [x] **ℹ️-иконка интро на главной убрана из отдельной центрированной строки** — теперь маленький
  superscript-бейдж рядом с логотипом «bridge42worlds» (как ™), клик открывает поповер вместо
  разворачивания текста в потоке. Список разделов (категорий) на главной — влево вместо центра
  (сортировка по числу статей уже была в JS, просто была не видна за центровкой).
- [x] **Обложки статей**: `pick_cover_image()`/`build_article` — новый безопасный фикс дублей версий
  arXiv (см. Трек 7) применяется и тут же, независимо от экспресс-режима.
- [x] **Авто-хэш ассетов вместо ручного `?v=N`** — юзер попросил вернуться к этому и починить. `generate.asset_ver()`: sha256 от содержимого всех css/js файлов (первые 10 hex), считается 1 раз за прогон, кэшируется. Все 12 шаблонов → `?v=$asset_ver`, все 13 мест вызова `tpl.substitute()` + inline f-string archive-страницы → передают `asset_ver=asset_ver()`. Теперь физически невозможно забыть бампнуть — не поменялся файл, не поменялся хэш, посетитель не перекачивает зря; поменялся хоть на символ — хэш другой автоматически.
- [x] **Картинка в карточке ленты была выровнена по label категории, а не по заголовку**. **Исправлено**: `.card-img-wrap margin-top: 4px → 26px` (посчитано по факту через getBoundingClientRect, не на глаз).
- [x] **Список статей под тегом/законом/учёным (после сужения до 620px) стал слишком тесным для текста карточки** (картинка 180px почти не оставляла места). **Исправлено**: `#search-results` вынесен ИЗ `.entity-body` в разметке (tag/law/scientist.html) — список снова на полную ширину страницы (868px) со стандартной 300px-картинкой, а реакции/related/мини-граф/поиск остались при 620px.
- [x] **Карточка статьи в ленте переработана: картинка-врезка вместо колонки на всю высоту**. Юзер попросил пример через `mcp__visualize` (мокап с двумя вариантами side-by-side), одобрил — внедрено. Было: `.article-card{display:flex}`, картинка 300px держала свою колонку на всю высоту блока, тексту доставался остаток. Стало: `.card-img-wrap{float:left; width:130px}`, заголовок/лейбл категории обтекают картинку сверху, а как только текст её перерастает — идёт на всю ширину карточки. `.card-body` перестал быть flex-колонкой (иначе float не работает, содержимое не обтекает) — теперь обычные блоки с margin-bottom вместо gap. `.card-actions{clear:both}` — реакции всегда под картинкой, даже если текста было мало. Мобилка (≤700px) — `float:none`, картинка на всю ширину сверху, как раньше. Заодно избавились от отдельного узкого варианта карточки для entity-body (180px-картинка больше не нужна — обычная 130px-врезка одинаково хороша при любой ширине контейнера).
- [x] 🐛 **После внедрения врезки — описание (.card-desc) НЕ обтекало картинку** (заголовок обтекал, описание нет). Причина: `.card-desc` использовал `-webkit-line-clamp` (нужен `display:-webkit-box`) для обрезки в 3 строки — этот режим не участвует в обтекании float вообще (укладывает контент не построчно). Поменял на `max-height:4.65em; overflow:hidden` — не помогло: `overflow:hidden` тоже создаёт новый Block Formatting Context, из-за чего блок обтекал float ЦЕЛИКОМ прямоугольником (даже строки НИЖЕ картинки поджимались вместе с первой). **Настоящий фикс**: обрезка текста строкой в JS (`truncateText()` в search.js, по числу символов с обрывом по границе слова + «…»), никакого `overflow`/`line-clamp` на `.card-desc` больше нет — обычный блок честно обтекает float построчно. Урок: `-webkit-box` И `overflow:hidden` — оба создают BFC, оба несовместимы с построчным обтеканием float.
- [x] **В списках статей текст карточки — теперь полная аннотация, не короткий description**. Юзер: для списков «аннотация» (адаптация авторского arXiv-abstract, уже показывается на странице статьи под `<details>`) подходит лучше — содержательнее. Сделано: `update_index()`/`_index_entry()` в generate.py пишут поле `abstract` в articles-index*.json (было только у `data.json`, в индекс не попадало); `cardHTML()` в search.js берёт `item.abstract || item.description || item.oneliner` (обрезка JS-строкой, см. выше). Мини-режим не тронут — там как был, так и остаётся `threads`. Все 62 статьи уже имели `abstract` в data.json (генерится сразу в пайплайне, бэкфилл `run.py abstracts` не понадобился).
- [x] 🐛 **`_index_entry()` падал с TypeError** при первом прогоне абзаца выше — `abstract[lang]` оказался ВЛОЖЕННЫМ словарём `{popular:.., simple:..}` (аннотация тоже версионная!), а не плоской строкой, как я наивно предположил. В коде уже был готовый хелпер `abstract_for(abstract, lang, version)` с полным фоллбэком (используется на странице статьи) — просто не воспользовался им сразу. Исправлено — оба места (`update_index`/`_index_entry`) теперь зовут `abstract_for()`.
- [x] **Аннотация на странице статьи теперь ВСЕГДА развёрнута** (было `<details><summary>` по клику). Убрал `<details>`, использую готовый (ранее неиспользуемый) `.abstract-lead`/`.abstract-label` — блок с лёгкой заливкой, постоянно на виду под oneliner.
- [x] **В списке карточек текст обрезался слишком рано** (230 символов) — юзер попросил «в идеале полностью». Подняты лимиты: индекс хранит до 1500 символов (было 1000), в карточке показываем до 1400 (было 230) — подавляющее большинство аннотаций теперь влезают целиком. Заодно `.card-desc` уменьшен до 13px (было 14) для плотности.
- [x] **Убрано поле поиска сверху страницы статьи** — юзер: незачем искать, уже читаешь конкретную статью. Заодно убраны `.search-hint`/`.filters` (тот же кластер, тоже не нужны здесь).
- [x] **Список авторов на странице статьи мог занимать пол-экрана у коллабораций на сотни имён, ширина не совпадала с текстом**. Исправлено: `.authors-top` теперь `max-width:620px` + `max-height:4.5em` (~3 строки) + `overflow-y:auto` — прокручиваемый бокс. Не `display:none`/`visibility:hidden`, весь текст остаётся в HTML — на индексацию поисковиком не влияет, ограничение чисто визуальное.
- [x] **Сайдбар тегов/законов справа на странице статьи проверен на «залипание»** — юзер снова пожаловался, что едет вместе со скроллом. Живая проверка (`position: sticky`, прокрутка на 1500px) показала, что сайдбар корректно держится на `top:64px` при ширине экрана ≥700px. Баг не воспроизвёлся — скорее всего, юзер видел это на узком окне (там `.article-side` намеренно `position:static`, раскладывается в обычную строку). Ничего не менял.
- [x] **Вертикальная прокрутка на формулах** — `.formula-render { overflow-x: auto }` без явного `overflow-y` заставляет браузер САМ выставить `overflow-y: auto` (спецификация не разрешает auto по одной оси + visible по другой) — у высоких формул (вложенные дроби, пределы) иногда вылезал вертикальный скролл. Исправлено: `overflow-y: visible` явно — только горизонтальный скролл для широких формул остался.
- [x] **Порядок вкладок сложности сменён на «от сложного к простому»**: Подробно → Популярно → Просто → Мини (было Популярно/Просто/Подробно, Мини последней). `VERSION_DISPLAY_ORDER` в gen_base.py — отдельная константа ТОЛЬКО для порядка кнопок, генерация статей по-прежнему идёт в старом порядке `VERSIONS` (не хотел трогать общий пайплайн ради вёрстки). Плюс каждая вкладка получила свой серый фон, светлеющий к простому уровню (в тёмной теме — наоборот, светлее = заметнее = сложнее, чтобы «весомость» читалась в обеих темах одинаково).
- [x] **Унифицированы блоки «Связанные X» на страницах тега/закона/учёного** — было: у тега «Учёные» жили ВНУТРИ шапки (другой стиль, цветная плашка-подложка), у закона так же, у тегов «Законы» — свой `.tag-laws` со плашками-пилюлями `.law-chip`. Стало: единый хелпер `related_row(label, links)` — плоский список без подложек, мелкий шрифт, через « · », все блоки (теги/законы/учёные) внизу в одном месте (`.entity-body`), одинаковый стиль независимо от типа сущности. У страницы учёного впервые появился блок «Связанные учёные» (эвристика: другие учёные, у которых есть общие `related_tags`, своего поля «совместные учёные» в данных нет). Заодно поправил найденный по пути баг двойного двоеточия «Законы::» (label уже содержал `:`, `related_row()` добавлял свой).
- [x] 🐛 **Страница учёного показывала пустой лейбл «Related laws:»**, если у учёного не было связанных законов — `templates/scientist.html` рисовал `.related-tags`-обёртку с `<strong>Label:</strong>` жёстко в шаблоне, а не через `related_row()`, который сам решает рисовать блок или нет (пусто → пустая строка). Заменил на `$related_tags_block`/`$related_laws_block` (готовые куски HTML из `related_row()`), убрал хардкод из шаблона — теперь пустой блок просто не рендерится, как у тегов/законов.
- [x] **Кнопка «развернуть на весь экран» на графах** — юзер: «изян ничего не стоит сделать», спросил про открытие в новом окне для мобилок. Решил не через новое окно/вкладку (на мобиле неудобно — вкладка вместо разворота, плюс лишний повторный fetch данных графа) и не через нативный Fullscreen API (`requestFullscreen` нестабильно/частично поддерживается в iOS Safari для произвольных элементов, не видео), а CSS-оверлеем: класс `.graph-fs-active` (`position:fixed; inset:0; z-index:1000`) на контейнере графа + `body.graph-fs-open{overflow:hidden}` — одинаково работает на десктопе и мобиле, без разрешений и без повторной загрузки данных. Логика в одном месте — общем движке `js/force-graph.js` (кнопка `.graph-fs-btn` инжектится в `cv.parentElement`), поэтому появилась сразу на всех 7 графах сайта (мини-граф тега/закона/учёного/автора + облака тегов/учёных/законов + граф знаний) без правки шаблонов. Escape и повторный клик закрывают. Попутный баг: сначала звал только `resize()` при разворачивании — авто-масштаб узлов в физике (`step()`) ограничен ×2.2 от текущего разброса точек, так что скачок с компактного 300px мини-графа на весь экран не влезал, узлы оставались мелким комком. Исправлено вызовом `restart()` вместо `resize()` — он пересоздаёт позиции узлов уже в новых границах канваса.
- [x] 🐛 **У учёных не было переводов на en/es** — рендер молча откатывался на русский текст (`lang/{lang}/data/scientists.json` не существовал → фоллбэк на `lang/ru/...`). Раньше `scientists.json` СОЗНАТЕЛЬНО исключили из общего перевода справочников — там имя учёного = ключ словаря = id = отображаемое имя ВЕЗДЕ, а прогон через тот же LLM-перевод дицт→дикт рисковал попутно «перевести» и ключи. Написал ОТДЕЛЬНУЮ, безопасную по конструкции функцию `translate_scientists()`: шлю МАССИВ (не объект) полей-для-перевода без id вообще — ключ физически не может покинуть Python и не может быть перепутан LLM, сверяю ответ строго по ПОЗИЦИИ. `lifespan`/`related_tags`/`name` копируются из исходника без изменений. Новый промпт `data/prompts/translate-scientist.txt`.
- [x] 🐛 **Список статей внизу тега/закона не синхронизировался с переключением уровня сложности** — текст самого тега/закона переключался (свой локальный JS в `.tag-ver`), а карточки статей ниже оставались на старой версии. Причина: `id="version-toggle"` был переименован в `tag-version-toggle`/`law-version-toggle` (в `generate.py`) специально, чтобы не пересекаться с обработчиком в `search.js` — но `search.js` слушает клики именно на `#version-toggle`, чтобы перерисовать список. Убрал переименование id — оба обработчика (локальный для `.tag-ver`, и `search.js` для списка) вешаются на один и тот же элемент без конфликта, синхронно.
- [x] **Добавлен заголовок «Теги» над списком тегов в сайдбаре статьи** — у «Законы» подпись была, у тегов сверху — нет, бросалось в глаза асимметрией. Новый `SIDE_TAGS_LABEL` + `.side-tags-label` (тот же стиль, что `.side-laws-label`).
- [x] **Левое меню-навигатор статьи расширено и включено на ВСЕХ режимах** (было — только в advanced, только пункты-разделы context/methods/...). Добавлены пункты «Feedback» (`#feedback`) и «Похожие статьи» (`#related`) — есть на любом режиме независимо от наличия разделов; в advanced они идут после разделителя `.article-nav-sep` под секциями. `id="feedback"` добавлен в `build_feedback_html()`.
- [x] **Единая ширина 620px везде** — список статей под тегом/законом/учёным/автором (`#search-results`) раньше нарочно был на полную ширину (868px) под старый флекс-макет карточек с 300px-картинкой; с новым компактным float-макетом (130px) теснота ушла, сузил до 620px — теперь совпадает с шириной текста тега/закона/статьи. Заодно страница автора получила `.entity-body`-обёртку (раньше вообще не была ничем ограничена по ширине) и мини-граф на ней перешёл с `.mini-graph-wide` (удалён, больше не используется) на обычный 620px.
- [x] **Лимиты длины Аннотации перенесены в промпт генерации** (`data/prompts/adapt-abstract.txt` + `refine-abstract.txt`): simple ≤350, popular ≤550, advanced ≤900 символов. Раньше текст просто «показываем всё, как получилось» — при сужении колонки до 620px длинные аннотации стали бы слишком высокими. Решение осознанно НЕ через CSS-обрезку/«показать ещё»: контролируем длину там, где текст рождается, значит на сайте всегда видно 100% сохранённого текста без обрезки. Существующие 61 статья (средняя длина уже превышала новые лимиты — до 1520 символов на advanced) пересчитаны через `run.py abstracts --force`.
- [x] **Компактнее раздел главной**: 1) кнопка календаря `📅` переехала из отдельной центрированной строки в одну строку с языками (`.langs-row`, справа), стала маленькой круглой иконкой 24px вместо крупной пилюли с подписью — `initCalendar()` раньше подменяла текст кнопки на «📅 Calendar» через `textContent`, из-за узкой иконки текст бы вылезал — перевёл в `title` (подсказка), не в видимый текст. 2) Список категорий (13-14 штук) был крупными плашками-пилюлями (фон+рамка на каждую), занимал 2-3 строки и «кричал» — теперь плоский текстовый список через « · », как «Связанные X» на тегах/законах: те же данные, но в 2 компактные строки мелким шрифтом. 3) Вступительный текст `.home-intro` теперь скрыт по умолчанию, разворачивается САМ только при первом визите (`localStorage b42_intro_seen`), дальше остаётся свёрнутым за иконкой `ℹ️` — кликом можно развернуть повторно в любой момент.
- [x] **2026-07-12: Страница авторов без выбранной буквы рендерила ВСЕ ~2764 авторов сразу** (юзер: «очень длинный список») — `update_all_authors()` в `generate.py` теперь на индексной странице (`/authors/index.html`) рендерит только ОДНУ букву-по-умолчанию («S», либо первая непустая) вместо всех 26+1 секций; подзаголовок поясняет, что показано и что искать можно шире. Поиск (`filterAuthors()` в `js/search.js`) на этой странице теперь строит результаты из уже загруженного `authorsGraph` (а не сканирует DOM одной буквы) — так что набранный запрос находит авторов ЛЮБОЙ буквы, а очистка поля возвращает закэшированный дефолтный вид. Страницы конкретных букв (`/authors/s.html` и т.п.) не тронуты — там как раньше просто скрываются несовпадающие `.author-row`.
- [x] **2026-07-12: Найдена и починена причина «Похожие статьи» = пусто у части законов** — три отдельных бага в одной цепочке:
  1. `law_list.py::load_tag_ids()` подбирает теги законам из active+educational ЯРУСОВ ВМЕСТЕ, а статьи тегируются только из active-пула (`generate.py` читает только `tags-list.json`). Если ВСЕ теги закона оказались educational-only — закон никогда не пересечётся по тегам ни с одной статьёй, «Похожие статьи» у него навсегда пустые. Нашлось 15/60 таких законов (13 реально починимы, 2 — `spectral_theorem`/`pythagorean_theorem` — честно пропущены LLM, чисто математические, по смыслу не пересекаются ни с одним активным тегом). Новый скрипт `law_tags_repair.py` (`--apply` — LLM подбирает 1-3 доп. активных тега на закон, не трогая образовательные) — можно гонять после роста, как `reference_dedupe.py`.
  2. 🐛 Починка тегов в `laws-list.json`/`laws-graph.json` **не долетала до реального рендера страницы** — `generate_law_page()` в `generate.py` берёт теги из `lang/{lang}/data/laws.json` (снимок на момент `law_describe.py`), отдельного файла, которого репаир-скрипт не трогал. Добавлена синхронизация тегов в `laws.json` (все 3 языка) в конце `law_tags_repair.py` и в `reference_dedupe.py::_redirect_tag_refs_in_laws()`.
  3. 🐛 **Более глубокий, ранее незамеченный баг**: даже с починенными тегами страница закона показывала «Ничего не найдено» — `templates/law.html`'s `data-context-tag` заполнялся ТОЛЬКО ПЕРВЫМ (`law_tags[0]`) тегом закона, а JS (`js/search.js` `pageContext`/`applyPageContext`) на каждой загрузке страницы ПЕРЕЗАПИСЫВАЕТ серверный список статей клиентским, фильтруя по этому одному тегу — если первый по алфавиту тег закона educational (как `hilbert_space` у Гейзенберга), список статей всегда пуст, даже когда СЕРВЕРНЫЙ рендер (`set(a["tags"]) & set(law_tags)`, полный список) был правильным. Это баг более широкого масштаба, чем 15 законов — бил по ЛЮБОМУ закону, где alphabetically-первый тег не встречается в статьях. Исправлено: `generate.py` теперь пишет ВСЕ теги закона через запятую в `data-context-tag`, `pageContext.tags` (массив) + OR-фильтр в `applyPageContext()` — тег-страницы (один тег) не затронуты.

---

## ✅ СДЕЛАНО за сессию (для контекста)
- **Refine**: статьи (simple/popular, параллельно, бейдж ✦), теги, законы (raw+refined хранятся, `--refine`).
- **Регистры исправлены**: simple = самая простая (неподготовл.), popular = богаче (подготовл.), advanced = глубже.
- **Аннотация**: 3 регистра; на странице — сворачиваемый `<details>`; строгая версия в **JSON-LD `abstract`** (SEO); mini — без неё.
- **Engagement**: Юнит 1 (страница: реакции 👍/👎/⭐ + избранное + фидбэк-чипы+коммент + чекбокс сырое⇄шлифованное),
  Юнит 2 (карточки ленты: реакции+избранное), Юнит 3 (вкладка «Избранное»), Юнит 4 (сравнение сырое⇄шлифованное на тегах/законах).
- **БД Supabase**: `likes` (like/dislike/superlike) + `feedback` (чипы+коммент), автоколонки version/lang/base_id — `docs/db-setup.sql`.
- **Фидбэк локализован** (ru/en/zh/fr/ar, `data-opt` = канонические ключи).
- Миниатюры до 6; `strip_markers` (чистые карточки); config→flash; `datetime.utcnow`→tz-aware; мёртвый `search.html` удалён.
- Прогон с нуля: 23 статьи + 20 законов/20 учёных/40 тегов.
- 🆕 Убраны плашки законов из карточек ленты (шаг к лёгкости).

---

## 🎯 ТРЕК 1 — Engagement + аналитика (ближний финиш)
Фазы по зависимостям (внутри — вместе, между — последовательно).

**Фаза A — фундамент аналитики** 🟡 ✅ ГОТОВО 2026-07-09
- [x] Анонимный `user_key` (UUID в localStorage `b42_uid`) — шлётся со всеми событиями.
- [x] Контекст события в БД: `source` ('page' пока, card/list — не сделано), `entity_type` (article|tag|law|scientist), device (mobile/desktop по UA).
- [x] Таблица `views` (entity_id, entity_type, lang, source, user_key, device, created_at) + `likes`/`feedback` тоже получили user_key/device → `docs/engagement-expand-migration.sql` (⚠️ **юзер должен сам прогнать в Supabase SQL Editor** — не деструктивно, ещё не применено).
- [x] `likes.js`: `react()`/`submitFeedback()` шлют user_key+device+entity_type; `logPageView()` — переходы на страницы (не карточки/списки — это осталось).

**Фаза B — реакции/просмотры ВЕЗДЕ** 🟡 ✅ реакции+фидбэк готовы, просмотры карточек — нет
- [ ] Просмотры карточек в ленте (IntersectionObserver, `source=card`) — НЕ сделано, сейчас только `source=page` при открытии.
- [ ] `source=list` для реакций со страниц-списков — не различается отдельно.
- [x] Реакции/избранное/фидбэк на тегах/законах/учёных — `build_actions_html()`/`build_feedback_html()`, во всех 4 шаблонах.

**Фаза C — унификация тегов/законов** 🔴 (рендер-рефактор)
- [ ] Страницы тегов/законов: список-вид → карточками как статьи (реакции/комменты/избранное), граф-вид оставить, тумблер список↔граф.
- [ ] Переключалка регистра простой/популярный/полный на этих страницах.
- [ ] Аннотацию показать на карточке полной (advanced) версии («там самое место»).
- [ ] Единый компонент карточки (статьи/теги/законы) — вынести, не дублировать.

**Фаза D — поиск и граф** 🔴 (крупное, Pagefind)
- [ ] Главная: полнотекстовый поиск (Pagefind, `docs/TZ-pagefind.md`) + два поля-фильтра (по тегам / по законам).
- [x] **Граф знаний: мульти-выбор + кросс-фильтр ✅ 2026-07-11** — было 3 поля с ОДНИМ значением каждое
  (эго-граф). Стало: чипы (можно набрать несколько тегов/законов/учёных сразу), кросс-фильтр между
  ТИПАМИ всегда «И» (выбор одного типа сужает подсказки в других — свой тип себя не сужает, чтобы
  можно было свободно добавлять ещё тегов), своя выпадашка вместо нативного datalist (поиск по
  вхождению, сортировка по алфавиту). Тумблер ИЛИ/И пробовали — убрали по фидбёку (путал выбор).
  Компактная строка полей (было — растягивалась по высоте), тёмные чипы без градиентов/теней.
- [x] **Тултипы + фильтр типов на мини-графах (тег/закон/учёный-страницы) ✅ 2026-07-11** — общий
  движок `force-graph.js` получил `opts.tooltip()` (наведение → всплывающая подсказка у курсора,
  текст берёт `mini`/`description_*`/scientist `description`); чекбоксы типов узлов (`.mg-kind`) —
  та же логика, что на большом графе, но центр страницы никогда не скрывается своим же фильтром.

---

## 🎨 ТРЕК 2 — Облегчение дизайна (референс: sciencenews.org)
Принципы: воздух, «дышащий» текст, комфортная длина строки, **выключка влево (не по ширине)**, ясная иерархия, меньше шума.
> **Решение 2026-07-09:** двигаемся к стилю ГЛАВНОЙ; делаем **единым проходом по стайлгайду** (собрать шкалу шрифтов/отступов/линий → раскатать на все страницы), а не только мелкими шагами. Приоритет после контента.
> Ориентир иерархии: **основной текст крупнее, второстепенный мельче**; разделители — тонкие линии / размер шрифта, а не плашки-подложки.
- [x] Убрать плашки законов из карточек ленты. ✅
- [x] Текст статьи: выключка влево (убран justify+hyphens). ✅ `line-height` 1.8 уже ок.
- [x] Ограничить ширину текста статьи: `.article-main p` max-width 620px (~75 знаков, было 718px/87 зн.). ✅ картинки остаются шире.
- [x] Больше воздуха между блоками ✅ 2026-07-09: h1/section/tag-header/related-tags margins увеличены.
- [~] **Редизайн карточки под `index-alt.html`** — базовый вариант живёт (serif-заголовок, ai-картинка, мета), финальная сверка с sciencenews.org не проводилась отдельно.
- [ ] На advanced-версии показывать больше меты; на popular/simple — чище (сейчас одинаково).
- [ ] В списках — переключатель «что отображать» (теги/законы/учёные) вместо показа всего сразу.
- [x] Типографика: h1 везде Source Serif 4 (было несогласованно — карточка serif, страница sans), `.section-title` левым краем вместо центра. ✅ 2026-07-09.
- [x] **Единый стайлгайд** (шрифты/отступы/тонкие линии) раскатан на article/tag/law/scientist/cloud-страницы. ✅
- [x] **Формулы без подложек** ✅ — тонкие border-top/bottom вместо фона+цветной линии.
- [x] **Списки тегов/законов/учёных → колоночная раскладка** ✅ — теги по домену науки (10 разделов), законы по типу, учёные A-Я, без плашек.
- [x] **Страница графа**: переключалки в одну строку (компактный `.kg-controls`, font-size 11.5px), граф поднят. ✅ Плюс сверху добавлен поиск+глубина (см. Трек 4 #34).
- [x] **Мини-графы на карточках тег/закон/учёный**: та же логика +/- (глубина 1-4), ширина выровнена с текстом (620px, было 640 по центру). ✅ 2026-07-09.
- [x] **AI-обложки/врезки — ширина и размер**: `.ai-cover` max-width 620px + max-height 340px (было во всю ширину, «огромные»); `.fun-fact`/`.callout`/`.key-numbers`/`.quote`/`.abstract-lead`/`.feedback` тоже max-width 620px. ✅ 2026-07-09.
- [ ] Пройтись по страницам, сверяясь с референсом (sciencenews.org) — финальный визуальный аудит не делали, полагались на внутреннюю консистентность.

---

## 🌳 ТРЕК 3 — Ко-эволюция графа знаний (образовательные теги = корни, контентный рост)
Двухъярусность УЖЕ есть (active + educational lists + graph-флаг + top-up).
> ✅ **Итерация 1 (база) починена 2026-07-09**: порядок теперь теги→законы(←тегов)→учёные(←тегов+законов,
> было только тегов)→описания тегов(←учёных+законов, законов раньше не было)→описания законов(←учёных+тегов).
> Плюс `dedupe_tags_vs_laws()` в `law_list.py` — чистит теги-дубли законов на каждом прогоне.
- [x] **Итер.2 «расширение» РЕАЛИЗОВАНА 2026-07-11**: `common.sample_corpus()`/`format_corpus_samples()` —
  случайная выборка реальных статей (title/tags/category/scientists) как контекст; новые промты
  `tag-list-gaps.txt`/`law-list-gaps.txt`/`scientist-list-gaps.txt` — модель видит существующий список +
  реальный корпус и предлагает именно то, чего не хватает для точной категоризации (не слепой топ-N).
  `--gaps N` флаг на `tag_list.py`/`law_list.py`/`scientist_list.py` и на `run.py tags/laws/scientists`.
  ⚠️ Найден и починен баг: `tag_list.py` без `--gaps` для АКТИВНОГО яруса не топ-ап, а слепая
  ПЕРЕЗАПИСЬ всего списка — `run.py tags --force --refine` без `--gaps` случайно стёр 11
  пробел-сгенерированных тегов. `cmd_tags` в run.py теперь не трогает список без `--gaps`/`--rebuild`,
  если он уже существует.
- [x] **Итер.3 «стабилизация» РЕАЛИЗОВАНА 2026-07-12**: `reference_dedupe.py` — дедупликация
  тегов/законов/учёных БЕЗ обращения к LLM (сверка нормализованных имён). Два случая: (A) один и
  тот же id одновременно в active и educational списке тегов (list-level коллизия — просто убрать
  лишнюю запись + снять ошибочный флаг `educational` с узла графа); (B) разные id с одинаковым
  именем (полноценный редирект ссылок: списки/описания/*-graph.json/`data.json` статей —
  `tags`/`main_tag`/`extra_tags` на всех уровнях popular/simple/advanced × ru/en/es —
  `laws-list.json`/`laws-graph.json` (`tags`), `scientists.json` (`related_tags`)).
  Первый прогон нашёл и починил 6 реальных дублей тегов (exoplanet, superconductivity,
  cosmic_microwave_background, entropy, neutrino, redshift — все были одновременно в active и
  educational); законы/учёные оказались чистыми. `--apply` для реальной правки, без флага — dry-run
  отчёт. Рекомендуется гонять после `run.py evolve` (пока не встроено автоматически в оркестратор —
  ручной шаг).
- [x] **2026-07-12: Реальная перегенерация «в порядок»** после сессии фиксов, с явным подтверждением
  юзера по масштабу/цене перед запуском: (1) `abstract_backfill_targeted.py --apply` — 57/57
  аннотаций батча 2026-07-01…07-06 (обрубленных старым лимитом) перегенерены с новой
  sentence-boundary логикой; попутно найден и починен РЕАЛЬНЫЙ баг — `agents.abstract.max_tokens`
  (4000) иногда не хватало (модель тратила бюджет на невидимое рассуждение до 0 видимых символов,
  `finish_reason: "length"` при ПУСТОМ content) → поднято до 12000 в `config.json`; (2)
  `express_refine_backfill.py --apply` — 103/103 январских экспресс-статей получили `refine_simple()`
  задним числом (сгенерены до того, как рефайн стал обязательным шагом экспресс-пайплайна) +
  пересобран перевод simple на en/es. Заодно найдены и убиты 2 подвисания процесса (0% CPU,
  10+ минут без ответа) при паре запусков — причина: SDK-клиент без `timeout` мог ждать зависший
  запрос до 10 минут по умолчанию; добавлен явный `timeout=180` в `common.py::client`. `run.py html`
  прогнан после каждого батча.
- [x] **2026-07-12: Лайтбокс для картинок статьи + перевод подписей к рисункам** — юзер пожаловался,
  что картинки открываются в новой вкладке (неудобно) и что подписи к рисункам на RU-версии
  почему-то на английском. Оба фикса:
  1. Новый [js/lightbox.js](js/lightbox.js) — клик по картинке (в ленте мозаики ИЛИ по главной
     AI-обложке) открывает её на месте с сохранением пропорций (`object-fit: contain`), стрелки
     ‹›/клавиши ← →/Esc листают ВСЕ картинки статьи по кругу, подпись всегда видна под картинкой
     (не по ховеру, как на миниатюре). `generate.py::gen_mosaic()` больше не ставит
     `target="_blank"`; AI-обложка обёрнута в `<a class="mosaic-open">`, но открывается как
     ОДИНОЧНАЯ галерея (не мешается с рисунками статьи — `.ai-cover` не внутри `.mosaic`).
     🐛 Найден и починен реальный CSS-баг по пути: `.lightbox { display: flex }` (мой же код)
     перебивал встроенное `[hidden]{display:none}` браузера — `close()` корректно выставлял
     `hidden=true`, но оверлей визуально не пропадал (крестик «не работал»). Фикс —
     `.lightbox[hidden] { display: none; }`.
  2. Подписи к рисункам вытаскиваются regex'ом из АНГЛИЙСКОГО PDF (`extract_captions` в
     gen_arxiv.py) и раньше сохранялись в data.json плоским английским списком — показывались
     на RU/ES без перевода. Теперь `captions` — `{"en":[...], "ru":[...], "es":[...]}`, новый
     `translate_captions()` в gen_llm.py + `data/prompts/caption-translate.txt`. Новая функция
     `captions_for_lang()` в generate.py читает нужный язык (с фоллбэком на "en" для старого
     плоского формата — обратная совместимость, не крэшит на нетронутых статьях). Бэкфилл
     [caption_backfill.py](caption_backfill.py) перевёл подписи у всех 155/155 статей, у которых
     они были (795 подписей всего), без единой ошибки.
- [x] **2026-07-12: Квантовый тест-раунд** — упражнение на проверку ко-эволюции: залили контент
  по quant-ph (`--category` теперь параметризован в `fetch_arxiv`/`run.py daily|range`, раньше был
  жёстко зашит `astro-ph.*`), затем выросли справочники с явным упором на квантовую физику.
  - Статьи: 20 полных (май 2025) + 30 экспресс (июнь–август 2025), 240→290.
  - 🐛 Найден и починен реальный баг: `scientist_list.py::_save()` резал `collected[:TOTAL]`
    (TOTAL=60 из config.json) — при `--gaps` росте СВЕРХ TOTAL это молча отрезало новых учёных
    ПОСЛЕ того, как за них уже заплатили (первый прогон `--gaps 9` сгенерил 9 учёных, которые
    тут же исчезли). Убрал срез — актуально для ЛЮБОГО роста scientists сверх исходного total.
  - 🐛 Обнаружен и починен намеренный перекос промта: `scientist-list.txt`/`-gaps.txt` явно уводят
    от Эйнштейна/Ньютона к менее раскрученным именам — из-за этого их (и Кюри, Гейзенберга,
    Планка, Резерфорда, Фарадея, Ферми, Галилея, Кеплера...) не было в базе вообще. Новый режим
    `scientist_list.py --famous N` / `run.py scientists --famous N` (промт
    `scientist-list-famous.txt`) — целевой добор общеизвестных имён без этого уклона.
    Аналогично `law_list.py --important N` (`law-list-important.txt`) — добор фундаментальных
    законов независимо от корпуса (нашёл: уравнение Уилера-Девитта, процесс Пенроуза, теорема
    Пенроуза о сингулярностях, квантовый эффект Холла, закон сохранения импульса и др.).
  - 🐛 Более широкая находка: 65/129 (~половина) учёных имели ≤1 related_tags — рост подбирает
    теги из СЛУЧАЙНОЙ выборки (`SAMPLE_TAGS`), которая не всегда попадает на реально подходящие.
    Новый [scientist_tags_repair.py](scientist_tags_repair.py) (аналог `law_tags_repair.py`, но
    для учёных, по ПОЛНОМУ списку активных тегов) — починил 64/65 (Мари Кюри — честный отказ,
    для её вклада (радиоактивность) в активном пуле правда нет подходящего тега).
  - Итог роста: tags 80/76→100/95 (+25%), laws 69→79 (+14%, из них 10 через `--important`),
    scientists 69→129 (~1.87×, из них 15 через `--famous`). `--focus` (общий параметр для
    `--gaps`/`--important`, см. `common.focus_line`) — разовая тематическая приоритизация роста,
    не меняет органический пробел-анализ на будущее.
- [x] **2026-07-13: Продолжение — школьная физика + gr-qc батч + arXiv-ретраи**
  - `ARXIV_CATEGORIES` в gen_base.py дополнен — новые категории (`cond-mat.dis-nn/quant-gas/soft/other`,
    `physics.app-ph/bio-ph/med-ph`, `cs.CC/CR/NI`, `math.DS`, `q-bio.NC`) раньше показывались сырыми
    кодами вместо названий на бейджах статей.
  - `law_list.py --important` ещё раунд, фокус — школьная/стандартная общая физика: законы Ньютона
    (все три), сохранение энергии, Ом, Гук, Архимед, все 4 начала термодинамики, идеальный газ,
    Бернулли, Фарадей (индукция), Ампер, Снеллиус, сохранение момента импульса. 79→94 законов.
    `law_tags_repair.py` почистил 4 новых без связи с активными тегами.
  - **gr-qc батч**: 50 полных (май 2026, `--refine`) — прошёл чисто. Экспресс на 150 — упал с
    краша на непойманном `ReadTimeout`+429 от arXiv API (день 13 из 31). 🐛 Нашёл и починил: у
    `fetch_arxiv()` в gen_arxiv.py не было НИКАКОГО ретрая (в отличие от LLM-вызовов в common.py) —
    добавил `_get_with_retry()` с тем же нарастающим бэкоффом (10с→30с→30с, 429 — отдельно 60-90с).
    Перезапустил — но arXiv к этому моменту уже держал устойчивый рейт-лимит (мы сильно
    нагрузили API несколькими батчами подряд за сессию): с ~15 мая КАЖДЫЙ день падал в 429/таймаут
    после всех ретраев. Результат: только 27/150 экспресс-статей (вместо краша — теперь просто
    «0 статей за день», диапазон доходит до конца корректно). Итог по gr-qc: 50 полных + 27 экспресс
    = 77 статей (240 без gr-qc → 367 всего). Юзер предложил решение — прислать bulk XML с метаданными
    статей, чтобы обходить постраничные запросы к API для исторических диапазонов; ждём файл.
- [x] **2026-07-13: Kaggle bulk-кэш arXiv + bulk-select/bulk-generate + большая волна UI-багов**
  - **Локальный кэш arXiv** (`arxiv_bulk_chunk.py`): скачали Kaggle-датасет `Cornell-University/arxiv`
    (5.4ГБ, весь arXiv с метаданными), расчанковали в `data/arxiv-bulk/{YYYY-MM}.jsonl` (443 файла,
    3.1М записей). `gen_arxiv.py::fetch_arxiv_local()` — новый путь, читает локально по
    категории+дате вместо live API; `fetch_arxiv()` пробует локальный кэш первым, живой API —
    fallback. Полностью обходит рейт-лимит arXiv для исторических диапазонов. Добил gr-qc май 2026
    до 172 статей (было 77/227 план).
  - 🐛 **Реальный баг**: `build_article()` дёргал `atom_xml = requests.get(...)` и `fetch_one_arxiv()`
    БЕЗ ретрая (в отличие от соседнего `get_license()`) — ронял кандидата в архив на любом
    arXiv-таймауте. Оба места переведены на `_get_with_retry()`.
  - **`article_bulk_select.py` + `run.py bulk-select`/`bulk-generate`** — year-wide каскадный отбор:
    вместо «лучшее за сегодня» смотрим пул за N месяцев по категориям, режем в 2 прохода (10%/20%
    на батч, переиспользует промт `article-select.txt`) + 3-й проход ранжирования (`article-rank.txt`)
    + аудит лицензий ПОСЛЕ отбора (не искажает «интересность», считает честную статистику потерь).
    `article-select.txt` обогащён критериями (практическая польза, нерешённые проблемы, макро-квантовые
    эффекты, красивые аналогии, польза науке И интерес читателю). Первый прогон: пул 45947 (astro-ph.*/
    nucl-ex/nucl-th/quant-ph/gr-qc, 12 мес) → 4595 → 918 → **833 готовы к генерации**
    (`data/bulk-select/20260713-080940.json`). `bulk-generate` идёт батчами по 100, проверяет
    пиковые часы DeepSeek перед каждым батчем (`common.deepseek_peak_status()`, 9-12/14-18 Пекин).
  - 🐛 **Краш-баг**: `render_formulas()` падал `KeyError: 'latex'` на формуле без этого поля от
    модели — ронял ВЕСЬ батч (79 уже готовых статей потеряли запись, LLM-трата вникуда). Починено
    (`if f.get("latex")`) + `write_article_pages()`-циклы во всех 3 местах (`process_day`,
    `generate_ids`, `bulk_generate`) обёрнуты try/except — одна плохая статья больше не топит батч.
  - 🐛 **Mini-версия всегда была на русском для en/es** — контент строился ОДИН раз из RU
    `versions_ru["popular"]` ВНЕ цикла по языкам и переиспользовался для всех — только nav/labels
    были локализованы. Починено во ВСЕХ 3 местах генерации mini (`write_article_pages`,
    `regenerate_all_html`, `translate_article_lang`) — теперь берут `translations[tier][lang]`,
    плюс fallback на express-поле `"mini"` (у express-статей короткий текст лежит там, не в `"threads"`).
  - **Заглушки картинок убраны** — если у статьи/тега/закона нет AI-картинки, теперь просто не
    рендерим `.ai-cover`/`.card-img-wrap` (раньше показывали пустой блок с ✦/📄, место терялось).
  - **Категории arXiv унифицированы**: было ДВА словаря названий (Python `ARXIV_CATEGORIES` в
    gen_base.py и отдельная хардкоженная копия `ARXIV_CAT_NAMES` в js/search.js — расходились при
    каждом добавлении категории). Теперь единый источник: `generate.write_arxiv_categories_json()`
    пишет `data/arxiv-categories.json`, search.js фетчит его. Заодно нашёл и добавил 15 новых
    незамапленных категорий (physics.ins-det/ao-ph/hist-ph/chem-ph/pop-ph/comp-ph/class-ph/soc-ph/
    atm-clus, stat.AP, cs.MS/IT/CL/RO, math.IT).
  - 🐛 **Главная грузилась медленно** — `data/authors-graph.json` весил **82 МБ** (!) из-за
    мега-коллабораций (статьи с сотнями/тысячами авторов создавали комбинаторный взрыв в списке
    `coauthors` на каждого автора), хотя страница автора показывает только первые 15. Добавлен
    `MAX_COAUTHORS = 30` кэп в `update_authors_graph()`/`rebuild_author_graph()` — файл сжался до
    **4.1 МБ** (20×).
  - 🐛 **Кнопка «следующая статья» иногда зацикливалась A→B→A→B** — `viewedIds` в js/scroll.js жил
    только в памяти и обнулялся на каждой полной навигации; теперь персистится в sessionStorage.
  - **Архив-страница переделана**: раньше рендерила ВСЕ статьи по всем дням одной гигантской
    статичной HTML (якоря календаря на `#{date}` внутри страницы) — при росте до тысяч статей и
    тяжело, и всё видно без фильтрации. Теперь — та же лента+календарь, что на главной (JS
    `showLatest()`/`initCalendar()`, подгрузка по скроллу батчами по 12).
  - **Граф-эксплорер**: дефолтные чекбоксы `law-law`/`sci-sci` (связи ВНУТРИ типа) выключены,
    остаётся только `law-sci` (между типами) — юзер попросил именно так; пресет «каркас» тоже обновлён.
  - **Связность графа знаний**: новый `tag_connectivity_repair.py` — чинит ОБРАТНОЕ направление к
    существующим `law_tags_repair.py`/`scientist_tags_repair.py` (те чинят закон/учёного без тегов,
    этот — тег, на который не ссылается НИ ОДИН закон/учёный). Нашёл 27/99 изолированных активных
    тегов, честно починил 8 (19 — легитимный отказ, для них правда пока нет подходящего
    закона/учёного в базе, например ammonia/methane/oxygen). Все три repair-скрипта перезапущены
    заново поверх текущего состояния — donors закрыты, кроме 1 честного матиматического отказа
    (pythagorean_theorem).
  - Мелкое: главная по умолчанию открывает English (`index.html` → JS-редирект на `/lang/en/`,
    источник контента `default_lang=ru` в config НЕ трогали — это для генерации, не для UI);
    кнопка «след. статья» вверху — без скруглений и компактнее; `CULTURE_NOTES["ar"]` в gen_llm.py
    дополнен инструкцией бережно упоминать арабских учёных/философов (Ибн аль-Хайсам, Аль-Хорезми,
    Ибн Сина, Аль-Бируни), когда уместно — сам арабский язык как активный ЕЩЁ НЕ добавлен
    (`config.languages` = ru/en/es), это отдельный дорогой шаг (перевод всего корпуса), ждёт
    отдельного подтверждения по бюджету.
  - Все правки применены к КОДУ генерации (заработают при следующем `run.py html`/новых статьях) —
    полный `run.py html` по всему архиву ещё предстоит прогнать (после того как текущий
    `bulk-generate` доберёт свои батчи, иначе гонка записи файлов).
- [x] **2026-07-13/14 ночь: ещё волна UI-фиксов + arab-authors батч + новый краш-баг**
  - Убраны галочки «показать сырое/нешлифованное» (статьи, теги, законы) — `refine_toggle`/
    `compare_toggle_html` и вся связанная разметка/CSS.
  - Авторы на карточках ленты — своя строка (было втиснуто в мету), до 20 вместо 3.
  - Страница авторов: дефолтная буква при заходе — случайная (`random.choice`), не всегда "S"
    (у западных имён непропорционально большая сама по себе) — список короче при первом визите.
  - Мини-граф автора (`.mini-graph`/`author-graph.js`/`force-graph.js` на `author.html`) —
    закомментирован, не удалён (юзер: «функционал крутой, но не понятно как строить и
    отображения даже нет, потом достанем быстро»).
  - **`arab_authors_select.py`** (новый) — целевой поиск статей по ШИРОКОМУ кругу физических
    разделов (не только наш фокус astro-ph/nucl/quant-ph/gr-qc — 12 категорий вкл. cond-mat.*/
    physics.*/hep-*), эвристика по именам авторов через LLM-батчи (честно помечено в промте как
    неподтверждённая эвристика, не факт национальности). Пул 178958 кандидатов (24 мес), нашёл
    128 за 9 из 20 батчей (хитрейт ~7%, выше ожидания — остановился раньше потолка), 115/128
    прошли лицензию → **104/110 сгенерировано** (express, `run.py bulk-generate`).
  - 🐛 **Новый краш**: `update_all_authors()` упал `OSError: [Errno 22] Invalid argument:
    'lang\\ru\\authors\\:.html'` — в графе авторов завёлся мусорный "автор" с именем ровно `:`
    (артефакт парсинга списка авторов какой-то статьи), `author_slug()` не чистил спецсимволы
    `: < > " | ? *`, запрещённые в именах файлов на Windows. Починено: (1) `author_slug()`
    экранирует эти символы в "-"; (2) `update_authors_graph()`/`rebuild_author_graph()` отсекают
    "авторов" без единой буквы в имени НА ВХОДЕ в граф (защита от рецидива). Граф авторов
    пересобран (10444 авторов), мусорная запись ушла. Сами 104 статьи arab-authors уже были
    записаны на диск ДО краша — не потеряны, упал только хвост (пересборка страниц авторов/
    sitemap/feeds/status), докручен вручную отдельным вызовом.
  - Баланс на 23:28 (2026-07-13) был ¥27.27 — юзер лёг спать с инструкцией «трать экономно».
    Broader-physics батч (доп. 100-200 статей, task #91) — оставлен НЕ запущенным, сознательно:
    два параллельных генератора (основной bulk-generate 833 в очереди + arab-authors) уже
    конкурируют за скромный остаток; решение по третьему — по факту, смотря что с балансом
    к утру.
- [x] **Оркестратор `run.py evolve [--rounds N]` РЕАЛИЗОВАН 2026-07-11** — раунд за раундом растит
  tags(active+edu)/laws/scientists через `--gaps` до потолков `config.json → growth.*_max`, стоп раньше
  если все три уже на потолке. Финальный `regenerate_all_html()` в конце.
- [x] **Конфиг роста добавлен**: `config.json → growth` = `{tags_active_max, tags_educational_max,
  laws_max, scientists_max, step}` — сейчас 150/150/100/100, шаг 12 за раунд.
- [x] Рост 2026-07-11: 30→38→48→60(гэп-акт.)→70 тегов-акт. / 72 образоват. / 60 законов / 60 учёных
  (несколько раундов вручную + один `run.py evolve --rounds 1`); знаниевый граф 265 узлов/1153 рёбер.

---

## 🔴 ТРЕК 4 — Крупное / отложенное
- [ ] **#20 Локализация** → единый `ui-strings.json` + `loc_for(section,lang)` (фидбэк ✅; осталось: title реакций, «★ Избранное», ~9 loc-словарей, nav). Разблокирует наращивание языков.
- [ ] **#21 Раскол `generate.py`** (render/index/pipeline function-level; фундамент gen_llm/gen_arxiv/gen_base есть).
- [ ] **Индексы: шардинг** (`docs/TZ-indexes.md`) — отложено осознанно (чинится офлайн из data.json); тогда же унифицировать `update_index`/`_index_entry`.
- [x] **#34 Эго-граф фильтр** ✅ 2026-07-09: поиск (datalist, все теги/законы/учёные) + BFS-глубина 1-4 (+/- степпер) в `graph-explorer.html` (`js/knowledge-graph.js`) И в мини-графах на страницах тег/закон/учёный (`js/mini-graph.js`). Кап на хабах — не делали (пока не мешает при 60 узлах).

---

## 🟢 ТРЕК 5 — Мелочи / хвосты
- [ ] 🟡 Реакции/фидбэк/views — защита клиентская (anon+RLS insert=true). Rate-limit/анти-спам — отдельно.
- [ ] 🟡 Проверить миниатюры (до 6, 3 ряда) на мобиле.
- [ ] 🟡 Локализовать title реакций (👍) + «★ Избранное» (в рамках #20).

---

## 🖼️ ТРЕК 6 — Единые AI-картинки + контент карточек ✅ ГОТОВО 2026-07-09
- [x] **B1. Обложки для тегов и законов** — `backfill_tag_law_images()` в generate.py, светлый фон.
- [x] **B2. Унифицировать в `run.py images`** — `--articles-only`/`--refs-only` флаги добавлены.
- [x] **B3. Отображение в карточках тегов/законов** — `ai_cover_html` на страницах тег/закон, max-width 620px.
- [x] **A2. Поле «sci-fi»** у статей — в `generate-article-*.txt` (3 версии), условно (пустая строка если связи нет — проверено на реальных статьях, LLM не форсирует).
- [x] **A3. Блок факт+фантастика** — `trivia_html()` в generate.py, единый `.fun-fact` блок с тонким разделителем между строками.

⚠️ **Картинки тегов/законов пока НЕ перегенерены на новых 20/20** (после `reset --all --lists` их смыло вместе со старыми) —
модель в конфиге сейчас `FLUX-2-pro` (дорогая), а разрешение получено ТОЛЬКО на статьи. Нужно решить: временно
переключить на schnell для рефов и прогнать `run.py images --refs-only`, или ждать отдельного ОК на pro для рефов тоже.

> ✅ Подтверждено в коде: **рекурсивная (раундовая) генерация** законов/учёных/edu-тегов возобновляема
> (`law_list.py`, `scientist_list.py`); **шлифовка тегов/законов** (`--refine`) реализована и прогнана.
> Кросс-ссылки (учёные↔законы, теги↔законы) починены — см. Трек 3.

---

## ⚡ ТРЕК 7 — Мини-уровень тегов/законов + Экспресс-режим статей (2026-07-11)

**Мини + практическое применение (тег/закон)** ✅ ГОТОВО
- [x] Новые поля `mini` (суть за 10 секунд, максимально образно) и `practical_application`
  (зачем нужно / где применяется) — в `tag-describe.txt`/`law-describe.txt` (генерация) и
  `tag-refine.txt`/`law-refine.txt` (шлифовка), сохраняются в `tags.json`/`laws.json`.
- [x] Попутно исправлена инструкция по расшифровке жаргона: на simple ОБЯЗАТЕЛЬНО пояснять
  каждый непростой термин в скобках, на popular — где термин не был явно объяснён раньше.
- [x] Новая вкладка «Мини» на страницах тега/закона (`data-ver="mini"` в tag.html/law.html) — было:
  клик на «Мини» у тега/закона показывал контент «Просто» (не было отдельной мини-версии), теперь
  показывает настоящий mini+practical_application. `search.js` `showTagVer()` — убрана заглушка-алиас.
- [x] Все 70 активных + 72 образовательных тега и 60 законов перегенерены `--force --refine` с новым
  промтом (заодно чинит и старый баг swap simple/popular, см. шапку файла).

**Экспресс-режим генерации статей** ✅ ГОТОВО, первый батч в проде
- [x] Идея: дешёвая массовая генерация — по авторской аннотации (arXiv API, НЕ полный текст PDF),
  один комбинированный LLM-вызов (`generate_express()`/`article-generate-express.txt`) вместо каскада
  advanced→simple→popular, урезанный список тегов в промте (25 самых используемых, `tag_list_express.py`
  по `article_count`), без `--refine`. PDF всё равно качается и парсится — обложка/мозаика настоящие.
- [x] По умолчанию публикуются только `mini`+`simple` (`config.json → express.tiers`); `popular`/`advanced`
  показывают заглушку «Полная версия готовится» (оба пути рендера — `text` у popular/simple/mini И
  секции `context/methods/...` у advanced, они рендерятся РАЗНЫМ кодом, заглушка учтена в обоих).
  Клик на заблокированную вкладку — сигнал интереса (`logExpressInterest()`, таблица `views`,
  `source='express_locked'`) для приоритизации, что апгрейдить первым.
  Апгрейд до полной версии — существующий `run.py regen <id>` (полная пересборка с нуля).
- [x] `run.py daily/range --express` (+`--limit N` на `range` — общий лимит на весь диапазон дат, не на день).
  Дедуп статей заодно расширен: раньше проверялся только точный arXiv id (с суффиксом версии `vN`) —
  новая версия уже обработанной статьи считалась совсем другой (дублировалась); теперь `existing_base_ids`
  в `load_generation_inputs()` сверяет id БЕЗ суффикса версии.
- [x] 🐛 Найден при живом тестировании: mini-страница брала title/oneliner из popular ЦЕЛИКОМ (копия
  словаря + замена только поля text на threads-тизер) — если popular заблокирован (дефолт экспресса),
  на мини-странице повисал заглушечный oneliner «Полная версия готовится» вместо настоящего заголовка.
  Исправлено в обоих местах генерации mini.html (`write_article_pages` и офлайн `regenerate_all_html`):
  threads по-прежнему берём из popular (там корректно лежит поле `mini`), а title/oneliner — из simple,
  если popular оказался заглушкой.
- [x] Смоук-тест (2 статьи) + первый боевой батч **завершён и проверен** 2026-07-12: 100 статей,
  диапазон 2026-01-01…01-14 (13 дней, лимит достигнут раньше конца диапазона), архив 137→239.
  Живая проверка на сайте: mini/simple — реальный контент, popular/advanced — заглушка,
  без консольных ошибок. Один ретрай на не-RU ответ модели, штатно отработал (`_default_lang_ok`).

---

## 🤔 РЕШЕНИЯ / договорённости
- [x] Модель текста → `deepseek-v4-pro` ✅ (2026-07-09, переключено в config.json).
- [x] БД «под анализ»: user_key + device + время ✅ — есть в likes/feedback/views (миграция ждёт применения в Supabase).
- [ ] Референс дизайна — sciencenews.org (прислан 2026-07-09) — финальная сверка не проводилась.
- [x] Учёные — имена НЕ переводим, всегда английский `id` везде (решение 2026-07-09, была причина ошибок при переводе ключей).
- [x] Языки — только ru/en на время тестов (ar отключён через `run.py lang remove ar`, вернуть через `lang add`).
- [ ] **Открытый вопрос**: картинки тегов/законов — на какой модели догенерить (см. Трек 6 ⚠️).
- [ ] **Открытый вопрос**: запускать ли `run.py evolve` (рост графа знаний, Трек 3) сейчас или после апробации контента.
