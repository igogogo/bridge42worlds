# Правлено 2026-08-05 по итогам репетиции восстановления: в строке 4 не хватало перевода
# строки («python-dotenv==1.0.0Pillow==11.0.0»), из-за чего `pip install -r` падал целиком.
# На рабочих машинах не замечали — там всё давно стояло; заметно только на чистой.
requests==2.31.0
openai>=1.50.0
pypdf==5.1.0
python-dotenv==1.0.0
Pillow==11.0.0
# R2/S3: заливка сайта, резервная копия, восстановление.
boto3>=1.34.0
# Проверка, что публикуемое не пересекается с .gitignore (cloudflare/publish_rules.py).
pathspec>=0.12.0
